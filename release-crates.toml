[release]
version = "0.21.0"
milestone = "0.21.0"
baseline = "0.20.0"
cumulative_milestones = ["0.21.0"]
policy = "independent"
stage = "internal"
exceptional = true
exception_reason = "Canonical ASN.1 semantic decoding extends the hostile DER parser boundary and requires an exceptional pentest before its internal tag."

[crates."brynja"]
previous_version = "0.20.0"
version = "0.21.0"
change = "code"
publish = false
reason = "Advance to v0.21.0 and expose canonical ASN.1 primitives and ordered containers without crates.io publication."

[crates."brynja-core"]
previous_version = "0.9.0"
version = "0.9.0"
change = "metadata"
publish = false
reason = "Retain published 0.9.0 unchanged at the internal v0.21.0 milestone."

[crates."brynja-crypto"]
previous_version = "0.1.2"
version = "0.1.2"
change = "metadata"
publish = false
reason = "Retain published 0.1.2 unchanged; cryptographic effects remain unimplemented."

[crates."brynja-crypto-cpu"]
previous_version = "0.1.1"
version = "0.1.1"
change = "metadata"
publish = false
reason = "Retain published 0.1.1 unchanged; no executable backend is admitted."

[crates."brynja-crypto-cpu-std"]
previous_version = "0.1.1"
version = "0.1.1"
change = "metadata"
publish = false
reason = "Retain published 0.1.1 unchanged; no runtime detector is implemented."

[crates."brynja-pki"]
previous_version = "0.2.0"
version = "0.2.0"
change = "code"
publish = false
reason = "Retain published 0.2.0 while adding canonical ASN.1 primitives and ordered containers for the future v0.25.0 checkpoint."

[crates."brynja-protocol"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 shared record framing unchanged."

[crates."brynja-tls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the TLS router remains unimplemented."

[crates."brynja-tls12"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the TLS 1.2 engine remains unimplemented."

[crates."brynja-tls13"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the TLS 1.3 engine remains unimplemented."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the TLS 1.3 handshake engine remains unimplemented."

[crates."brynja-quic-tls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; QUIC-TLS remains unimplemented."

[crates."brynja-sanitization"]
previous_version = "0.1.1"
version = "0.1.1"
change = "metadata"
publish = false
reason = "Retain published 0.1.1 unchanged and isolated from facades, engines, defaults, and FIPS boundaries."

[crates."brynja-dtls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the DTLS engine remains unimplemented."

[crates."brynja-platform"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; platform and provider effects remain unimplemented."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Retain deterministic clock, random, and RFC 9850 support; v0.18 authority tests remain internal to brynja-core and production-unreachable."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
