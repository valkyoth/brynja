[release]
version = "0.24.20"
milestone = "0.24.20"
baseline = "0.20.0"
cumulative_milestones = ["0.21.0", "0.22.0", "0.22.1", "0.22.2", "0.22.3", "0.23.0", "0.23.1", "0.23.2", "0.23.3", "0.23.4", "0.24.0", "0.24.1", "0.24.2", "0.24.3", "0.24.4", "0.24.5", "0.24.6", "0.24.7", "0.24.8", "0.24.9", "0.24.10", "0.24.11", "0.24.12", "0.24.13", "0.24.14", "0.24.15", "0.24.16", "0.24.17", "0.24.18", "0.24.19", "0.24.20"]
policy = "independent"
stage = "internal"
exceptional = true
exception_reason = "Portable legacy acceptance and security-sensitive assurance scope selection require an exceptional pentest before tagging."

[crates."brynja"]
previous_version = "0.24.19"
version = "0.24.20"
change = "metadata"
publish = false
reason = "Advance internal facade metadata for frozen legacy portable acceptance and focused assurance; no production API or publication change."

[crates."brynja-legacy-md5"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Complete isolated portable MD5 with ordinary and hardened byte/bit APIs; no modern admission or publication."

[crates."brynja-legacy-sha1"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Implement the complete isolated legacy SHA-1 leaf with ordinary and hardened byte/bit APIs; retain it unpublished until reviewed checkpoint admission."

[crates."brynja-core"]
previous_version = "0.9.0"
version = "0.9.0"
change = "metadata"
publish = false
reason = "Retain published 0.9.0 unchanged at the internal v0.21.0 milestone."

[crates."brynja-crypto"]
previous_version = "0.1.2"
version = "0.1.2"
change = "code"
publish = false
reason = "Retain the published version while its internal source reexports all four complete ParallelHash/ParallelHashXOF constructions without publication."

[crates."brynja-hash-core"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Extend the unpublished no_std hash interface boundary with one canonical borrowed arbitrary-bit input descriptor while retaining it for v0.25.0."

[crates."brynja-hash-sha2"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Retain the unpublished fully implemented SHA-2 family after combined ordinary, arbitrary-bit, hardened, package-external and explicit backend-disposition acceptance."

[crates."brynja-hash-sha3"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Retain the unpublished SHA-3 leaf version while adding complete SP 800-185 encodings and cSHAKE128/cSHAKE256 ordinary, arbitrary-bit, streaming, incremental-output and hardened APIs without publication."

[crates."brynja-mac-kmac"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Add the unpublished no_std KMAC leaf with complete KMAC128/KMAC256 and KMACXOF128/KMACXOF256 ordinary, arbitrary-bit, streaming, hardened-output and constant-time verification APIs."

[crates."brynja-hash-tuple"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Add the unpublished no_std TupleHash leaf with complete TupleHash128/TupleHash256 and TupleHashXOF128/TupleHashXOF256 ordinary, arbitrary-bit, exact-length streaming-item, incremental-output and hardened APIs."

[crates."brynja-hash-parallel"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Add the unpublished allocation-free no_std ParallelHash leaf with all four fixed/XOF identities, arbitrary-bit, hardened, and caller-scheduled APIs."

[crates."brynja-hash-parallel-std"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Add the unpublished opt-in bounded native-thread executor with deterministic merge, cancellation, panic containment, and secret leaf-storage cleanup."

[crates."brynja-crypto-cpu"]
previous_version = "0.1.1"
version = "0.1.1"
change = "code"
publish = false
reason = "Retain published 0.1.1 while adding unadmitted x86_64 AVX2 and AArch64 SHA3 Keccak candidates, direct KAT/quarantine behavior, and explicit RISC-V Keccak scalar-only disposition for v0.25.0."

[crates."brynja-crypto-cpu-std"]
previous_version = "0.1.1"
version = "0.1.1"
change = "code"
publish = false
reason = "Retain published 0.1.1 while extending honest family reporting and scalar fallback; all candidates remain unadmitted and RISC-V automatic detection stays disabled."

[crates."brynja-pki"]
previous_version = "0.2.0"
version = "0.2.0"
change = "code"
publish = false
reason = "Retain published 0.2.0 while adding canonical ASN.1 primitives and ordered containers for the future v0.25.0 checkpoint."

[crates."brynja-protocol"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 shared record framing unchanged."

[crates."brynja-tls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; the TLS router remains unimplemented."

[crates."brynja-tls12"]
previous_version = "0.1.8"
version = "0.1.8"
change = "dependency"
publish = false
reason = "Retain published 0.1.8 while its inherited brynja-crypto dependency advances; the TLS 1.2 engine remains unimplemented."

[crates."brynja-tls13"]
previous_version = "0.1.8"
version = "0.1.8"
change = "dependency"
publish = false
reason = "Retain published 0.1.8 while its inherited brynja-crypto dependency advances; the TLS 1.3 engine remains unimplemented."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.8"
version = "0.1.8"
change = "dependency"
publish = false
reason = "Retain published 0.1.8 while its inherited brynja-crypto dependency advances; the TLS 1.3 handshake engine remains unimplemented."

[crates."brynja-quic-tls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; QUIC-TLS remains unimplemented."

[crates."brynja-sanitization"]
previous_version = "0.1.1"
version = "0.1.1"
change = "dependency"
publish = false
reason = "Retain published 0.1.1 while source advances its exact first-party sanitization pin to reviewed 2.0.4; keep the adapter isolated from facades, engines, defaults, and FIPS boundaries."

[crates."brynja-dtls"]
previous_version = "0.1.8"
version = "0.1.8"
change = "dependency"
publish = false
reason = "Retain published 0.1.8 while its inherited brynja-crypto dependency advances; the DTLS engine remains unimplemented."

[crates."brynja-platform"]
previous_version = "0.1.8"
version = "0.1.8"
change = "metadata"
publish = false
reason = "Retain published 0.1.8 unchanged; platform and provider effects remain unimplemented."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Retain deterministic clock, random, and RFC 9850 support; v0.18 authority tests remain internal to brynja-core and production-unreachable."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
