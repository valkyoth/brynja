[release]
version = "0.7.0"
policy = "independent"
stage = "public"

[crates."brynja"]
previous_version = "0.6.0"
version = "0.7.0"
change = "code"
publish = true
reason = "Mandatory facade release exposing the v0.7 transactional borrowed read cursor and prior value domains."

[crates."brynja-core"]
previous_version = "0.3.0"
version = "0.4.0"
change = "code"
publish = true
reason = "Add the allocation-free v0.7 transactional borrowed read cursor with exhaustive truncation and no-mutation tests."

[crates."brynja-crypto"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-crypto package change; retain the published 0.1.0 boundary."

[crates."brynja-pki"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the published dependency boundary to brynja-core 0.4.0; PKI remains unimplemented."

[crates."brynja-tls"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the modern router boundary to the v0.7 core and version-engine package graph; the router remains unimplemented."

[crates."brynja-tls12"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the TLS 1.2 boundary to brynja-core 0.4.0 and brynja-pki 0.1.3; the engine remains unimplemented."

[crates."brynja-tls13"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the TLS 1.3 stream boundary to brynja-core 0.4.0 and brynja-tls13-handshake 0.1.3; the engine remains unimplemented."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the record-independent handshake boundary to brynja-core 0.4.0 and brynja-pki 0.1.3; the engine remains unimplemented."

[crates."brynja-quic-tls"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the QUIC-TLS adapter boundary to brynja-core 0.4.0 and brynja-tls13-handshake 0.1.3; the adapter remains unimplemented."

[crates."brynja-dtls"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the DTLS boundary to brynja-core 0.4.0 and brynja-pki 0.1.3; the engine remains unimplemented."

[crates."brynja-platform"]
previous_version = "0.1.2"
version = "0.1.3"
change = "dependency"
publish = true
reason = "Repin the platform adapter boundary to brynja-core 0.4.0; provider implementations remain future work."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only test support; it is never a crates.io product."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
