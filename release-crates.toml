[release]
version = "0.3.0"
policy = "independent"
stage = "public"

[crates."brynja"]
previous_version = "0.2.0"
version = "0.3.0"
change = "code"
publish = true
reason = "Mandatory facade release for the standards source-ledger milestone; it adds no protocol implementation."

[crates."brynja-core"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-core package change; retain the published 0.1.0 boundary."

[crates."brynja-crypto"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-crypto package change; retain the published 0.1.0 boundary."

[crates."brynja-pki"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-pki package change; retain the published 0.1.0 boundary."

[crates."brynja-tls"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-tls package change; retain the published 0.1.0 router boundary."

[crates."brynja-tls12"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-tls12 package change; retain the published 0.1.0 engine boundary."

[crates."brynja-tls13"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-tls13 package change; retain the published 0.1.0 engine boundary."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-tls13-handshake package change; retain the published 0.1.0 boundary."

[crates."brynja-quic-tls"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-quic-tls package change; retain the published 0.1.0 adapter boundary."

[crates."brynja-dtls"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-dtls package change; retain the published 0.1.0 engine boundary."

[crates."brynja-platform"]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No brynja-platform package change; retain the published 0.1.0 adapter boundary."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only test support; it is never a crates.io product."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
