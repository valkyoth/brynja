# Security Controls

Status: v0.20.0 signed and published; v0.21.0 pentest PASS/PASS and awaiting green hosted checks

| Control | Foundation enforcement |
| --- | --- |
| Dependency surface | Exactly one external package is admitted: exact first-party `sanitization 2.0.3`, reachable only through separately selected `brynja-sanitization`, with package/source hashes, no default or selected feature, no transitive package, inherited unsafe inventory, online freshness, and broken-fixture enforcement; every other external package is rejected |
| First-party Rust cryptography | The permanent golden rule requires every Brynja primitive, construction, key operation, protocol cryptographic operation, CPU backend and FIPS service to be implemented from first-party Rust source; policy rejects foreign/native source and binary artifacts, build scripts, build dependencies, Cargo native-link metadata, foreign ABIs, native link attributes and included native binaries, with nine broken fixtures; separately gated Rust intrinsics or inline assembly remain Brynja-owned symbols, never external modules |
| CI tool provenance | Exact versions and independently pinned `.crate` SHA-256 hashes are verified before installing security and SBOM tools |
| Unsafe Rust | Workspace lint denies it by default; a machine inventory permits exactly five source-hash-bound modules for the private volatile clearer, SHA-256 attestation, x86_64 SHA, AArch64 SHA2, and opt-in std detector boundaries, with exact block/item/proof/intrinsic counts; every other allowance, unsafe block/item, assembly, or FFI site is rejected |
| `no_std` | Every package declares `#![no_std]` |
| Package classification | Committed policy classifies all 28 packages and exact direct, optional, external, feature, target, source, and publication boundaries |
| Modern/legacy isolation | No-default and all-feature graph validators reject either facade reaching the other package class, research, or repository tooling |
| TLS version isolation | Both resolved graphs require the evergreen router to reach separate TLS 1.2 and TLS 1.3 engines while QUIC reaches only the recordless TLS 1.3 handshake |
| Review size | Source files over 500 lines fail checks |
| Toolchain | Rust 1.97.1 release pin plus explicit 1.90.0-1.97.1 matrix; Kani 0.67.0 is separately paired with Rust 1.90.0 and cannot constrain or redefine normal builds |
| Assurance harness | First-party deterministic mutation/replay and canonical external-process differential runners enforce public-test-input, case-count, timeout, simultaneous output, no-shell, parser, class, adapter-count, and mismatch boundaries; campaign OS sandboxing remains an explicit caller duty |
| Bare metal | Complete all-feature workspace compiles on ARMv7E-M, RV32IMAC, and x86_64 OS-less targets without claiming runtime or platform services |
| External assurance tools | Exact Kani, AFL++, honggfuzz, Miri, and sanitizer version/revision pins are machine checked and forbidden from Cargo manifests; policy-only or skipped evidence is never a proof |
| Standards | Exact HTTPS host/path and redirect allowlists, independently reviewed non-self-replacing pins, bounded responses, DTD/entity rejection, 104 immutable RFCs, 15 local-only NIST/ITU authorities, exact RFC-index and eight IANA snapshots, 290 reviewed errata decisions, lifecycle and milestone ownership, complete updated-by/obsoleted-by closure, deterministic ledger generation, broken fixtures, and release-time live drift rejection |
| Protocol surfaces | Deterministic classification of 127 semantic decisions, 195 nested registries, and 4,126 individual records across all eight pinned IANA collections with exact source-ledger binding, disposition, milestone, code target, test target, and broken-fixture enforcement |
| Failure domains | All 256 alert registry bytes retain assigned, reserved, or unassigned identity; local alert selection is protocol-version-aware; close and cancellation cannot enter `TlsFailure`; failure envelopes accept no arbitrary text or bytes and implement no formatting traits |
| Numeric and resource domains | Private-field compile-time bounds, checked arithmetic, distinct count/length types, fail-closed pointer-width conversion, non-wrapping sequence/epoch advancement, named single-assignment immutable budget construction with typed duplicate/incomplete-domain errors and without positional arguments, defaults, or mutable setters, and limit-value-free typed exhaustion are enforced by exhaustive small-domain matrices, every-domain duplicate and omission tests, boundary tests, representation tests, a workspace-wide overlong-argument lint, and compile-fail doctests |
| Borrowed read cursor | Private input/position state, checked end offsets, bounds-checked slice access, exact dynamic/typed/fixed-array borrowing, success-only advancement, consuming trailing-data checks, non-clonable and non-formattable cursor state, and value-free errors are enforced by every-position/request matrix tests, truncation at every byte, every trailing suffix, overflow/zero-length/identity/storage tests, compile-fail doctests, and the workspace no-indexing lint |
| Transactional write cursor | Private output/position state, exclusive caller-buffer borrowing, checked complete-range and aggregate-part preflight, exact single-slice/multi-part/repeated-byte mutation, success-only advancement, byte-for-byte failure preservation, immutable written-prefix inspection, consuming exact-capacity completion, non-clonable and non-formattable cursor state, and value-free errors are enforced by every-position/request matrices, whole-buffer sentinels, multi-part, overflow, zero-length, empty-output, identity/storage, compile-fail, `no_std`, and no-indexing tests |
| Workspace and arenas | Named single-assignment secret/plaintext/transcript/certificate/output layout, checked aggregate length, exact backing-length admission, one safe fixed-order split, sealed zero-sized compile-time domain identities, named simultaneous domain borrows, monotonic complete-range allocation, per-domain high-water and successful non-empty allocation counters, retained-byte complete-initialization duty, byte- and accounting-transactional failures, private non-clonable/non-formattable state, and value-free errors are enforced by every-domain duplicate/omission checks, exhaustive small layouts, every-position/request matrices, overflow/exhaustion/zero-length/sentinel/pointer-identity/isolation/representation tests, compile-fail exclusive-borrow/domain-swap tests, `no_std`, and no-indexing policy; domain names classify raw storage only, certificate storage is not private-key storage, and sensitive consumption is prohibited until v0.10 lifetime states and the v0.11 proven destruction primitive exist |
| Secret lifecycle | The v0.10 abstract contract retains all-target destruction duties and failed-Drop handling; v0.11 separately owns one complete caller allocation by exclusive borrow, clears old bytes before write-only sequential initialization, admits read access only after exact completion, and clears the complete region on incomplete finish, both Drop paths, or explicit clear |
| Owned-memory zeroization | One private per-byte volatile-store loop plus compiler barrier is checked at MIR, LLVM IR, and assembly levels across ten stable compilers and nine targets, with pinned Miri and AddressSanitizer; the exact claim excludes registers, copies, caches, DMA-visible copies, dumps, suspend images, physical memory, concurrency, forgotten owners, and termination |
| Constant-time foundation | Private normalized one-byte `Choice` and `CtMask` values, one named public declassification, and equality/selection/swap over unsigned words and compile-time-sized byte arrays are enforced by exhaustive tests, compile-fail examples, hash-locked source policy with fourteen broken fixtures, mask optimization barriers, and optimized LLVM/function-body assembly witnesses across ten stable compilers and nine targets; target-specific checks reject non-array conditional branches, non-public fixed-array branches, and direct RV32 Choice-register branches or memory addressing after argument-register alias canonicalization and classification of all eighteen conditional forms, with ten focused assembly fixtures; dynamic slices, secret-dependent lengths, signed values, arbitrary composition, timing measurement, proof, and microarchitectural guarantees remain excluded |
| Provider authority | Nineteen direction-specific operations including separate MAC generation/verification, immutable single-assignment capabilities, named transactional installation, frozen caller resource/work limits, mandatory nonempty destruction duties, opaque borrowed handles, exact-operation authorization, exact installed-provider retention, immutable version-neutral request metadata, verification byte-output prohibition, no request-side result constructors, monotonic provider-owned work charging, and pre-effect resource checks are enforced by nine behavioral tests, six compile-fail examples, a hash-locked four-file policy and thirteen broken fixtures; registry search, fallback, mutable request output, provider-native IDs, caller-supplied work, upstream platform dependency, platform effects, algorithm policy, and FIPS approval remain excluded, while pending lifecycle is owned separately by the v0.16 upstream contract |
| Entropy and secure randomness | Affine exact-purpose raw input, declared strength capacity, exact lengths, non-cloneable initialized state, runtime-generation/fork binding, bounded successful-request reseed, transactional caller-owned output, complete clearing, retryable/permanent faults, terminal quarantine, and mandatory explicit/Drop destruction-failure handling are enforced by core and deterministic-fixture tests plus a SHA-256-bound source policy and nine broken fixtures; arbitrary bytes gain no entropy-quality or FIPS claim, and no DRBG, algorithm, OS RNG, FFI, platform source, or production test provider exists |
| Wall and monotonic time | Separate canonical signed Unix wall values and opaque generation-bound monotonic instants prevent domain interchange; checked duration, carry, borrow, range, direction, generation, purpose, and deadline arithmetic, explicit value-free unavailability, equal-tick admission, terminal rollback latching, redacted ticks, deterministic unpublished sources, compile-fail examples, exact source hashes, and nine broken fixtures enforce the boundary; no OS clock, PKI validation, timer engine, ticket service, replay store, cryptographic result, or FIPS evidence exists |
| Pending operation lifecycle | Exact certificate-path, external-signature, and accelerator-eligible admission requires the same installed provider's identity, poll/cancel capabilities, and applicable destruction targets; identity is rechecked after every effect-free phase, including guarded preparation immediately before activation, provider-derived effect-free nonzero costs are authoritatively charged before a non-forgeable permit exists, effect-free preparation creates inert local state that the lifecycle owns before borrowed activation may create external resources, affine no-state retries preserve ownership, activation/resume/cancel/destruction borrow lifecycle-owned opaque state across unwinding, checked effect/backpressure limits fail closed, and completion/cancellation/provider-failure/exhaustion/Drop consume destruction authority covering frozen external-key, accelerator, cache, DMA, and other duties before an authoritative terminal result exists; sixteen deterministic/adversarial tests, four compile-fail examples, exact source hashes, twenty-one broken fixtures, and mandatory failed-Drop handling enforce the upstream contract without implementing any provider, key store, accelerator, algorithm, platform effect, FIPS service, or independent-verification claim |
| FIPS-aware provider architecture | Broad operation-category sets classify every current installed-provider capability explicitly non-approved; every nonempty approved set fails until exact algorithm, parameter, backend and usage identities span execution; nonzero build digests, one exact module-owned scalar/accelerated environment and feature bundle, explicit SSP flow and provider-derived destruction duties freeze without admitting ordinary sealed-provider identity, `BackendPolicy`, opportunistic dispatch, std detection or the std CPU adapter; an explicit trusted application-implementable runner receives the mandatory integrity/KAT plan but grants no execution or approved status, while v0.125.0/v0.127.0 require an opaque unforgeable module-owned attestation issued only by complete final-image tests before either can become reachable; private guard Drop, reentry, rejection, impossible transition, generation exhaustion and catastrophic events permanently fail the caller-owned session; this latch is intentionally session-scoped while no service is approved or executable, and v0.127.1 requires module-wide sibling-proof failure before that changes; non-cloneable thread-bound informational service indicators report operation, disposition, provider and health generation, authorize no execution, expose no broad provider handle and become stale after failure; six behavior groups, four compile-fail examples, exact source hashes and twenty-four broken fixtures enforce the boundary without implementing a module, algorithm, provider effect, self-test algorithm, CPU kernel, build reproduction, SSP effect, CMVP submission, certificate, official validation or independent verification |
| Mandatory security outcomes | Sealed type-level domains distinguish all seventeen planned decision classes; one allocation-free caller-owned authority permits one checked-generation-bound incomplete decision and returns exhaustive accepted, approved, non-approved, rejected, pending, canceled, failed, or terminal results; public resolutions cannot forge accepted or approved authority, future positive paths require sealed exact-subject evidence, resolved non-terminal work remains `AwaitingCommit` until an opaque disposition-specific result is explicitly committed, accepted/approved/non-approved/rejected/canceled/failed values cannot be interchanged, rejection/failure reasons are read-only, commit checks the exact disposition retained by the authority, dropping pending work or an outcome permanently fails closed, mandatory self-test failure latches terminal integrity failure, and explicit terminal decisions cannot claim ordinary success; external-key success consumes one non-cloneable thread-bound authority/generation token for the external-store target and duplicate, substitution, provider-failed, or abandoned completion fails closed; fourteen behavior tests, seven compile-fail examples, six reviewed hashes, the 500-line ceiling, and twenty-nine broken fixtures enforce no std/alloc/unsafe/FFI/provider-effect/audit-event crossing without implementing policy, authentication, protocol selection, ticket, replay, ECH, provider effects, external storage, cryptography, protocol engines, independent verification, or FIPS validation |
| Observational security events | Opaque events can only duplicate typed v0.18 pending/outcome values or non-ready snapshots; closed event data contains exact available decisions, dispositions, or terminal reasons but no authority generation, secret, handle, identity, plaintext, transcript, PSK identity, ticket, ECH inner name, arbitrary string, byte payload, or stable cross-connection identifier; records are explicitly untimestamped until one caller wall/monotonic timestamp is supplied and cannot be relabeled; a fixed-capacity caller-owned FIFO has deterministic order, no callback/I/O/retry/wait/effect path, immediate full/zero-capacity loss, and exact-until-saturated visible drop accounting; ten integration tests, two internal tests, three compile-fail examples, four reviewed hashes, the 500-line ceiling, and twenty-two broken fixtures enforce that missing, ignored, delayed, duplicated, or dropped events cannot authorize, commit, complete, latch, alert, mutate, or obscure mandatory state and that no sink, delivery, persistence, serialization, protocol, cryptographic, independent-review, or FIPS claim exists |
| TLS and DTLS record framing | One `no_std`, allocation-free shared crate accepts only an externally selected typed TLS 1.2, TLS 1.3, DTLS 1.2, or DTLS 1.3 policy; borrowed parsers and transactional caller-buffer encoders preserve permitted wire bytes, enforce profile-specific header constants and bounds, reject malformed/truncated records and RFC 6520 Heartbeat in every modern profile, and categorically reject TLS 1.3 application data from unprotected wire records; it cannot negotiate, downgrade, decrypt, authenticate, reconstruct DTLS sequence numbers, enforce replay, perform I/O, or transition a handshake; eighteen behavior tests, three compile-fail examples, seven reviewed source hashes, the 500-line ceiling, and thirty broken fixtures enforce the boundary |
| Bounded DER framing | One safe-Rust `no_std`, allocation-free, non-recursive reader borrows exact header/content/encoded slices and emits primitive, constructed-start, and balanced-end events under immutable input, depth, node, child, identifier, length, value, work, and fixed-stack ceilings; canonical low/high tags, definite minimal lengths, universal end-of-contents rejection, pre-access parent-boundary checks, checked offsets, parent containment, and failure-atomic position are enforced by fourteen tests, three compile-fail examples, an exhaustive 65,536-input two-octet corpus, six reviewed hashes, the 500-line ceiling, and thirty-three broken fixtures; the scheduled assessment's Low semantic-boundary oracle is remediated and repository-owner retest passed with zero open findings; type-specific ASN.1 canonicality, SET ordering, X.509, cryptography, authentication, independent verification, and FIPS validation remain absent |
| Canonical ASN.1 values | Borrowed safe-Rust validators enforce exact DER BOOLEAN, minimal two's-complement INTEGER, BIT STRING padding, OCTET STRING, minimal terminated base-128 OID arcs, admitted restricted/Unicode strings, real UTC/GeneralizedTime calendars, nested SEQUENCE, direct-tag-ordered SET, and padded-octet-ordered SET OF under the existing immutable DER limits; nine behavior groups include exhaustive 256-value Boolean and 65,536-value bit-padding/OID corpora, six compile-fail examples, ten reviewed hashes, the 500-line ceiling, and forty broken fixtures; schema decoding, DEFAULT omission, escape-bearing ISO 2022 strings, AlgorithmIdentifier, X.509, cryptography, authentication, independent verification, and FIPS validation remain absent |
| CPU backend authority | Sealed backend identities and exact feature/operation profiles remain observational until opaque measured-artifact/environment evidence and a direct trusted-provider KAT bound to the exact session and instance yield thread-bound active authority; caller-owned no-atomics state, monotonic health/runtime generations, permanent quarantine, exact-operation revalidation, exact-session CPU leases, sealed context/guard/kernel traits, migration exclusion held across direct execution, CPU/hart, migration, complete-feature and OS/architecture-state revalidation, post-callback logical revalidation, explicit opportunistic fallback, and fail-closed required/validated policies are enforced by thirteen behavior groups, eleven compile-fail examples, exact source hashes, and twenty-three broken fixtures; public instance/lease/context/guard/kernel construction, application callbacks, CPU probing, ISA kernels, unsafe code, global caches, performance admission, provider effects, and FIPS validation remain excluded |
| CPU acceleration boundary | `brynja-crypto-cpu` contains zero-dependency `no_std` x86_64 SHA and AArch64 SHA2 candidates, static selection, direct KAT, caller-owned health and permanent quarantine; separate opt-in `brynja-crypto-cpu-std` contains runtime detection and explicit fallback/required modes. Exact source/policy hashes, ABI/feature bundles, unsafe allowances, emitted instructions, graph isolation and twenty broken fixtures keep both candidates unadmitted and ordinary execution scalar/fail-closed until native evidence is complete; both stay outside the facade, engines, defaults, bare metal and FIPS graphs |
| CPU evidence admission | Hash-bound machine policy registers five native and three supplemental QEMU lanes, thirteen harnesses, exact recorded provenance, measured-binary and machine-readable artifact fields, repository-wide LF checkout, signed-64-bit-only JSON parsing, backend-specific ABI/operating-state equality, 90-day freshness, one-logical-CPU runs, balanced benchmark order, noise, speedup, code-size and cold-start budgets, and explicit statistical residual risk; a persistent clean-commit local/SSH manager detaches native candidate checks, clones the exact remote commit, retrieves bundles and locally rejects lane, source, transcript, instruction, symlink and checksum substitution, but labels every such bundle non-authorizing; recorded owner metadata and hashes are explicitly not authentication, so all candidate/native claims remain forbidden until a reviewed trusted-runner verifier exists; a deterministic zero-admission ledger, 55 schema plus ten runner broken fixtures, and a dependency-free `no_std` scalar/mock state fixture reject checkout-byte drift, self-asserted results, artifact semantic drift, oversized/float/non-finite JSON, disabled or meaningless vector state, fabricated native labels and vendor/model substitution, stale or mixed-CPU evidence, non-finite/noisy/biased measurements, resource-bound violations, QEMU promotion, required-mode fallback, KAT/quarantine/differential failures, and unavailable-runner support claims |
| Sanitization adapter | One separately selected non-copyable fixed-size wrapper provides closed construction/replacement errors, redacted diagnostics, closure-scoped inspection, explicit clear, and named exact-length copies to/from Brynja regions; metadata and lock policy reject facade, engine, legacy split, feature, transitive-package, `zeroize`, and FIPS-boundary activation, while behavior, compile-fail, differential, Miri, and emitted-code tests cover the boundary |
| Test-only key logging | All pinned RFC 9850 labels and three line endings are implemented transactionally only in permanently unpublished `brynja-test-support`; workspace metadata and negative fixtures reject every production dependency, optional dependency, feature, target edge, publication flag, and resolved-graph path to the package |
| Normative requirements | 167 stable identifiers bind exact source, section, status, errata, authority role, strength, applicability, decision, mapping scope, owner, lifecycle, revision, assurance invariants, work bound, residual risk, target, positive and negative tests, and evidence gap; the generated closure covers all 127 currently locked authorities, 305 modern roadmap rows plus eight independent legacy stages, and 4,449 current surfaces, with the OpenPGP and newly expanded family-completeness rows fail-closed behind explicit authority admission, and includes 50 residual requirements, 33 residual authorities, 182 reviewed residual normative sections, 165 mapped sections, 17 explicit exclusions, and 765 formerly uncovered surfaces; immutable history and 110 requirement fixtures plus four surface-security fixtures reject omissions, undeclared evidence dispositions, orphaned cross-bundle delegations, semantic RFC 6066 laundering, TLS 1.2 authority omissions, wire/configuration conflation, non-reciprocal or boundary-mixed surface groups, wrong protocol ownership, actionable source-blocked legacy requirements, blanket section inheritance, drift, role errors, stale revisions, illegal transitions, unrelated mappings, weak bounds, missing anchors, rights gaps, stale mutable guidance, orphaned plans, repository-escaping targets, and premature claims |
| Authority refresh and rights | Every local NIST/ITU source is local-only with an explicit distribution review; all eight mutable IANA registries and five dependent NIST publication pages have exact refresh owners; the resolved hybrid-source record and blocked legacy-source-rights and FIPS-validation baselines remain machine-readable |
| Hybrid admission | Final RFC 10024 and final IANA assignments admit planning authority for exactly X25519MLKEM768, SecP256r1MLKEM768, and SecP384r1MLKEM1024 at v0.120.0; no implementation is present, and drafts and private values remain forbidden |
| Tags and releases | Every roadmap version requires a signed commit, complete automated tag gate, clean GitHub and CodeQL, explicit authorization, exact immutable signed annotated tag, release notes, and SBOM; scheduled or exceptional public checkpoints additionally require a backwards-looking cumulative PASS/PASS report with zero open findings and a strict publisher-context gate |
| GitHub protection | Active machine-checked main ruleset requires signed linear history, review and CodeQL while retaining explicit accountable owner/admin bypass |
| CI | Read-only permissions, full-SHA action pins, explicit installation of all three OS-less targets before the ordinary repository gate, live release-control verification, Clippy enforcement for both all-feature and no-default-feature configurations, stage-aware acceptance of development milestones without scheduled reports, and fail-closed PASS/PASS or remediation-stage RETEST REQUIRED/PENDING validation whenever a current report exists |
| CodeQL | GitHub Default setup; no advanced workflow |
| Panic posture | Panics are forbidden by workspace lint; release builds retain overflow checks and abort if an otherwise unreachable panic occurs, accepting process termination as the final fail-closed response rather than permitting recovery from a violated invariant |
