[release]
version = "0.13.2"
milestone = "0.13.2"
baseline = "0.10.0"
cumulative_milestones = ["0.11.0", "0.11.1", "0.11.2", "0.12.0", "0.13.0", "0.13.1", "0.13.2"]
policy = "independent"
stage = "internal"
exceptional = true
exception_reason = "CPU-boundary source-admission and policy-integrity findings triggered an exceptional assessment; exact signed remediation candidate 2fa60d05d8c4472426cdb979243f53e2e959c231 passed repository-owner retest."

[crates."brynja"]
previous_version = "0.13.1"
version = "0.13.2"
change = "code"
publish = false
reason = "Advance to the v0.13.2 CPU package-boundary milestone while keeping both reserved packages outside the facade and selecting no crate for crates.io publication."

[crates."brynja-core"]
previous_version = "0.7.0"
version = "0.7.0"
change = "code"
publish = false
reason = "Retain published 0.7.0 while carrying the cumulative owned-memory, constant-time, provider, and CPU-backend contract implementations to the v0.15.0 checkpoint."

[crates."brynja-crypto"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 while the shared package header clarifies brynja-crypto as the provider, policy, and composition layer above future leaf-family crates; functionality remains unimplemented."

[crates."brynja-crypto-cpu"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Reserve the zero-dependency no_std ISA-kernel package and eight exact backend module contracts without admitting an implementation."

[crates."brynja-crypto-cpu-std"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Reserve the directly selected future host detector adapter as an inert no_std placeholder depending only on brynja-crypto-cpu."

[crates."brynja-pki"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; PKI remains unimplemented."

[crates."brynja-tls"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the router remains unimplemented."

[crates."brynja-tls12"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the TLS 1.2 engine remains unimplemented."

[crates."brynja-tls13"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the TLS 1.3 engine remains unimplemented."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the handshake engine remains unimplemented."

[crates."brynja-quic-tls"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the QUIC-TLS adapter remains unimplemented."

[crates."brynja-sanitization"]
previous_version = "unpublished"
version = "0.1.0"
change = "code"
publish = false
reason = "Implement the separately selected exact-pinned sanitization adapter; hold its first publication for a future cumulative public checkpoint."

[crates."brynja-dtls"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while carrying the cumulative development-tag documentation delta; the DTLS engine remains unimplemented."

[crates."brynja-platform"]
previous_version = "0.1.6"
version = "0.1.6"
change = "metadata"
publish = false
reason = "Retain published 0.1.6 while the upstream core provider contracts become available for later downstream platform implementations; no platform effect is implemented."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only test support; it is never a crates.io product."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
