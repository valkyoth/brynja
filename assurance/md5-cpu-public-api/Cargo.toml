[package]
name = "brynja-md5-cpu-public-api-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
brynja-legacy-md5 = { path = "../../crates/brynja-legacy-md5", version = "=0.1.0", default-features = false, features = ["cpu", "cpu-evidence"] }

[lints.rust]
unsafe_code = "forbid"
[lints.clippy]
unwrap_used = "forbid"
expect_used = "forbid"
