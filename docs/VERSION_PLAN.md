# Brynja Version Plan

Status: reconciled planning sequence

This document defines the exclusive, ordered Brynja release line through
`1.0.0`. Each version is an independently reviewable implementation stop.
Split growing work; never merge unrelated scope to preserve numbering.

[RELEASE_PLAN.md](RELEASE_PLAN.md) is normative for Goal, Deliverables,
Verification, and Exit criteria. It repeats every exact title and scope; the
validator rejects numbering, ordering, title, or scope drift.

## Admission Rules For Every Version

The [operation-direction audit](ROADMAP_API_COMPLETENESS_AUDIT.md) records
the explicit standalone DER completion at v0.33.9 and the format, key-role and
prerequisite corrections. Every promised codec or construction closes each
standard-defined operation direction with independent known answers and public
consumer fixtures, not self-round-trips alone. Noninvertible functions do not
need imaginary reverse APIs. Executable consumers follow their implementation
and acceptance prerequisites; earlier structural formats remain explicitly
non-executable until their separately numbered integration gate.

Every milestone retains `no_std` core production packages and rejects every
unreviewed external crate in the core workspace or any facade, engine, crypto,
default, legacy, bare-metal or FIPS graph. The only planned encoding exception
is an exact-pinned, feature-minimal `base64-ng` family edge admitted at
v0.47.1 and reachable solely through bounded Base64, PEM, or OpenPGP armor
packages; it can never implement cryptography or enter `brynja-fips-module`.
Every graph retains bounded hostile-input and pre-authentication work,
mandatory owned-region secret destruction, adversarial tests, supported Rust
and target evidence, SBOM, the complete automated tag gate, clean GitHub and
CodeQL, and explicit user authorization before the immutable signed tag.
Scheduled or exceptional public checkpoints additionally require an up-to-date
committed cumulative PASS pentest report before tagging and publication.

The permanent golden rule is stronger than dependency isolation: every Brynja
cryptographic and FIPS service is implemented from first-party Rust source,
never a C or foreign/native cryptographic module or wrapper. Separately gated
first-party intrinsics or inline assembly may implement exact CPU symbols;
external assembly, objects, libraries, foreign ABIs and delegated software
cryptographic providers are forbidden. Future rustls and Tokio companion
adapters may own separately locked pure-Rust framework API dependencies, but
remain downstream and cannot weaken this rule or enter the core graphs.

Protocol and optional-module dependency direction is downstream from frozen
interfaces and validated provider ports. Early negotiation policy is distinct
from final engine routing. Optional features pass a composition gate before
public API freeze. FIPS module catastrophic failure is distinct from terminating
a connection that violates its approved-only profile.

Every arithmetic or cryptographic implementation milestone introduces its
applicable proof harness beside the code. Proof claims distinguish full-width
symbolic proofs, limb-count-parameterized proofs, reduced-width exhaustive
models, and production-width vector or differential evidence; residual gaps
remain explicit and reduced-width evidence never claims full-width equivalence.

Every row that says **implement** completes a real, documented public API for
the exact named capability. A milestone cannot close with only internal pieces,
a stub, a test-only path, or an implementation that requires a later
acceleration or integration row to perform its advertised operation. At least
one consumer-style end-to-end test must import only public API and perform the
real operation against an authoritative expected result; official vectors,
boundary and misuse cases, compiled documentation examples, stateful or
streaming paths, and downstream composition tests remain additionally required
where applicable. Passing private unit tests alone is not usable-API evidence.

Every algorithm and construction also has an explicit API-profile disposition.
The machine-readable profile register must cover every standardized operation
and safe reusable shape that the implementation promises: byte and applicable
bit-string input, one-shot and incremental state, fixed and extendable output,
verification, caller-buffer and permitted overlap modes, ordinary versus
secret-bearing ownership, portable and admitted backend selection, no_std and
separately isolated hosted adapters, provider and external-key use, and every
supported import, export, generation, reset, clone, snapshot, cancellation and
destruction transition. A profile may be rejected when it would be inherently
unsafe or nonstandard, but it cannot disappear merely because the primitive's
mathematical result is already implemented.

An ordinary non-erasing hash or XOF state is a public-data profile, even though
its mathematical function accepts arbitrary bytes. It cannot implement or
satisfy a secret-bearing, keyed, password, KDF, private-prehash or protected-
transcript contract. Every capability that can own secret-derived data must use
a distinct non-Copy, non-Clone, non-formatting and non-serializing hardened
owner that compiler-resistantly destroys every Brynja-owned key, scalar,
schedule, sponge or chaining state, partial buffer, temporary block, encoded
secret, intermediate, pending value and failure-path copy on success, error,
cancellation, replacement, rekey, failed construction, recoverable panic
unwinding and Drop. Cleanup is non-panicking and still attempts every
independent region after an adjacent cleanup failure. `mem::forget`, process
abort, forced termination, power loss, registers, caches, OS context, crash
images and unavoidable compiler copies remain measured residuals rather than
false erasure claims. The machine-readable secret-state register names
each owner, field and temporary, creation and destruction edge, sanitization
symbol, emitted-code evidence and residual gap. CI blocks a secret-bearing API
or consumer whose register entry or exact evidence is incomplete.

Hardened ownership capabilities and construction-owner traits are sealed:
downstream code cannot implement, forge, assert, or wrap an ordinary state into
them. Every secret-derived output is explicitly declassified as public,
transferred into a typed secret-owned destination, or rejected before output
mutation; any unavoidable partial secret output is cleared before failure.
Incremental XOF and KDF APIs cannot lose this classification through a raw
caller buffer.

Secret cleanup is part of the implementation milestone, never routine follow-
up work after exposing the capability. A dedicated patch row is reserved when
the shared hardened owner or its evidence is independently review-sized; when
a cleanup gap is discovered after an immutable tag, the existing completeness-
gap rule inserts and blocks on the next patch before any dependent use.

Mandatory internal cleanup is always available through Brynja's dependency-
free `no_std` destruction primitive and never depends on an optional feature.
The separately selected `brynja-sanitization` adapter over the exact admitted
`sanitization` release is used wherever its fixed-region ownership contract is
the correct boundary, with differential and emitted-code evidence against the
core primitive; it never creates a reverse dependency, silently enters the
main or FIPS graph, or turns cleanup off when absent. The secret-state register
records the exact core or adapter mechanism used by every owned region.

`1.0.0` is a completeness and production-readiness boundary, never a schedule
target. There is no limit on the number of pre-1.0 versions. Before a
construction, protocol, profile, facade, or named historical-protocol package
can close, every authenticated standardized capability attached to it must
have one complete first-party implementation: every defined operation and
direction, every admitted family member and parameter set, a public API where
direct use is claimed, authoritative vectors, misuse and boundary evidence,
and an explicit modern or legacy security disposition. This closure is
transitive: an admitted HMAC-MD5 profile, for example, requires the complete
isolated MD5 implementation before the HMAC profile can close.

Age, deprecation, weakness, or lack of a secure default is never sufficient to
classify a real standardized capability as merely unsupported. Current
capabilities live in modern packages. Obsolete or dangerous capabilities live
in conspicuously warned, separately selected `brynja-legacy-*` packages and may
require an explicit dangerous-operation policy token, but remain complete
enough to send, receive, generate, parse, sign, verify, encrypt, decrypt, import,
and export wherever their governing specification defines those operations.
Modern packages never negotiate or fall back to them implicitly.

There is exactly one first-party implementation of an algorithm. If a modern
algorithm or individual family member later becomes legacy, the legacy facade
wraps or reexports that exact implementation with stricter policy and warnings;
it never copies the cryptographic code or silently changes its vectors. A
generic construction does not silently admit unassigned, reserved, private-use,
or unauthenticated values, but every named instantiation, registered and
authenticated protocol capability, mandatory or optional standardized
algorithm, advertised family member, or dependency used by a pre-1.0 modern or
legacy consumer belongs before `1.0.0`.

Intentional rejection remains valid only for malformed or noncanonical input,
standard-forbidden combinations, reserved or unassigned values, unknown
private-use values without caller authority, unsafe implicit downgrade or
fallback, intrinsically non-production diagnostics such as secret key logging,
or a source that cannot yet be authenticated or lawfully implemented. All
previously listed standalone hashes and legacy work now belong before 1.0,
including complete public APIs and transitive dependencies. Missing sources
remain blockers, not guessed implementations or automatic exclusions. Only
unavailable RISC-V native/community qualification keeps its post-1.0 campaign.

Any completeness gap found before tagging expands and blocks the current row.
If an immutable tag already exists, insert the next available patch-numbered
row and complete it before dependent or adjacent work continues. Reconcile both
plans, requirements, evidence, tests, and release notes around that patch; do
not rewrite the older tag or defer essential behavior silently to a distant
milestone.

A planned row is also split before implementation when it combines independent
algorithm identities, unrelated primitive families, portable implementation
and architecture acceleration, or implementation and its downstream usability
gate. Shared arithmetic or permutation code does not make several public
algorithms one review unit. Each resulting patch row must close one precisely
named usable capability, and the family receives its own packaged public API
acceptance before a dependent construction or protocol consumes it. Registry-
driven umbrella rows are planning gates only: they must generate and insert all
required review-sized tagged child rows before any listed implementation begins.

Every multi-version implementation chain reserves two ordered acceptance
boundaries before that chain starts. First, a **Portable Public API Usability
Acceptance** boundary must pass before any acceleration implementation,
admission, native measurement, or final evidence sweep begins. It freezes a
runnable downstream fixture and documented command using only ordinary public
packages, features, and symbols; validates every completed family member on
representative real data against authoritative or independent expected
results; and checks package contents and portability without private hooks.
This boundary may be the final deliverable of the last portable implementation
row or its own review-sized patch, but it must be committed and green first.

Second, after acceleration and its evidence are complete, a final
**Cross-Backend Public API Usability Acceptance** boundary reruns the frozen
fixture without weakening it against the portable path and every admitted
backend, verifies unavailable candidate and required-backend failures, and
closes the family. A failure that changes code invalidates and reruns every
affected proof, native result, emitted-code artifact, performance result,
side-channel result, KAT, package archive, and acceptance result. Evidence is
bound to the exact implementation commit; earlier evidence cannot be carried
across a code change merely because the public output later appears correct.
Exploratory benchmarks may guide design before portable acceptance, but cannot
support admission or a release claim.

The public verification-status table marks a started family **In progress**
until the final cross-backend boundary passes. Only that closing milestone may
change it to **Fully implemented**. Independent verification and FIPS
validation remain separate claims. When no acceleration is planned, an
explicitly scalar-only closing row may satisfy both boundaries after recording
that disposition. This executable evidence does not replace formal proof,
independent review, or certification, and it cannot become the first point at
which the implementation is actually usable. If a chain crosses a crates.io
checkpoint, the checkpoint itself must already pass the applicable consumer
acceptance even when a separately tagged closing patch follows later.

Every CPU-accelerated implementation remains an optional first-party backend
behind the same `no_std` provider contract as its portable scalar reference.
An acceleration row cannot start until its release notes and evidence index
identify the preceding tagged portable-acceptance boundary and frozen fixture.
Candidate detection is separate from activation; safe code can activate only an
exact compiler-proven or runtime-proven feature bundle that passed its own
known-answer test. Unsupported, unmeasured, unhealthy, or quarantined paths
remain scalar and visible in secret-free reporting. `std` runtime detection is
confined to a separate opt-in adapter crate, while bare-metal and OS-less builds
use compile-time evidence or an explicitly reviewed platform capability token.
QEMU is supplemental coverage, never native performance or side-channel
evidence. Every accelerated symbol, dispatch path, compiler, CPU family, and
operational environment remains independently evidenced and FIPS-scoped.

Every signed tag advances the `brynja` facade manifest to the tag version.
Only scheduled or exceptional public checkpoints publish the facade and its
selected dependency closure to crates.io. Supporting crates retain independent
SemVer lines and publish only when their cumulative package or exact internal
dependency requirements change. Repository-only crates never publish, changed
dependencies publish first, and the facade publishes last.
`release-crates.toml` and `scripts/release/release_crates.py` enforce this policy.

## TLS Package And Retirement Rule

`brynja-tls` remains the evergreen public facade and one-pass router.
`brynja-tls12`, `brynja-tls13`, and each later admitted TLS generation own
separate version-specific engines; record-independent TLS 1.3 state is isolated
in `brynja-tls13-handshake` for stream TLS and QUIC. Adding a TLS generation
requires a new package, requirements closure, implementation sequence, engine
audit, and router integration and audit milestones.

A successor does not automatically make an older TLS generation legacy.
Retirement requires a newly added numbered security-boundary milestone backed
by current standards and cryptographic evidence. It removes the engine from all
modern graphs and negotiation before any controlled-interoperability package is
created. Any continuation starts a separate
`brynja-legacy-tls1N` SemVer, warning, audit, and pentest line; the former
modern package is explicitly deprecated and never forwards to legacy code.

## Phase 0: Repository, Effects, Memory, And Wire Foundations

Generated standards scope and upstream dependency direction precede implementation.

Beginning after the signed `v0.10.0` tag, every row advances the `brynja`
manifest and receives its ordinary signed `vX.Y.Z` tag after the complete
automated gate and green GitHub and CodeQL. Scheduled public pentest and
crates.io checkpoints occur every fifth minor version (`v0.15.0`, `v0.20.0`,
and so on); patch rows remain tagged development milestones inside their
existing range. Each checkpoint pentests backwards over all changes after the
previous public tag through the new candidate: v0.10.0 to v0.15.0, then
v0.15.0 to v0.20.0, and so forth. Exceptional pentest and early-release
triggers, `v1.0.0-rc.1`, and `v1.0.0` are defined by
[RELEASE_PLAN.md](RELEASE_PLAN.md). Supporting crates retain independent SemVer.

| Version | Milestone | Exclusive scope and completion context |
| --- | --- | --- |
| `0.1.0` | Workspace Foundation | Preserve the explicit `brynja-legacy-*` naming boundary, evergreen `brynja-tls` router facade, version-specific `brynja-tls12`, `brynja-tls13`, and `brynja-tls13-handshake` package graph, and the remaining workspace foundation with no cryptographic or protocol security claim. |
| `0.2.0` | Release And Isolation Enforcement | Harden committed-report and exact-tag comparison, validate all-feature graphs and every package class, add negative modern and legacy isolation fixtures, and document protected release controls. |
| `0.3.0` | Requirements And Standards Source Ledger | Generate the normative source ledger from every algorithm, encoding, extension, protocol, validation, and operational milestone; close current RFC updated-by and obsoleted-by chains, record errata decisions and IANA snapshots, distinguish current authorities from compatibility baselines, and require the final ECDHE-ML-KEM group RFC and code points before admission. |
| `0.3.1` | Machine-Readable Protocol Surface Decision Register | Generate a machine-readable register covering every current TLS, DTLS, QUIC-TLS, PKIX, HPKE, ECH, legacy-protocol, algorithm, extension, content and handshake message, alert, cipher suite, signature scheme, named group, certificate and key format, and relevant IANA entry; classify each as implemented, intentionally rejected, safely ignored, caller-owned, legacy-only, or future work with normative source, owning milestone, code and test targets, including explicit decisions for Heartbeat, status_request_v2, SSLKEYLOGFILE, TLS 1.3 post-handshake authentication, certificate-with-external-PSK, legacy PKCS1 client signatures, ML-KEM PKIX credentials, HPKE non-base modes, unsigned X.509 certificates, QUIC version-specific transport cryptography, and compression algorithms; fail when a source, registry snapshot, status, erratum, or classification drifts. |
| `0.3.2` | Normative Requirement Matrix Foundation | Define stable requirement identifiers bound to exact source hashes, sections, status and errata; model planned, implemented, tested, evidenced, rejected, caller-owned, legacy and blocked lifecycles; generate bidirectional source, decision, milestone, target-symbol-or-boundary, test and evidence mappings; and prove extraction and drift failures on the normative-language and registry authorities. |
| `0.3.3` | Cryptography Encoding And PKIX Normative Coverage | Populate and review every applicable normative statement and invariant for admitted primitives, arithmetic, DER, key and certificate formats, service identity, path processing, revocation, OCSP and Certificate Transparency; record explicit algorithm exclusions, current-versus-compatibility authority, positive and negative target tests, work bounds, and unresolved evidence. |
| `0.3.4` | TLS DTLS And QUIC Normative Coverage | Populate and review every applicable normative statement and invariant for current and compatibility TLS, hardened TLS 1.2, QUIC-TLS, DTLS 1.2 and DTLS 1.3; map every message, extension, alert, registry value, state transition, transport boundary, intentional rejection and caller-owned responsibility to its milestone and target tests. |
| `0.3.5` | Optional Legacy And Residual Normative Closure | Populate HPKE, ECH, ML-KEM and hybrid, optional TLS facilities, legacy protocol, operational and presently pinned non-RFC requirements; represent unavailable future or mutable authorities as fail-closed blockers owned by their dependent milestone; reconcile exact cross-bundle section ownership, confine every RFC 9853 RRC surface to DTLS, separate RFC 6066 wire-ignore behavior from configuration rejection, cover RFC 6066 independently in TLS 1.2, and reject every orphan, duplicate, stale, obsolete-as-current, silently weakened or uncovered planned surface before cryptographic or protocol implementation begins. |
| `0.4.0` | Assurance Harness And Bare-Metal Matrix | Establish first-party mutation and differential harnesses, true bare-metal targets, and pinned external assurance-tool policy without adding third-party crates to repository Cargo manifests. |
| `0.5.0` | Error Alert And Exhaustion Domains | Freeze non-secret error, alert, close, provider-failure, and resource-exhaustion domains; prohibit secret-bearing formatting and ambiguous failure collapse. |
| `0.6.0` | Bounded Numeric And Resource Domains | Add private-field compile-time bounded `u64`/`usize` values with checked arithmetic, distinct count and byte-length types with fail-closed pointer-width conversion, non-wrapping protocol-neutral sequence and epoch values, and explicit immutable resource/work budgets whose typed exhaustion errors reveal no limit values; retain parsing, mutable accounting, wire widths, direction-specific state, and protocol behavior for later owners. |
| `0.7.0` | Borrowed Read Cursor | Implement a borrowed read cursor with exact consumption, truncation-at-every-byte coverage, and no indexing panics. |
| `0.8.0` | Transactional Write Cursor | Implement caller-buffer write cursors with transactional encode-or-no-mutation behavior. |
| `0.9.0` | Caller-Owned Workspace And Arena Model | Define caller-owned workspaces and non-overlapping secret, plaintext, transcript, certificate, and output arenas with overlap rules, high-water tracking, and allocation counters. |
| `0.10.0` | Secret Lifetime And Destruction Contract | Define non-cloneable and non-serializable secret ownership, transition, error, cancellation, provider-failure and drop destruction, immediate obsolete-secret cleanup, external-store and accelerator duties, a mandatory production guarantee for the complete owned memory region, and RFC 9850 key logging only in a separately compiled test-support artifact that cannot enter production packages or features. |
| `0.11.0` | Owned-Memory Zeroization Primitive | After explicit unsafe-policy approval, implement the smallest isolated first-party primitive needed to preserve zeroization stores through optimization; define proof obligations, cache and DMA completion duties, MIR, LLVM and assembly evidence for every supported compiler and target, and precise exclusions for registers, copies, dumps, and physical memory. |
| `0.11.1` | Sanitization Adapter Admission Review | Audit the latest stable first-party `sanitization` crate against Brynja's MSRV, `no_std`, license, unsafe, target, complete-owned-region destruction, feature, dependency, advisory, optimization-evidence, and FIPS-boundary policies; compare one protocol-neutral adapter with a legacy-specific split, require an activated graph with no `zeroize` or other third-party crate, and record a fail-closed admit-or-reject decision without changing any Brynja production dependency graph. |
| `0.11.2` | Optional Brynja Sanitization Adapter | Conditional on the v0.11.1 admission decision, implement and prepare for separate publication at the next scheduled or exceptional release checkpoint a `no_std` `brynja-sanitization` downstream adapter using exact-pinned `sanitization` with default features disabled, adapter-owned wrapper types, and identical modern and legacy destruction semantics; keep it out of every facade, engine, default feature, and FIPS validated-module closure, or close the milestone with documented non-admission if any invariant cannot be preserved. |
| `0.12.0` | Constant-Time Foundation | Implement constant-time equality, choice and mask types, conditional select and swap, fixed-width secret operations, compiler barriers, and rules forbidding secret-dependent control flow, indexing, loop counts, and error timing. |
| `0.13.0` | Provider Capabilities And Opaque Handles | Define all protocol-facing crypto, signature, KEM, AEAD, entropy, clock, path, storage, and pending-operation contracts in upstream no_std interface modules such as brynja-core, with opaque handles, frozen capabilities, transactional installation, exact-operation token binding, and no implicit fallback; brynja-platform only implements downstream contracts. |
| `0.13.1` | CPU Backend Capability And Dispatch Contract | Before any cryptographic primitive exists, freeze first-party scalar, opportunistic-accelerated, required-accelerated, and validated-module backend policies; separate candidate detection from admitted activation; define exact feature-bundle evidence, non-forgeable thread-bound capability tokens, backend identity, per-operation dispatch, startup known-answer tests, health generations, quarantine, fail-closed required mode, and secret-free reporting so safe code cannot execute an unsupported instruction or silently change service approval. |
| `0.13.2` | CPU Acceleration Package And Unsafe Boundary | Freeze future `brynja-crypto-cpu` as an optional zero-dependency `no_std` package for isolated ISA kernels and static selection, and future `brynja-crypto-cpu-std` as a separate opt-in `std` runtime-detection adapter; keep scalar `brynja-crypto`, every protocol engine, default feature, bare-metal graph, and ordinary facade independent of both, keep the std adapter outside `brynja-fips-module`, and require a separately hashed, versioned, under-500-line unsafe-intrinsic or assembly boundary for every admitted backend without authorizing any implementation yet. |
| `0.13.3` | Native CPU Evidence And Performance Admission Harness | Establish reproducible backend evidence manifests, forced-backend and unsupported-feature processes, KAT and quarantine fault injection, scalar differential corpora, emitted-code and side-channel capture, code-size and latency budgets, and benchmark admission thresholds across local AMD x86_64, an observed-feature AWS Intel x86_64 instance, Apple M2, AWS AArch64, and the available RISC-V cloud host; record CPU, microcode, OS, compiler, flags, frequency policy, and feature evidence, treat emulation as supplemental only, and leave an unavailable or unmeasured backend unadmitted rather than blocking portable scalar support. |
| `0.14.0` | Entropy And Secure-Random Contracts | Separate affine caller-provided raw entropy from non-cloneable initialized secure randomness; bind exact purpose, strength capacity, bytes, runtime generation, fork and bounded reseed rules, transactional caller-owned output, retryable versus permanently quarantined failure, synchronous destruction, and a deterministic/fault provider confined to unpublished test support; add no algorithm, OS RNG, FFI, source-quality, independent-verification, or FIPS claim. |
| `0.15.0` | Wall And Monotonic Clock Contracts | Define non-interchangeable typed wall time for PKI and typed monotonic time for timers, freshness, tickets, and replay policy with checked arithmetic and explicit unavailable-time behavior. |
| `0.16.0` | Pending Operations And Accelerator Lifecycle | Define resumable provider tokens, certificate, signature and accelerator requests, cancellation, retry semantics, backpressure, and failure-atomic state transitions; external-key and accelerator-handle destruction completes only through a mandatory single-consumption token transition, never through an informational event. |
| `0.17.0` | FIPS-Aware Provider Architecture | Freeze broad operation classification with every current capability explicitly non-approved and every nonempty approved set rejected until exact service identities span execution; add self-test and permanent-failure hooks, exact scalar and CPU-backend dispatch ownership, non-authorizing service indicators, provider-derived SSP destruction duties, deterministic module-build expectations, CPU-feature and operational-environment assumptions, and sealed-provider exclusions; ordinary opportunistic or std-adapter selection can never enter or alter the future module, and no validation claim is made. |
| `0.18.0` | Mandatory Security Outcome Authority Contract | Define authoritative engine state and exhaustive mandatory typed results for self-tests, service approval, protocol and profile selection, authentication, tickets, resumption, PSKs, early data, anti-replay, amplification, exhaustion, provider failure, key lifecycle, ECH, policy, and terminal transitions; public inputs cannot forge accepted or approved authority without sealed exact-subject evidence, each validated disposition has an opaque non-interchangeable result with private reasons and an exact authority-retained commit match, self-test failure is permanently terminal, every resolved non-terminal result requires an affine commit whose abandonment fails closed, pending abandonment fails closed, external-key destruction completes only through an exact mandatory token transition, and ignoring every informational output cannot make rejected, non-approved, incomplete, or failed work appear accepted, approved, complete, or successful. |
| `0.18.1` | Bounded Observational Security Event Schema | Define an upstream no_std Sans-I/O SecurityEvent audit schema that only duplicates the authoritative outcomes frozen at v0.18.0; events are caller-drained, allocation-free, bounded, secret-free, format-safe, alert-independent, optionally caller-timestamped or explicitly untimestamped for later enrichment, use saturating drop counters with visible saturation, contain no secret or stable correlating identifier, never reenter, and cannot block, authorize, complete, or alter cryptographic or protocol state. |
| `0.19.0` | TLS And DTLS Record Framing | Keep record framing independent of protocol selection and fallback; ignore TLSPlaintext legacy_record_version where required, validate TLSCiphertext constants where applicable, preserve bytes, reject RFC 6520 Heartbeat content and negotiation in every modern profile, and leave version choice exclusively to typed handshake policy. |
| `0.20.0` | Bounded DER Reader | Implement a non-recursive DER tag, length and value reader with definite, minimal, overflow-safe, depth-, node-, size-, and work-bounded parsing. |
| `0.21.0` | Canonical ASN.1 Primitives | Add canonical ASN.1 integer, bit and octet string, OID, Boolean, string, sequence and set, and time primitives with malformed and non-canonical corpora. |

## Phase 1: First-Party Cryptography, Identity Formats, And PKI

Reusable cryptography, complete shared codecs and key formats pass independent
crypto and PKI gates. Early imported-key RSA signing is extended by the later
explicit key-generation and full RSA-family milestones.

| Version | Milestone | Exclusive scope and completion context |
| --- | --- | --- |
| `0.22.0` | SHA-256 | Freeze the reusable no_std `brynja-hash-core` interface and `brynja-hash-sha2` family boundary, then implement streaming and fixed-message SHA-256 with official vectors, boundary lengths, and exhaustion handling; make `brynja-crypto`, TLS, PKI, and later FIPS consumers use that exact implementation without exposing the later separately selected catalogue facade or admitting unrelated hash families. |
| `0.22.1` | SHA-256 x86_64 And AArch64 Acceleration | Add separately forced and reported SHA-256 backends using exact x86_64 SHA-extension bundles on AMD and Intel and exact AArch64 SHA2 bundles on Apple M2 and AWS Arm; preserve the scalar state and digest API, streaming and fixed-message equivalence, checked length and exhaustion behavior, safe std and static no_std dispatch, startup KAT quarantine, and per-compiler constant-time and emitted-code evidence without claiming register erasure. |
| `0.22.2` | SHA-256 RISC-V Acceleration Candidate | Implement a first-party RISC-V SHA-256 backend only for an exact ratified scalar-crypto or vector-crypto feature bundle expressible across the supported Rust line; run it on the available RISC-V host when its observed ISA qualifies, otherwise retain it as a non-dispatchable candidate with emulator and generated-code evidence, keep scalar fallback authoritative, and prohibit an accelerated support claim until matching native correctness, performance, and side-channel evidence exists. |
| `0.22.3` | SHA-256 Public API Usability Acceptance | Close the SHA-256 implementation chain with a runnable downstream-style fixture that uses only the documented public `brynja-hash-sha2` and `brynja-crypto` APIs to hash representative real byte content through one-shot, irregular streaming, scalar, and every admitted accelerated route; verify authoritative digests, package installability, no_std portability, honest backend reporting, and deterministic misuse and exhaustion behavior without private hooks, test-only features, or adding algorithm scope. |
| `0.23.0` | Complete Portable SHA-224 | Complete portable SHA-224 in `brynja-hash-sha2` beside SHA-256, reusing the reviewed 32-bit compression owner while preserving SHA-224's distinct FIPS 180-4 initial value, 224-bit output, checked length domain, streaming state, one-shot function, digest type, and public identity. |
| `0.23.1` | Complete Portable SHA-384 And SHA-512 | Implement the complete portable SHA-384 and SHA-512 algorithms in `brynja-hash-sha2` over one private reviewed 64-bit compression owner, with distinct FIPS 180-4 IVs, outputs, 128-byte buffering, 128-bit length encoding, checked byte domains, streaming states, one-shot functions, digest types, and public identities. |
| `0.23.2` | Complete SHA-512/224 And SHA-512/256 | Complete SHA-512/224 and SHA-512/256 in `brynja-hash-sha2` as distinct named FIPS 180-4 algorithms over the reviewed 64-bit foundation, implement and verify the SHA-512/t IV-generation procedure for exactly the approved 224- and 256-bit identities, and reject the false model that either algorithm is ordinary SHA-512 truncation. |
| `0.23.3` | Complete SHA-2 CPU Acceleration | Extend every admitted SHA-256-family and SHA-512-family backend to the complete six-algorithm SHA-2 surface on x86_64, AArch64, and qualifying RISC-V; reuse compression kernels without merging algorithm identities, and require per-variant KAT, chunking, exhaustion, forced-path, quarantine, native performance, emitted-code, and scalar-equivalence evidence. |
| `0.23.4` | Complete SHA-2 Public API Usability Acceptance | Close the SHA-2 chain with a packaged downstream fixture that uses only public APIs to hash representative real content through SHA-224, SHA-256, SHA-384, SHA-512, SHA-512/224, and SHA-512/256 in one-shot and irregular streaming modes, forces every admitted backend, and detects wrong IVs, wrong truncation, packaging gaps, or incomplete family documentation. |
| `0.24.0` | Keccak Foundation And Complete SHA3-224/SHA3-256 | Freeze reusable no_std `brynja-hash-sha3` sponge ownership around one private Keccak-f[1600] permutation, then complete SHA3-224 and SHA3-256 with distinct rates, suffixes, fixed-output states, one-shot and streaming APIs, authoritative vectors, padding boundaries, checked lengths, proofs, and no raw public permutation. |
| `0.24.1` | Complete SHA3-384 And SHA3-512 | Complete SHA3-384 and SHA3-512 over the reviewed sponge owner with distinct rates and outputs, one-shot and streaming APIs, authoritative vectors, padding and length boundaries, domain-separation negatives, proofs, and direct public usability without depending on SHAKE or acceleration. |
| `0.24.2` | Complete SHAKE128 And SHAKE256 | Complete SHAKE128 and SHAKE256 as distinct FIPS 202 XOFs with absorb, finalization, incremental multi-squeeze, zero-length and arbitrary caller-bounded output, exact domain separation, checked state transitions, authoritative vectors, proofs, and public APIs without exposing Keccak-f[1600]. |
| `0.24.3` | SHA-3 And SHAKE Portable Public API Usability Acceptance | Before acceleration or native evidence, freeze and pass a packaged downstream fixture covering all four SHA-3 digests and both SHAKE XOFs through public one-shot, streaming, incremental multi-squeeze, and forced portable paths, including representative real data, zero-length and multi-block output, authoritative vectors, no_std installation, package contents, checked failures, and domain-separation negative tests; keep the family status In progress. |
| `0.24.4` | SHA-3 And SHAKE Byte-Oriented Acceleration Evidence | Only after v0.24.3 is committed and green, add architecture-specific Keccak-f[1600] candidates for the six existing byte-oriented SHA-3/SHAKE APIs on x86_64, AArch64, and qualifying RISC-V, admit only paths justified by exact native, emitted-code, performance, side-channel, KAT, quarantine, and scalar-equivalence evidence, rerun the unchanged v0.24.3 fixture through portable and every admitted backend, and keep the family In progress until later bit-string and hardened-state profiles pass final acceptance. |
| `0.24.5` | Cross-Authority Standards Lifecycle Monitor | Extend the standards ledger with bounded allowlisted monitoring for every official publication landing page, immutable document, RFC status and errata feed, IANA registry, and admitted architecture specification; record upstream edition, revision, draft, update-planned, superseded and withdrawn state separately from Brynja's current, compatibility, legacy-only, disabled and rejected dispositions; run scheduled and manual drift checks that require human impact review and exact requirement, code, evidence and release decisions without automatically downloading, reclassifying, weakening, or moving any implementation. |
| `0.24.6` | Cryptographic API Profile And Secret-State Closure Register | Implement a machine-readable, CI-enforced register that assigns every current and planned primitive, construction and protocol consumer its complete safe public API profiles and every secret-owning type, field, temporary, lifecycle edge, recoverable-unwind path, sanitization symbol, output classification, evidence artifact and residual gap; distinguish ordinary public-data state from sealed downstream-unforgeable hardened state, require non-panicking all-region cleanup despite adjacent failures, classify public declassification versus typed secret output and partial-output clearing, record `mem::forget`/abort/termination/power-loss residuals, reject unsafe or nonstandard conveniences explicitly, and block implementation exit, composition and Fully implemented status when any promised operation, ownership mode or cleanup duty lacks an owner. |
| `0.24.7` | Complete SHA-2 Arbitrary-Bit Input APIs | Extend all six SHA-2 identities with canonical FIPS 180-4 arbitrary-bit-message one-shot and incremental APIs, including unambiguous final partial-byte representation, exact bit-length exhaustion, byte-aligned equivalence, padding at every tail position, public consumer examples, vectors and proofs, while preserving the existing byte APIs and every scalar or accelerated digest identity. |
| `0.24.8` | Complete Hardened SHA-2 State APIs | Add distinct public secret-bearing states for all six SHA-2 identities that share the exact reviewed compression algorithms but own and compiler-resistantly destroy partial input, chaining state, message schedules, block copies and all Brynja-owned intermediates on every terminal and recoverable-unwind path; seal hardened capabilities against downstream implementation or forgery, make cleanup non-panicking and all-region despite adjacent failures, classify digest output as explicit public declassification or typed secret ownership with unchanged or cleared failure destinations, prohibit Copy, Clone, formatting, serialization, reset into ordinary state and use of any accelerated path without matching cleanup and spill evidence. |
| `0.24.9` | Complete SHA-3 And SHAKE Arbitrary-Bit APIs | Extend all four SHA-3 digests and both SHAKE XOFs with a distinct low-bit-first canonical FIPS 202 message type for one-shot and incremental final-tail absorption, plus typed arbitrary-bit SHAKE output and consuming final-bit squeeze APIs; cover exact delimited-suffix composition, partial-byte validation, checked length exhaustion, byte-aligned equivalence, official NIST bit vectors, an independent oracle and every rate/tail boundary without exposing raw Keccak-f[1600]. |
| `0.24.10` | Complete Hardened SHA-3 And SHAKE State APIs | Add distinct public secret-bearing SHA-3 and SHAKE absorb and reader states over the exact private permutation, destroying sponge lanes, partial buffers, suffix staging, squeeze state, output staging and every Brynja-owned temporary on finalization, exhaustion, error, cancellation, recoverable unwinding and Drop through non-panicking all-region cleanup; seal hardened capabilities against downstream implementation or forgery, classify fixed and incremental output as explicit public declassification or typed secret ownership with unchanged or cleared failure destinations, keep hardened dispatch portable unless each accelerated symbol has exact cleanup and spill evidence, and make the owner reusable by later cSHAKE, KMAC, HMAC and protected-prehash constructions without exposing raw state. |
| `0.24.11` | SHA-2 And SHA-3/SHAKE Cross-Backend Final Acceptance | Freeze package-external fixtures for every SHA-2, SHA-3 and SHAKE byte, arbitrary-bit, ordinary and hardened public profile; run the exact final code through portable, every cleanup-qualified admitted backend, and every required-backend unavailable or quarantine path; compare identical mathematical outputs, exercise one-shot, incremental, multi-squeeze, error, cancellation, recoverable-unwind, adjacent-cleanup-failure and Drop paths, verify explicit public declassification versus typed secret output, refresh every affected native, timing, emitted-code, proof, sanitization, package and differential artifact after any code change, and only then change the root, facade, leaf, current-status and claim-register family entries from In progress to Fully implemented while independent verification and FIPS validation remain separate. |
| `0.24.12` | SP 800-185 Encoding Foundation And Complete cSHAKE | Bind the current final SP 800-185 authority and its announced revision state, implement exact left_encode, right_encode, encode_string and bytepad with checked bit-length arithmetic and canonical arbitrary-bit-string inputs, then complete cSHAKE128 and cSHAKE256 with N and S customization, empty-N-and-S SHAKE equivalence, fixed and XOF output, ordinary and hardened secret-bearing one-shot, streaming and incremental squeeze APIs over the v0.24.10 owner, exact domain separation, complete internal sanitization, official examples, proofs and no raw permutation exposure. |
| `0.24.13` | Complete KMAC And KMACXOF | Implement KMAC128, KMAC256, KMACXOF128 and KMACXOF256 once in `brynja-mac-kmac` over the exact cSHAKE owner, covering the complete standards-valid key, message, customization and output domains through clearly separated conformance and strength-enforcing constructors, secret-owned sponge and encoding state, hardened cleanup, affine finalization, constant-time tag verification, official examples, misuse tests, proofs, and non-approved indicators without confusing a MAC, PRF, XOF, or unkeyed digest. |
| `0.24.14` | Complete TupleHash And TupleHashXOF | Implement TupleHash128, TupleHash256, TupleHashXOF128 and TupleHashXOF256 with ordered unambiguous arbitrary-bit-string tuple items, ordinary and hardened secret-bearing whole-item and exact-length streaming item APIs, customization, fixed and incremental XOF output, checked tuple and output bounds, transactional failures, complete internal sanitization, official examples, partition and collision-separation negatives, proofs and public types that cannot silently flatten a tuple into one byte string. |
| `0.24.15` | Complete ParallelHash And ParallelHashXOF | Implement ParallelHash128, ParallelHash256, ParallelHashXOF128 and ParallelHashXOF256 with every standards-defined B, n, customization and output encoding rule, ordinary and hardened secret-bearing deterministic bounded sequential no_std paths, caller-scheduled ordered leaf jobs with explicit secret ownership for real parallel no_std use, and a separate optional zero-third-party-dependency std executor with explicit worker and maximum-leaf budgets, one fail-closed operation at a time per executor, reusable worker-sized clearing storage and recoverable scoped thread creation; cover complete worker and temporary sanitization, partial and empty leaves, failure and cancellation, ordering, resource exhaustion, fixed and XOF output, official examples, scalar/parallel equivalence, proofs and no hidden allocation in the core crate. |
| `0.24.16` | SP 800-185 Portable Public API Usability Acceptance | Freeze and pass package-external fixtures for all fourteen named cSHAKE, KMAC/KMACXOF, TupleHash/TupleHashXOF and ParallelHash/ParallelHashXOF instances through ordinary and hardened secret-bearing one-shot, streaming, incremental squeeze, forced portable, arbitrary-bit-tail and representative real-data paths; verify N, S, K, tuple boundaries, B, L, zero-length cases, exact official examples, no_std package use, complete internal cleanup and misuse failures while keeping the derived family In progress. |
| `0.24.17` | SP 800-185 Cross-Backend And Parallel Final Acceptance | Rerun the unchanged v0.24.16 ordinary and hardened fixture through portable and every cleanup-qualified admitted Keccak backend, exercise sequential, caller-scheduled and optional std ParallelHash execution with deterministic outputs, bounded failure and complete worker cleanup, collect cross-architecture performance, timing, emitted-code, KAT, quarantine and sanitization evidence, and mark SP 800-185 Fully implemented only when every named instance, ownership profile and backend has an explicit passing or unadmitted disposition on the final code. |
| `0.24.18` | Complete First-Party Legacy SHA-1 | Implement complete byte and canonical arbitrary-bit streaming and one-shot SHA-1 once in isolated `brynja-legacy-sha1`, with every FIPS 180-4 operation, ordinary and hardened secret-bearing states, official vectors, checked bit exhaustion, complete internal sanitization, public consumer APIs and conspicuous collision warnings; add no modern facade, default, TLS, PKIX, FIPS or general-hash edge, and require later HMAC, HKDF and OpenPGP consumers to use separately typed admission without reimplementation. |
| `0.24.19` | Complete First-Party Legacy MD5 | Implement the complete RFC 1321 MD5 algorithm once in isolated `brynja-legacy-md5`, including byte and canonical arbitrary-bit one-shot and streaming input, padding, little-endian bit length, ordinary and hardened secret-bearing states, complete internal sanitization, official and independent vectors, checked exhaustion and public compatibility APIs with conspicuous collision and chosen-prefix warnings; admit no signature, certificate, password, modern protocol, default, facade or FIPS use. |
| `0.24.20` | Legacy SHA-1 And MD5 Portable Public API Acceptance | Freeze package-external no_std fixtures and package archives for every SHA-1 and MD5 byte, bit-string, ordinary and hardened public compatibility profile; run representative real files, authoritative and independent digests, streaming partitions, exhaustion, cleanup and isolation failures through forced portable paths, prove collision warnings and modern-versus-legacy dependency, feature, type and symbol separation, and keep both legacy families In progress until acceleration and final cross-backend evidence pass. |
| `0.24.21` | SHA-1 CPU Acceleration | After the frozen v0.24.20 portable boundary is green, implement isolated first-party SHA-1 compression candidates for exact x86/x86_64 SHA-extension and AArch64 SHA1 feature bundles, with no_std static selection and opt-in std runtime detection; require forced-path KATs, scalar differentials, migration-safe feature revalidation, quarantine, emitted-code, AMD/Intel and Apple/AWS Arm native correctness, performance and timing evidence, cleanup-qualified hardened dispatch, and an explicit scalar-only disposition for RISC-V or any target without a ratified toolchain-expressible SHA-1 facility. |
| `0.24.22` | MD5 Multi-Buffer SIMD Acceleration | After v0.24.20, implement first-party allocation-free MD5 multi-buffer candidates that process independent messages through exact x86_64 AVX2 and, when separately justified, AVX-512 bundles plus AArch64 NEON and qualifying ratified RISC-V Vector bundles; expose bounded caller-owned batch lanes without pretending single-stream MD5 has dedicated hardware acceleration, preserve scalar semantics for uneven tails and unsupported targets, and require lane-permutation, mixed-length, inactive-lane, KAT, differential, quarantine, native performance, timing, emitted-code, failure-atomicity and hardened lane/register/spill cleanup evidence before admission. |
| `0.24.23` | Legacy SHA-1 And MD5 Cross-Backend Final Acceptance | Rerun the byte-identical v0.24.20 package-external fixtures on the exact final source through portable, every admitted SHA-1 backend, every admitted MD5 batch width, scalar tails, unavailable-required and quarantined routes; collect current AMD/Intel, Apple/AWS Arm and qualifying RISC-V or explicit unsupported evidence, repeat every affected proof, sanitizer, cleanup, timing, KAT, package and differential artifact after any code change, and only then mark the isolated legacy SHA-1 and MD5 families Fully implemented while retaining collision-insecure, non-default, independently-unverified and non-FIPS status. |
| `0.25.0` | Complete Generic HMAC Construction | Freeze a reusable no_std `brynja-mac-hmac` boundary over the admitted fixed-output hash interface, then implement the complete HMAC construction with long-key normalization, empty and block-boundary keys, arbitrary message partitioning, exact and policy-bounded truncation, constant-time verification, affine finalization, and hardened destruction; expose typed HMAC instantiations for every modern pre-1.0 fixed-output SHA-2 and SHA-3 digest without confusing MAC tags with unkeyed digests. |
| `0.25.1` | Isolated HMAC-SHA-1 And HMAC-MD5 Compatibility | Implement explicit legacy-only HMAC-SHA-1 and HMAC-MD5 adapters over the exact v0.24 implementations, with RFC vectors, truncation policy, constant-time verification, secret-state cleanup, and hard type and package isolation; keep them absent from modern defaults, TLS, PKIX, OpenPGP modern profiles, FIPS approved services, and generic algorithm negotiation. |
| `0.25.2` | HMAC Public API Usability Acceptance | Close the HMAC chain with downstream fixtures for every modern SHA-2/SHA-3 instantiation and the separately selected legacy SHA-1/MD5 adapters, exercising one-shot, streaming, long keys, truncated verification, invalid tags, package installation, cleanup evidence, and compile-time prevention of digest/tag or modern/legacy substitution. |
| `0.26.0` | Complete HKDF And TLS Labels | Implement complete generic RFC 5869 HKDF Extract and Expand over admitted HMAC algorithms plus TLS HKDF-Expand-Label, including absent versus empty salt, empty input and info, exact 255-block bounds, checked output and counter exhaustion, aliasing policy, secret-owned intermediate state, cleanup, and symbolic or bounded proof harnesses; modern profiles admit SHA-2 while any SHA-1 compatibility use remains isolated and explicit. |
| `0.26.1` | HKDF Public API Usability Acceptance | Exercise the packaged public HKDF API with all RFC 5869 cases, representative TLS 1.3 labels, multi-block outputs, boundary failures, irregular input ownership, modern SHA-2 instantiations, and the separately selected legacy SHA-1 compatibility path, proving no private API, allocation, std, implicit hash selection, or partial output is required. |
| `0.27.0` | Complete Portable AES-128 | Implement complete FIPS 197 AES-128 key expansion and forward and inverse block operations with typed keys, official vectors, immediate schedule destruction, portable constant-time code without secret-indexed tables, proofs, emitted-code evidence, and directly usable encrypt/decrypt APIs. |
| `0.27.1` | Complete Portable AES-192 And AES-256 | Complete FIPS 197 AES-192 and AES-256 key expansion and forward and inverse block operations over the reviewed portable owner while preserving distinct key and round identities, typed APIs, official vectors, cleanup, proofs, and per-compiler and per-target constant-time evidence. |
| `0.27.2` | Complete AES x86_64 And AArch64 Acceleration | Add isolated AES-128, AES-192, and AES-256 encrypt and decrypt backends for exact x86_64 AES-NI or VAES bundles and exact AArch64 AES bundles, with AMD, observed-feature AWS Intel, Apple M2, and AWS Arm native evidence; retain identical KAT, quarantine, no_std static selection, opt-in std selection, side-channel, and destruction semantics. |
| `0.27.3` | Complete AES RISC-V Acceleration Candidate | Add RISC-V AES-128, AES-192, and AES-256 encrypt and decrypt backends for exact ratified scalar-crypto or vector-crypto bundles only when the compiler and observed deployment ISA can express them safely; require official vectors, scalar differentials, forced dispatch, generated-code review, and qualifying native evidence before admission. |
| `0.27.4` | AES Public API Usability Acceptance | Close the AES primitive chain with a packaged public consumer that performs forward and inverse known-answer operations for all three key widths through scalar and every admitted backend, verifies round trips and schedule cleanup, rejects wrong key sizes and unavailable forced paths, and preserves no_std, package, and architecture isolation. |
| `0.28.0` | GHASH | Implement constant-time GHASH finite-field arithmetic and a bounded incremental interface. |
| `0.28.1` | GHASH CPU Acceleration | Add exact x86_64 carry-less multiplication, AArch64 PMULL, and qualifying RISC-V crypto/vector GHASH backends with identical field representation, reduction, incremental boundaries, and finalization; test every backend independently and paired with scalar AES, require native correctness, timing, and performance evidence, and keep feature detection, activation, KAT health, and FIPS identity explicit. |
| `0.28.2` | GHASH Public API Usability Acceptance | Close the GHASH chain with a packaged downstream fixture covering empty, partial, multi-block, incremental, scalar, and every admitted accelerated path against authoritative field and GCM-derived vectors, while proving canonical field representation, length accounting, finalization, and no_std package usability. |
| `0.29.0` | Complete AES-GCM | Implement complete NIST SP 800-38D AES-GCM seal and open over AES-128, AES-192, and AES-256, including 96-bit and general IV processing, supported tag lengths, nonce and invocation limits, checked length domains, official vectors, authenticate-before-release, exact in-place or disjoint buffers, partial-overlap rejection, unchanged failure destinations, and failure-atomicity proofs. |
| `0.29.1` | Complete GMAC | Implement the complete AAD-only GMAC surface over every admitted AES width with exact IV and tag policies, invocation and length limits, constant-time verification, official vectors, state cleanup, distinct MAC tag types, and public APIs that cannot be confused with AES-GCM ciphertext operations. |
| `0.29.2` | Integrated Accelerated AES-GCM And GMAC | Integrate only admitted AES and GHASH backend pairs into accelerated AES-GCM and GMAC providers with per-operation dispatch, preserving every nonce, limit, buffer, authentication, failure-atomicity, cancellation, equivalence, health, and scalar-fallback rule; prohibit mixed, unhealthy, or unvalidated pairings. |
| `0.29.3` | AES-GCM And GMAC Public API Usability Acceptance | Close the GCM chain with packaged public seal, open, and GMAC fixtures spanning all AES widths, admitted IV and tag sizes, AAD-only, empty, partial, multi-block, in-place, disjoint, scalar, and accelerated routes; verify authoritative results, tamper rejection with unchanged output, limit exhaustion, package installation, and precise FIPS-versus-unvalidated status. |
| `0.30.0` | Complete Portable ChaCha20 And Public Acceptance | Implement complete ChaCha20 with original and IETF nonce/counter profiles only where authenticated consumers require them, checked counters, deterministic exhaustion, exact block and tail behavior, overlap rules, official vectors, proofs, and a packaged public consumer so the scheduled checkpoint publishes a directly usable scalar implementation. |
| `0.30.1` | ChaCha20 CPU Acceleration | Add benchmark-admitted x86_64, AArch64, and qualifying RISC-V ChaCha20 backends for parallel blocks while preserving the scalar quarter-round, counter, nonce, tail, overlap, and deterministic exhaustion contract; force each width and tail path, compare every output with scalar, and reject wider paths that regress representative TLS record sizes. |
| `0.30.2` | ChaCha20 Accelerated Public API Usability Acceptance | Re-run the packaged ChaCha20 consumer through every admitted accelerated backend and scalar fallback, covering original and IETF profiles, counters, block and tail lengths, overlap, exhaustion, unavailable required paths, package installation, backend reporting, and exact output equivalence. |
| `0.31.0` | Complete Poly1305 | Implement complete Poly1305 key clamping, block and partial-block processing, canonical reduction, one-time-key ownership, constant-time verification, checked lengths, cleanup, official vectors, proofs, and distinct public MAC APIs without yet composing an AEAD. |
| `0.31.1` | Complete ChaCha20-Poly1305 | Compose the exact admitted ChaCha20 and Poly1305 owners into complete RFC 8439 AEAD seal and open with AAD, nonce and length limits, one-time-key derivation, authenticate-before-release, exact in-place or disjoint buffers, partial-overlap rejection, unchanged failure destinations, cleanup, official vectors, and failure-atomicity proofs. |
| `0.31.2` | Poly1305 And ChaCha20-Poly1305 CPU Acceleration | Add benchmark-admitted x86_64, AArch64, and qualifying RISC-V Poly1305 backends and integrate them only with compatible admitted ChaCha20 paths; preserve one-time-key, reduction, verification, aliasing, plaintext-release, failure-atomicity, health, and scalar-equivalence rules. |
| `0.31.3` | ChaCha20 Poly1305 And AEAD Public API Usability Acceptance | Close the RFC 8439 chain with packaged downstream ChaCha20, Poly1305, and ChaCha20-Poly1305 operations using only public APIs, covering block and tail counters, one-time keys, AAD, in-place and disjoint buffers, tamper and exhaustion failures, official vectors, scalar and every admitted backend, and no plaintext release before authentication. |
| `0.32.0` | Fixed-Limb Integer Foundation | Implement fixed-limb unsigned representation, canonical conversion, comparison, addition, subtraction, shifts, carry, borrow, normalization, and RSA-size policies without attacker-selected allocation, schedule, or limb count; introduce full-width or limb-generic harnesses and a directly testable internal arithmetic API. |
| `0.32.1` | Fixed-Limb Multiplication And Montgomery Arithmetic | Complete fixed-schedule multiplication, squaring, Montgomery conversion, multiplication, reduction, and modulus validation over the reviewed limb owner with production-width vectors, carry and reduction proofs, aliasing rules, and no variable-time secret normalization. |
| `0.32.2` | Fixed-Limb Modular Exponentiation Acceptance | Complete fixed-schedule public and secret modular exponentiation, window and table-access policy, RSA modulus and exponent bounds, cleanup, proof claims, differential vectors, and a packaged arithmetic fixture before RSA encoding or key operations consume the foundation. |
| `0.33.0` | Fixed-Width Prime-Field Foundation | Implement fixed-width canonical prime-field representation, conversion, addition, subtraction, multiplication, squaring, reduction, equality, conditional selection, and zero/one rules separately from RSA limbs, with field-modulus identity and full-width or explicitly bounded proofs. |
| `0.33.1` | Prime-Field Inversion Square Roots And Scalar Primitives | Complete fixed-schedule inversion, square-root and nonsquare handling, scalar representation and range validation, reduction, conditional negation, batch policy, exceptional inputs, cleanup, and proof harnesses needed by admitted curves without exposing a generic unsafe modulus API. |
| `0.33.2` | ECC Complete-Formula Foundation Acceptance | Freeze the complete-formula and coordinate invariants shared by admitted curves, exercise identity, doubling, addition, negation, exceptional and canonicalization cases through concrete test fields, and package the arithmetic evidence required before X25519 or short-Weierstrass curve milestones begin. |
| `0.33.3` | DER Ownership And Type-Coverage Foundation | Extract the existing DER reader and ASN.1 values into one zero-external-dependency no_std brynja-encoding-der leaf with compatibility re-exports from brynja-pki; freeze an authenticated X.680/X.690 and errata type/rule inventory, public codec profiles, exact resource limits and paired-operation owners before any encoder or cryptographic consumer is added. |
| `0.33.4` | Transactional DER Length And Framing Writer | Implement public checked DER encoded-length measurement and tag-length-value writing in brynja-encoding-der with all tag classes, high-tag numbers, minimal definite lengths, caller-owned buffers, exact completion and unchanged public output on failure; pair every emitted frame with the existing strict reader without yet claiming complete typed DER values. |
| `0.33.5` | Canonical DER Basic Value Codecs | Complete paired public encoding and decoding for DER BOOLEAN, INTEGER, ENUMERATED, NULL, BIT STRING, OCTET STRING, OBJECT IDENTIFIER and RELATIVE-OID values using canonical byte representations, caller-owned buffers, checked lengths and exact type identity without machine-integer truncation. |
| `0.33.6` | Canonical DER Extended Value Codecs | Complete the remaining authenticated DER universal value codecs, including REAL, character-string families, object descriptors, time/date/duration and IRI forms where defined by the admitted ASN.1 edition, with matching strict decoders and canonical writers; close the full type inventory without substituting unchecked bytes or platform floating-point approximations. |
| `0.33.7` | Schema-Aware Constructed DER Codecs | Implement bounded public schema-aware DER SEQUENCE, SET, SEQUENCE OF, SET OF, CHOICE, explicit and implicit tags, OPTIONAL and DEFAULT handling plus remaining structured universal forms over the shared leaf; enforce exact canonical ordering, schema constraints and byte preservation for signed objects in both directions. |
| `0.33.8` | Hardened DER Secret-Output Ownership | Add explicit hardened DER serialization profiles that clear every Brynja-owned secret temporary and partial output, transfer completed private encodings only into typed secret-owned destinations, and retain transactional public-output behavior; reuse the mandatory core clearing boundary and optional sanitization adapter only under its admitted contract. |
| `0.33.9` | Complete DER Public Codec Acceptance | Close the standalone DER chain with package-external encode/decode fixtures for every admitted universal type and schema rule, ordinary and hardened output, exact independent encodings, canonical round trips, malformed input and bounded failure; block ECDSA signature encoding, key containers and PKIX generation until the shared leaf passes. |
| `0.34.0` | X25519 Field And Ladder | Implement X25519 field encoding, canonical decoding policy, clamping, fixed Montgomery ladder, and low-order input handling, with full-width or explicitly reduced-width ladder and exceptional-input proof harnesses introduced beside the implementation. |
| `0.35.0` | X25519 ECDH Lifecycle | Implement unbiased ephemeral input generation, no ephemeral private-key reuse, imported public and private consistency policy, all-zero shared-secret rejection, immediate scalar destruction, and provider-token binding to group, connection, and transcript; reusable static agreement remains a separately typed v0.45.17-v0.45.18 profile. |
| `0.35.1` | X25519 CPU Acceleration | Benchmark and, where the frozen margin and constant-time evidence pass, add fixed-schedule x86_64, AArch64, and qualifying RISC-V X25519 field and ladder backends; preserve clamping, canonical input policy, low-order and all-zero rejection, ephemeral lifecycle, immediate scalar destruction, and exact provider-token binding, and retain scalar for any CPU family whose optimized path is not independently evidenced. |
| `0.35.2` | X25519 Public API Usability Acceptance | Close the X25519 chain with a packaged downstream fixture that generates and imports keys, computes both sides of real exchanges, checks RFC 7748 vectors and iteration cases, rejects low-order and all-zero results, exercises lifecycle and cleanup failures, and forces scalar and every admitted backend without private hooks. |
| `0.36.0` | P-256 Group Operations | Implement P-256 point decoding, on-curve and subgroup validation, complete group operations, fixed-schedule scalar multiplication, and official group vectors; introduce point-rejection, scalar-range, group-exception, and canonicalization proof harnesses beside the implementation. |
| `0.37.0` | P-256 ECDH Lifecycle | Implement unbiased P-256 private-scalar generation, no ephemeral reuse, imported key consistency, invalid shared-secret handling, immediate scalar destruction, and exact group, connection, and transcript provider-token binding. |
| `0.38.0` | P-256 ECDSA | Implement P-256 ECDSA signing and verification, strict fixed-width and shared v0.33.9 DER signature encoding/decoding, low-S policy decision, and deterministic and randomized nonce policy using the secure-random contract. |
| `0.38.1` | P-256 CPU Acceleration | Benchmark and admit isolated x86_64, AArch64, and qualifying RISC-V P-256 field, group, scalar-multiplication, ECDH, and ECDSA backends only with complete-formula, scalar-range, nonce, strict-encoding, low-S, lifecycle, differential, fault, and per-target constant-time evidence; signing and verification backend identities remain explicit and independently quarantinable. |
| `0.38.2` | P-256 Public API Usability Acceptance | Close the P-256 chain with packaged downstream key import and generation, SEC1 point encoding and decoding, ECDH agreement, deterministic and randomized ECDSA signing and verification, malformed and low-S policy cases, scalar and admitted accelerated paths, authoritative vectors, and immediate secret-lifecycle evidence. |
| `0.39.0` | P-384 Group Operations | Implement P-384 point decoding, on-curve and subgroup validation, complete group operations, fixed-schedule scalar multiplication, and official group vectors; introduce point-rejection, scalar-range, group-exception, and canonicalization proof harnesses beside the implementation. |
| `0.40.0` | P-384 ECDH Lifecycle | Implement unbiased P-384 private-scalar generation, no ephemeral reuse, imported key consistency, invalid shared-secret handling, immediate scalar destruction, and exact group, connection, and transcript provider-token binding. |
| `0.41.0` | P-384 ECDSA | Implement P-384 ECDSA signing and verification with strict fixed-width and shared v0.33.9 DER signature encoding/decoding, nonce policy, vectors, per-target side-channel evidence, and independent review. |
| `0.41.1` | P-384 CPU Acceleration | Benchmark and admit isolated x86_64, AArch64, and qualifying RISC-V P-384 field, group, scalar-multiplication, ECDH, and ECDSA backends under the same complete-formula, strict-encoding, nonce, lifecycle, differential, fault, and side-channel duties as scalar; record a reviewed scalar-only decision wherever code size, latency, or evidence does not justify acceleration. |
| `0.41.2` | P-384 Public API Usability Acceptance | Close the P-384 chain with the same packaged downstream key, point, ECDH, ECDSA, malformed-input, nonce, low-S, lifecycle, vector, scalar, and accelerated-path evidence required for P-256, using only the ordinary public P-384 API and package artifacts. |
| `0.42.0` | RSA-PSS Verification | Implement strict RSA public-key decoding and RSA-PSS verification with unambiguous parameters and modulus and exponent policy. |
| `0.43.0` | RSA PKCS1 v1.5 Verification | Implement strict RSASSA-PKCS1-v1_5 certificate-signature verification for SHA-256, SHA-384 and SHA-512 with complete padding, exact DigestInfo, no trailing bytes, and no SHA-1 or MD5 aliases; keep TLS CertificateVerify and signing excluded. |
| `0.44.0` | RSA-PSS Private Operations | Implement blinded fixed-schedule first-party RSA-PSS private operations for strictly validated imported keys, with CRT consistency, fault detection, immediate blinding and intermediate destruction, and external-signer support; key generation is deferred from this milestone to v0.46.55, not excluded from v1. |
| `0.44.1` | RSA CPU Acceleration | Benchmark and admit architecture-specific fixed-limb multiplication, squaring, reduction, and exponentiation backends for x86_64, AArch64, and qualifying RISC-V without changing modulus policy, imported-key validation, blinding, fixed schedule, CRT consistency, fault detection, or intermediate destruction; verification and private-operation paths remain distinct and every optimized symbol receives arithmetic proof and native side-channel evidence. |
| `0.44.2` | RSA Public API Usability Acceptance | Close the admitted RSA chain with packaged downstream RSA-PSS and strict PKCS1 v1.5 verification plus blinded RSA-PSS signing using validated imported keys, official and adversarial vectors, malformed encodings, CRT and fault failures, scalar and admitted accelerated arithmetic, external-signer composition, and complete intermediate destruction. |
| `0.45.0` | Complete Ed25519 And Checkpoint Acceptance | Implement complete RFC 8032 Ed25519 signing and verification with canonical encodings, small-order and malleability rejection, deterministic nonce derivation, official vectors, constant-time secret operations, cleanup, and a packaged public fixture so the scheduled checkpoint publishes a directly usable pure Ed25519 capability. |
| `0.45.1` | Complete Ed25519ctx | Implement complete Ed25519ctx signing and verification over the reviewed Ed25519 owner with exact domain separation, context length and empty-context semantics, canonical validation, official vectors, public types that cannot be confused with pure Ed25519, constant-time secret operations, and cleanup. |
| `0.45.2` | Complete Ed25519ph | Implement complete Ed25519ph signing and verification over the reviewed Ed25519 owner with exact prehash and context domain separation, streaming prehash ownership, context limits, canonical validation, official vectors, public types that cannot be confused with Ed25519 or Ed25519ctx, constant-time secret operations, and cleanup. |
| `0.45.3` | Ed25519 Family CPU Acceleration | Benchmark and admit fixed-schedule x86_64, AArch64, and qualifying RISC-V Ed25519-family field, scalar, and group backends only when pure, context, and prehash modes preserve canonical encoding, domain separation, rejection, lifecycle, fault handling, scalar equivalence, and per-target constant-time evidence. |
| `0.45.4` | Ed25519 Family Public API Usability Acceptance | Close Ed25519, Ed25519ctx, and Ed25519ph with packaged public sign and verify fixtures, authoritative positive and negative vectors, cross-mode rejection, no_std installation, secret cleanup, external-signer composition, and scalar plus every admitted backend. |
| `0.45.5` | Complete X448 Field And Ladder | Implement complete RFC 7748 X448 field arithmetic, encoding and canonical-decoding policy, scalar clamping, fixed Montgomery ladder, low-order and all-zero handling, official vectors, proofs, constant-time evidence, and a public raw agreement primitive without yet admitting reusable ECDH key lifecycle. |
| `0.45.6` | Complete X448 ECDH Lifecycle | Complete X448 key import and consistency validation, unbiased ephemeral generation, single-use agreement, invalid-secret handling, immediate scalar destruction, provider-token lifecycle, and packaged cross-party public ECDH usability over the exact v0.45.5 primitive. |
| `0.45.7` | Complete Ed448 | Implement complete RFC 8032 Ed448 signing and verification with exact context domain separation, canonical field, scalar and point encodings, subgroup and malleability rejection, deterministic nonce derivation, official vectors, constant-time secret operations, cleanup, and a documented public API. |
| `0.45.8` | Complete Ed448ph | Implement complete Ed448ph signing and verification over the reviewed Ed448 owner with exact prehash and context domain separation, streaming prehash ownership, canonical validation, official vectors, distinct public types, constant-time secret operations, and cleanup. |
| `0.45.9` | Curve448 CPU Acceleration | Benchmark and admit x86_64, AArch64, and qualifying RISC-V X448 and Ed448 field, ladder, scalar, and group backends only where native performance and per-mode correctness, canonicalization, lifecycle, fault, emitted-code, side-channel, KAT, quarantine, and scalar-equivalence evidence pass. |
| `0.45.10` | Curve448 Public API Usability Acceptance | Close X448, Ed448, and Ed448ph with packaged downstream agreement, sign, and verify fixtures, authoritative vectors, cross-party agreement, negative and cross-mode cases, no_std installation, secret cleanup, and every admitted backend without merging protocol identities. |
| `0.45.11` | Complete P-521 Field And Group Operations | Implement P-521 field and scalar arithmetic, SEC1 point encoding and decoding, on-curve and subgroup validation, complete group operations, fixed-schedule scalar multiplication, official vectors, proofs, and public point operations as the reviewed foundation for later ECDH. |
| `0.45.12` | Complete P-521 ECDH Lifecycle | Complete P-521 unbiased private generation, public-key import and consistency validation, single-use ECDH, invalid-secret handling, immediate destruction, provider-token lifecycle, official agreement vectors, and a documented public API over the exact v0.45.11 group owner. |
| `0.45.13` | P-521 CPU Acceleration | Benchmark and admit separately reviewable P-521 x86_64, AArch64, and qualifying RISC-V field, scalar, group, validation, and ECDH paths only where proofs, emitted-code, side-channel, native correctness, performance, KAT, quarantine, fault, lifecycle, and scalar-equivalence evidence pass. |
| `0.45.14` | P-521 Public API Usability Acceptance | Close P-521 with packaged point, key-import, cross-party ECDH, malformed-input, invalid-secret, lifecycle, cleanup, authoritative-vector, scalar, and every admitted accelerated consumer path, preserving no_std installation and future HPKE integration boundaries. |
| `0.45.15` | Complete P-521 ECDSA | Complete reusable P-521 ECDSA signing and verification over the existing P-521 arithmetic with deterministic and randomized nonce policy, digest/prehash handling, canonical fixed-width and DER signatures, key generation/import/export, low-S policy, cleanup and a frozen portable public fixture. |
| `0.45.16` | P-521 Signature Cross-Backend Acceptance | Rerun the frozen P-521 ECDSA fixture through portable and every eligible existing P-521 backend, qualifying signature-specific timing, nonce, fault and cleanup behavior before admission; close P-521 signing and verification explicitly before TLS, PKIX and OpenPGP consumers. |
| `0.45.17` | Reusable Static Key-Agreement Ownership | Add explicitly selected reusable static key-agreement profiles for X25519, X448, P-256, P-384 and P-521 beside the existing single-use ephemeral owners, with validated import/generation, typed secret export where authorized, peer validation, public-key derivation and protocol-domain-separated lifecycle for later HPKE and OpenPGP consumers. |
| `0.45.18` | Static And Ephemeral Agreement Acceptance | Close public static and ephemeral agreement profiles through packaged role-aware workflows and every eligible admitted backend before HPKE, key containers or OpenPGP reuse the owners; preserve single-use ephemeral guarantees and explicit longer-lived static-key policy. |
| `0.46.0` | Version-One Algorithm And Transitive Completeness Register | Freeze the authenticated modern and historical algorithm, operation, parameter, format, protocol-consumer, and registry closure for every pre-1.0 Brynja capability; assign every standardized item to one modern or opt-in legacy owner, generate its transitive dependency graph, and reject partial families, missing directions, duplicate implementations, or unspecified compatibility edges. |
| `0.46.1` | Review-Sized Cryptographic Substrate Schedule Freeze | Convert the v0.46.0 register into an exact review-sized tagged schedule before implementation begins; require one independently testable algorithm identity or tightly coupled construction per implementation row, place a frozen package-external portable acceptance boundary before every acceleration or admission row and a final unchanged-fixture cross-backend acceptance boundary after its evidence, and insert or reorder patch rows rather than enlarging a frozen row when authenticated sources disclose more work. |
| `0.46.2` | Complete Legacy MD2 And PKIX Hash Boundary | Implement complete streaming and fixed-message MD2 in isolated `brynja-legacy-md2` with RFC 1319 vectors, padding, checksum, checked lengths, collision warnings, a public compatibility API, and only separately admitted historical certificate or container consumers. |
| `0.46.3` | Complete Legacy RIPEMD-160 | Implement complete streaming and fixed-message RIPEMD-160 in isolated `brynja-legacy-ripemd160` with authoritative vectors, checked exhaustion, collision and strength warnings, public compatibility APIs, and typed later OpenPGP or certificate consumers. |
| `0.46.4` | Finite-Field Group Arithmetic And Validation | Implement reusable finite-field arithmetic, validated safe-prime parameters, public-key validation, fixed-schedule exponentiation, unbiased private exponents, imported-key consistency, cleanup, vectors, and proof harnesses for finite-field DH consumers without yet claiming every FFDHE named group. |
| `0.46.5` | Complete FFDHE Groups And Public Acceptance | Complete FFDHE2048 through FFDHE8192 over the v0.46.4 owner and exercise every group through packaged key-generation, validation, two-party agreement, invalid-key, lifecycle, cleanup, no_std, vector, and public API fixtures. |
| `0.46.6` | Complete Legacy DSA Parameters And Keys | Implement every authenticated historical DSA parameter profile required by PKIX, TLS, or OpenPGP, including parameter validation and generation, key generation and import, subgroup and range rules, deterministic test seams, cleanup, vectors, and isolated public key APIs. |
| `0.46.7` | Complete Legacy DSA Signing Verification And Acceptance | Complete deterministic and randomized DSA signing, strict verification, encodings, nonce lifecycle, fault defenses, authoritative vectors, and packaged public compatibility fixtures across every v0.46.6 parameter profile with no modern default or FIPS-approved edge. |
| `0.46.8` | Complete Legacy ElGamal Parameters And Keys | Implement OpenPGP-compatible ElGamal parameter and key generation, import, validation, subgroup policy, randomness ownership, encodings, cleanup, proof harnesses, and public key APIs without yet releasing decrypted plaintext. |
| `0.46.9` | Complete Legacy ElGamal Encryption Decryption And Acceptance | Complete ElGamal encryption and oracle-resistant decryption over v0.46.8 with uniform failure, blinding, fault resistance, authoritative vectors, transactional plaintext release, packaged public fixtures, and no signature or modern-default confusion. |
| `0.46.10` | Complete AES-CCM | Implement complete AES-CCM over AES-128, AES-192, and AES-256 with every standards-admitted nonce, message, AAD, and tag domain, invocation limits, exact in-place or disjoint operation, partial-overlap rejection, failure atomicity, vectors, proofs, and public APIs. |
| `0.46.11` | Complete CCM-8 Profiles And CCM Acceptance | Bind exact CCM-8 profiles to v0.46.10, then exercise CCM and CCM-8 through packaged public fixtures across every AES width, nonce, tag, AAD, boundary, tamper, overlap, exhaustion, scalar, and admitted accelerated path. |
| `0.46.12` | Complete ECB Building Block And CBC Mode | Implement the internal ECB building-block and complete CBC encrypt/decrypt over admitted block ciphers with exact IV, caller-selected padding separation, streaming, overlap, length, error, cleanup, vectors, and explicit legacy-policy selection. |
| `0.46.13` | Complete CTR Mode | Implement complete CTR over admitted block ciphers with exact counter layouts selected by typed profiles, preflighted counter exhaustion, streaming and random partition equivalence, overlap rules, cleanup, vectors, and public APIs. |
| `0.46.14` | Complete CFB And OFB Modes | Implement complete CFB at every authenticated segment width and complete OFB over admitted block ciphers with exact IV, streaming, feedback, overlap, exhaustion, cleanup, vectors, and public compatibility APIs. |
| `0.46.15` | Block-Cipher Modes Public API Usability Acceptance | Exercise v0.46.12-v0.46.14 through packaged mode fixtures and authoritative vectors for every admitted cipher, direction, segment width, IV, padding boundary, partition, overlap, malformed input, exhaustion, no_std, and modern-versus-legacy policy. |
| `0.46.16` | Complete Legacy DES | Implement complete DES encrypt/decrypt, key schedule, parity and weak-key policy, official vectors, constant-time portable code, cleanup, and a public compatibility API with no default, modern, approved-service, or implicit-negotiation edge. |
| `0.46.17` | Complete Two-Key TripleDES | Implement complete two-key TripleDES encrypt/decrypt over the reviewed DES owner with exact keying option, parity, weak-key, block-limit, cleanup, vector, and isolated public compatibility behavior. |
| `0.46.18` | Complete Three-Key TripleDES And DES Family Acceptance | Implement complete three-key TripleDES and close DES, two-key TripleDES, and three-key TripleDES through packaged vectors, round trips, weak-key, parity, limit, cleanup, no_std, and isolation fixtures. |
| `0.46.19` | Complete Legacy RC2 | Implement complete RC2 with effective-key-bit processing, key expansion, encrypt/decrypt, all authenticated parameter profiles, authoritative vectors, constant-time evidence, cleanup, and an isolated public compatibility API. |
| `0.46.20` | Complete Legacy RC4 | Implement complete RC4 key scheduling and stream processing with exact historical key and state semantics, exhaustion, bias and cryptanalytic warnings, vectors, cleanup, and an isolated public compatibility API. |
| `0.46.21` | Complete Legacy RC5 | Implement every authenticated RC5 word-size, round-count, and key-length profile required by named legacy protocols, including key expansion, encrypt/decrypt, vectors, bounds, cleanup, and isolated public APIs. |
| `0.46.22` | RC2 RC4 And RC5 Public Acceptance | Exercise RC2, RC4, and every admitted RC5 parameter profile through packaged public fixtures, authoritative vectors, boundaries, streaming where applicable, cleanup, no_std support, warnings, and hard modern/FIPS graph isolation. |
| `0.46.23` | Complete Legacy IDEA | Implement complete IDEA key schedule and forward and inverse block operations with every standardized key rule, authoritative vectors, constant-time evidence, cleanup, block limits, and an isolated public compatibility API. |
| `0.46.24` | Complete Legacy CAST5 | Implement complete CAST5/CAST-128 key schedule and encrypt/decrypt operations with every standardized key rule, authoritative vectors, constant-time evidence, cleanup, block limits, and an isolated public compatibility API. |
| `0.46.25` | IDEA And CAST5 Public Acceptance | Exercise IDEA and CAST5 through packaged public encrypt/decrypt, vector, boundary, weak-key, cleanup, no_std, and isolation fixtures before any OpenPGP, TLS, WTLS, PCT, or SNP consumer selects them. |
| `0.46.26` | Complete Legacy Blowfish | Implement complete Blowfish key expansion and encrypt/decrypt for every authenticated key-size profile, with vectors, weak-key and block-limit policy, constant-time evidence, cleanup, and an isolated public compatibility API. |
| `0.46.27` | Complete Legacy Twofish | Implement complete Twofish key schedules and encrypt/decrypt for every standardized key size required by authenticated Brynja protocols, with vectors, limits, constant-time evidence, cleanup, and isolated public APIs. |
| `0.46.28` | Blowfish And Twofish Public Acceptance | Exercise Blowfish and Twofish through packaged public fixtures across every admitted key size, direction, vector, boundary, weak-key, block-limit, cleanup, no_std, and modern-versus-legacy isolation case. |
| `0.46.29` | Complete Camellia-128 | Implement complete Camellia-128 key schedule and encrypt/decrypt with authoritative vectors, constant-time portable code, cleanup, block limits, public APIs, and distinct algorithm identity. |
| `0.46.30` | Complete Camellia-192 And Camellia-256 | Implement complete Camellia-192 and Camellia-256 over the reviewed Camellia owner with distinct key schedules and identities, authoritative vectors, constant-time portable code, cleanup, block limits, and public APIs. |
| `0.46.31` | Camellia Acceleration And Public Acceptance | Admit Camellia acceleration only with native evidence, then exercise all three Camellia widths through packaged scalar and accelerated encrypt/decrypt, vector, limit, cleanup, no_std, protocol-policy, KAT, quarantine, and public API fixtures. |
| `0.46.32` | Complete SEED | Implement complete SEED key schedule and encrypt/decrypt with authoritative vectors, constant-time portable code, cleanup, block limits, and an isolated public compatibility API for authenticated TLS and legacy consumers. |
| `0.46.33` | SEED Public API Usability Acceptance | Exercise SEED through packaged public encrypt/decrypt, authoritative vector, boundary, block-limit, cleanup, no_std, protocol-policy, and isolation fixtures before any consumer binding. |
| `0.46.34` | Complete ARIA-128 | Implement complete ARIA-128 key schedule and encrypt/decrypt with authoritative vectors, constant-time portable code, cleanup, block limits, public APIs, and distinct algorithm identity. |
| `0.46.35` | Complete ARIA-192 And ARIA-256 | Implement complete ARIA-192 and ARIA-256 over the reviewed ARIA owner with distinct key schedules and identities, authoritative vectors, constant-time portable code, cleanup, block limits, and public APIs. |
| `0.46.36` | ARIA Acceleration And Public Acceptance | Admit ARIA acceleration only with native evidence, then exercise all three ARIA widths through packaged scalar and accelerated encrypt/decrypt, vector, limit, cleanup, no_std, protocol-profile, KAT, quarantine, and public API fixtures. |
| `0.46.37` | Complete SM3 | Implement complete streaming and fixed-message SM3 with authoritative vectors, checked lengths and exhaustion, constant-time applicability, optional admitted acceleration, cleanup policy, a public hash API, and explicit regional-profile selection. |
| `0.46.38` | Complete SM4 | Implement complete SM4 key schedule and encrypt/decrypt with authoritative vectors, constant-time portable and admitted accelerated paths, cleanup, block limits, public APIs, and exact later TLS mode bindings. |
| `0.46.39` | SM3 And SM4 Public Acceptance | Exercise SM3 and SM4 through separate packaged public hash and block-cipher fixtures, vectors, boundaries, streaming, encrypt/decrypt, scalar and accelerated paths, cleanup, no_std, backend health, and explicit regional-policy selection. |
| `0.46.40` | Complete SM2 Field Group And Keys | Implement complete SM2 field and scalar arithmetic, point operations, encodings, subgroup rules, key generation, import and validation, official vectors, proofs, cleanup, and typed public key APIs. |
| `0.46.41` | Complete SM2 Signing And Verification | Implement complete SM2 signing and verification over v0.46.40 with identity binding, nonce and digest rules, encodings, range and malleability policy, fault defenses, vectors, cleanup, and public APIs. |
| `0.46.42` | Complete SM2 Key Agreement And Encryption | Implement complete SM2 key agreement, KDF composition, encryption and oracle-resistant decryption with identity, confirmation, randomness, subgroup, failure-atomicity, lifecycle, vectors, cleanup, and public APIs. |
| `0.46.43` | SM2 Family Public API Usability Acceptance | Exercise SM2 keys, sign/verify, agreement, encrypt/decrypt, identities, encodings, malformed inputs, faults, cleanup, no_std, and separately typed TLS and PKIX profiles through packaged public fixtures. |
| `0.46.44` | Complete GOST R 34.11-94 | Implement complete streaming and fixed-message GOST R 34.11-94 for every authenticated parameter set, byte-order and padding rule, authoritative vectors, checked exhaustion, warnings, and isolated public compatibility APIs. |
| `0.46.45` | Complete Streebog-256 And Streebog-512 | Implement complete streaming and fixed-message Streebog-256 and Streebog-512 with distinct identities, byte order, padding, authoritative vectors, checked exhaustion, constant-time applicability, and public APIs. |
| `0.46.46` | GOST Hash Family Public Acceptance | Exercise GOST R 34.11-94 and both Streebog variants through packaged one-shot, streaming, vector, boundary, parameter-set, byte-order, exhaustion, no_std, warning, and protocol-binding fixtures. |
| `0.46.47` | Complete GOST 28147-89 | Implement complete GOST 28147-89 encrypt/decrypt for every authenticated parameter set with key schedule, vectors, constant-time evidence, cleanup, limits, and isolated public compatibility APIs. |
| `0.46.48` | Complete Magma | Implement complete Magma key schedule and encrypt/decrypt with exact standardized byte order, authoritative vectors, constant-time evidence, cleanup, limits, and public APIs. |
| `0.46.49` | Complete Kuznyechik | Implement complete Kuznyechik key schedule and encrypt/decrypt with exact standardized byte order, authoritative vectors, constant-time evidence, cleanup, limits, and public APIs. |
| `0.46.50` | Complete GOST MAC Modes Key Meshing And Wrapping | Implement every authenticated GOST MAC, block-cipher mode, key-meshing, parameter-set, and key-wrapping construction required by registered TLS, PKIX, or named legacy consumers over the exact v0.46.47-v0.46.49 owners. |
| `0.46.51` | GOST Symmetric Family Public Acceptance | Exercise every admitted GOST symmetric cipher, parameter set, direction, MAC, mode, meshing, and wrapping profile through packaged vectors, malformed cases, limits, cleanup, no_std, and exact consumer-policy fixtures. |
| `0.46.52` | Complete GOST Signature Families | Implement every authenticated GOST R 34.10 signature generation, curve and parameter set with key generation and validation, sign, verify, encodings, vectors, proofs, faults, cleanup, and typed public APIs. |
| `0.46.53` | Complete GOST Agreement And KDF Composition | Implement every authenticated GOST agreement profile, VKO or related KDF composition, curve and parameter set with validation, derive, confirmation where required, lifecycle, vectors, proofs, cleanup, and public APIs. |
| `0.46.54` | GOST Public-Key Family Acceptance | Exercise every admitted GOST signature and agreement generation, curve, parameter set, encoding, key lifecycle, vector, negative, cleanup, no_std, TLS, and PKIX profile through packaged public fixtures. |
| `0.46.55` | Complete RSA Key Generation Import And Validation | Complete RSA key generation plus public and private import, structural and mathematical validation, modulus and exponent policy, prime generation, CRT construction, consistency checks, randomness, faults, proofs, cleanup, and public key APIs. |
| `0.46.56` | Complete RSA-PSS Signing And Verification | Complete RSA-PSS sign and verify over v0.46.55 across every admitted digest, salt, modulus, and parameter profile with blinding, CRT and fault checks, uniform failures, vectors, cleanup, and modern public APIs. |
| `0.46.57` | Complete RSAES-OAEP Encryption And Decryption | Complete RSAES-OAEP encrypt and oracle-resistant decrypt across every admitted digest, MGF, label, modulus, and parameter profile with blinding, fault checks, uniform failures, transactional plaintext release, vectors, cleanup, and public APIs. |
| `0.46.58` | Complete RSASSA-PKCS1-v1_5 Signing And Verification | Complete strict RSASSA-PKCS1-v1_5 sign and verify across every admitted digest and modulus profile with exact DigestInfo parsing, blinding, CRT and fault checks, uniform failures, vectors, cleanup, and explicitly selected modern or legacy APIs. |
| `0.46.59` | Complete RSAES-PKCS1-v1_5 Encryption And Decryption | Complete RSAES-PKCS1-v1_5 encrypt and oracle-resistant decrypt across every authenticated compatibility profile with exact padding, randomness, blinding, fault checks, uniform failures, transactional plaintext release, vectors, cleanup, warnings, and isolated APIs. |
| `0.46.60` | RSA Family Public API Usability Acceptance | Exercise RSA key generation/import, PSS, OAEP, signature PKCS1 v1.5, and encryption PKCS1 v1.5 through packaged vectors, malformed encodings, oracle and fault cases, cleanup, scalar and accelerated arithmetic, no_std, external-key, and policy-isolation fixtures. |
| `0.46.61` | Complete PBKDF2 | Implement complete PBKDF2 over every admitted HMAC with exact PRF identity, password, salt, iteration, block-counter, output, overflow and work limits, caller workspaces, cleanup, authoritative vectors, and public APIs. |
| `0.46.62` | Complete Legacy PBES1 Profiles | Implement every authenticated PKCS #5 PBES1 compatibility profile with exact KDF, cipher, parameters, limits, uniform password failures, cleanup, vectors, warnings, and isolated public APIs. |
| `0.46.63` | Complete PBES2 And EncryptedPrivateKeyInfo | Implement complete PBES2, every admitted PBKDF2 PRF and encryption scheme, strict parameters and limits, plus EncryptedPrivateKeyInfo import/export using the accepted v0.33.9 DER codecs and hardened output owner, uniform password failures, caller workspaces, cleanup, vectors, and public APIs. |
| `0.46.64` | Password KDF And Encrypted-Key Public Acceptance | Exercise PBKDF2, every PBES1 compatibility profile, PBES2, and EncryptedPrivateKeyInfo import/export through packaged vectors, password failures, hostile parameters, round trips, cleanup, no_std, and modern-versus-legacy policy fixtures. |
| `0.46.65` | Complete SSL 3.0 MAC And Key Schedule | Implement exact SSL 3.0 MAC, master-secret and key-block schedules, finished and certificate-verify digests, limits, constant-time verification, cleanup, authoritative vectors, and isolated public compatibility APIs. |
| `0.46.66` | Complete TLS 1.0 And TLS 1.1 PRF | Implement exact TLS 1.0/1.1 MD5-plus-SHA-1 PRF, master-secret, key-block, finished, and certificate-verify derivations with secret-state cleanup, length and label rules, vectors, and isolated public APIs. |
| `0.46.67` | Complete TLS 1.2 PRFs And Truncated HMAC Profiles | Implement TLS 1.2 PRFs across every admitted hash plus every authenticated truncated-HMAC profile, with exact labels, lengths, constant-time verification, cleanup, vectors, and typed public compatibility APIs. |
| `0.46.68` | Complete Export KDFs And Legacy TLS Crypto Acceptance | Implement every authenticated export-grade key expansion, then exercise v0.46.65-v0.46.68 MAC, PRF, finished, certificate-verify, truncation, export, limit, cleanup, and cross-version negative behavior through packaged fixtures without enabling protocol fallback. |
| `0.46.69` | Complete SRP-6a Groups Verifiers And Credentials | Implement complete SRP-6a group and public-value validation, verifier and credential generation, password and salt handling, invalid-value and offline-attack policy, vectors, proofs, cleanup, and public credential APIs. |
| `0.46.70` | Complete SRP-6a Sessions And Public Acceptance | Complete client and server ephemeral operations, proofs, session derivation, confirmation, lifecycle and failure behavior over v0.46.69, then exercise both roles through packaged vectors, invalid values, cleanup, no_std, and exact later TLS-SRP profile fixtures. |
| `0.46.71` | Complete DEFLATE Codec | Implement complete first-party RFC 1951 DEFLATE compression and decompression with all block types, canonical Huffman construction, bounded history and encoder workspaces, deterministic generation, stream completion, malformed-input and bomb defenses, vectors, differentials, and public APIs. |
| `0.46.72` | Complete ZLIB Codec | Implement complete first-party RFC 1950 ZLIB compression and decompression over v0.46.71 with exact header, dictionary policy, Adler-32, reset, completion, malformed-input, bomb, deterministic-generation, vector, differential, and public API behavior. |
| `0.46.73` | DEFLATE ZLIB ZIP And TLS Compression Acceptance | Bind raw ZIP and authenticated TLS DEFLATE framing to v0.46.71-v0.46.72, then exercise every direction, block type, checksum, reset, boundary, workspace, malformed stream, bomb defense, no_std, and public profile through packaged fixtures. |
| `0.46.74` | Complete Brotli Family | Implement complete first-party Brotli compression and decompression required by TLS certificate compression with admitted dictionary policy, bounded caller workspaces, deterministic generation, exact framing, bomb defenses, vectors, differential evidence, and public APIs without native code. |
| `0.46.75` | Complete Zstandard Family | Implement complete first-party Zstandard compression and decompression required by TLS certificate compression with admitted dictionary policy, bounded caller workspaces, deterministic generation, exact framing and checksums, bomb defenses, vectors, differential evidence, and public APIs without native code. |
| `0.46.76` | Brotli And Zstandard Public Acceptance | Exercise Brotli and Zstandard encoding, decoding, certificate-compression profiles, dictionaries, framing, checksums, workspaces, deterministic output, malformed input, bombs, no_std boundaries, and public APIs through packaged fixtures. |
| `0.46.77` | Complete Legacy LZS Compression | Implement complete first-party LZS compression and decompression plus exact TLS framing, reset, history, boundaries, malformed-stream and bomb defenses, vectors, bounded workspaces, public APIs, and legacy-policy behavior. |
| `0.46.78` | LZS Public API Usability Acceptance | Exercise LZS encode/decode and TLS framing through packaged public fixtures across history, reset, boundary, malformed, truncation, bomb, workspace, no_std, warning, and graph-isolation cases. |
| `0.46.79` | Complete BZip2 Decoder | Implement complete first-party BZip2 decompression with exact stream and block framing, transforms, Huffman decoding, CRCs, bounded caller workspaces, completion, malformed-input and bomb defenses, vectors, differentials, and public APIs. |
| `0.46.80` | Complete BZip2 Encoder | Implement complete first-party BZip2 compression over the v0.46.79 format owner with transforms, Huffman construction, CRCs, bounded caller workspaces, deterministic generation, stream and block completion, vectors, differentials, and public APIs. |
| `0.46.81` | BZip2 Public API Usability Acceptance | Exercise BZip2 encode/decode and later OpenPGP compatibility boundaries through packaged round trips, authoritative corpora, framing, CRC, deterministic-generation, malformed-input, bomb, workspace, no_std, and public API fixtures. |
| `0.46.82` | ML-DSA Foundation And Complete ML-DSA-44 | Implement the shared FIPS 204 arithmetic, sampling, encoding, key-consistency, randomness, context, cleanup, and proof foundation plus complete ML-DSA-44 key generation, deterministic and hedged signing, verification, vectors, malformed-input handling, and public APIs. |
| `0.46.83` | Complete ML-DSA-65 | Implement complete ML-DSA-65 over the reviewed v0.46.82 foundation with distinct parameters and types, key generation, deterministic and hedged signing, external randomness, context, verification, encodings, rejection sampling, vectors, faults, cleanup, and public APIs. |
| `0.46.84` | Complete ML-DSA-87 | Implement complete ML-DSA-87 over the reviewed v0.46.82 foundation with distinct parameters and types, key generation, deterministic and hedged signing, external randomness, context, verification, encodings, rejection sampling, vectors, faults, cleanup, and public APIs. |
| `0.46.85` | ML-DSA Acceleration And Public Acceptance | Admit optional acceleration only with parameter-specific native evidence, then exercise ML-DSA-44, ML-DSA-65, and ML-DSA-87 through packaged keygen, deterministic and hedged sign, verify, context, malformed, fault, cleanup, no_std, scalar, and accelerated fixtures. |
| `0.46.86` | Complete SLH-DSA SHA2-128 Parameter Sets | Implement complete FIPS 205 SLH-DSA SHA2-128s and SHA2-128f key generation, pure and prehash signing and verification where standardized, context, randomization, encodings, bounds, faults, cleanup, vectors, proofs, public APIs, and performance policy. |
| `0.46.87` | Complete SLH-DSA SHA2-192 And SHA2-256 Parameter Sets | Implement complete SLH-DSA SHA2 192s/192f and 256s/256f parameter sets over reviewed SHA2 foundations with distinct types, all standardized operations, context, randomization, encodings, bounds, faults, cleanup, vectors, proofs, and public APIs. |
| `0.46.88` | Complete SLH-DSA SHAKE-128 Parameter Sets | Implement complete SLH-DSA SHAKE-128s and SHAKE-128f key generation, pure and prehash signing and verification where standardized, context, randomization, encodings, bounds, faults, cleanup, vectors, proofs, public APIs, and performance policy. |
| `0.46.89` | Complete SLH-DSA SHAKE-192 And SHAKE-256 Parameter Sets | Implement complete SLH-DSA SHAKE 192s/192f and 256s/256f parameter sets over reviewed SHAKE foundations with distinct types, all standardized operations, context, randomization, encodings, bounds, faults, cleanup, vectors, proofs, and public APIs. |
| `0.46.90` | SLH-DSA Public API Usability Acceptance | Exercise all twelve SHA2 and SHAKE `s` and `f` SLH-DSA parameter sets through packaged keygen, pure and prehash sign/verify where standardized, context, randomization, encoding, malformed, fault, cleanup, no_std, vector, proof-claim, and performance-policy fixtures. |
| `0.46.91` | Cross-Backend Performance And Admission Gate | Reconcile every implemented primitive and operation against the scalar reference and CPU-backend register; require explicit admitted, candidate, rejected, or scalar-only decisions for AMD x86_64, observed-feature AWS Intel, Apple M2, AWS AArch64, and RISC-V with exact symbols, bundles, KAT, quarantine, dispatch, native evidence, residual risks, and FIPS dispositions. |
| `0.46.92` | Cryptographic Substrate Cross-Family Usability Acceptance | Exercise every v0.46.2-v0.46.90 primitive, construction, mode, KDF, MAC, public-key operation, compression direction, and parameter family through its package-external fixture; prove single ownership, explicit dangerous selection, no implicit modern negotiation, no unauthenticated output, cleanup, no_std portability, and exact modern, legacy, regional, PQ, and FIPS status. |
| `0.46.93` | Complete Registered Algorithm Closure Gate | Regenerate the transitive register from every authenticated TLS, DTLS, PKIX, OpenPGP, SSL, WTLS, PCT, and SNP source; block v0.47.0 unless every capability consumed by the completed substrate has full operation-direction acceptance and every later capability has an exact earlier-than-consumer implementation and acceptance owner, with no forward implementation dependency or unnumbered gap. |
| `0.47.0` | Cryptographic Substrate Audit Gate | Complete independent cryptographic-substrate review of every scalar primitive, admitted and candidate CPU implementation symbol, dispatcher, capability token, KAT and quarantine path, optional std adapter, unsafe boundary, per-target constant-time and zeroization claim, and residual gap; remediate findings before PKI or TLS consumption. |
| `0.47.1` | Base64-ng Admission And Encoding Boundary | Audit the latest stable first-party `base64-ng` family against MSRV, `no_std`, allocator, feature, dependency, unsafe, native-code, license, advisory, target, streaming, canonical-encoding, strict-decoding, caller-buffer and resource policies; exact-pin only the smallest acceptable default-feature-disabled package edge, require an allocation-free OpenPGP armor profile before admitting `base64-ng-openpgp`, and otherwise use `base64-ng` solely for transforms behind Brynja-owned PEM and armor framing without duplicating Base64. |
| `0.48.0` | Bounded PEM Decoding And Chain Containers | Using only the v0.47.1-admitted Base64 boundary, implement bounded strict PEM decoding plus certificate-chain containers with label, count, size, whitespace, trailing-data, canonical-encoding, and resource policies; retain a documented non-admission path rather than weakening no_std or allocation-free guarantees, and keep the bidirectional PEM family In progress until v0.48.1-v0.48.2 encoding and acceptance. |
| `0.48.1` | Transactional PEM Encoding | Implement bounded public PEM encoding over the exact admitted Base64 boundary and existing PEM labels/containers, with canonical output formatting, streaming completion, caller buffers, certificate-chain generation and explicit typed-secret private-key output; do not add another Base64 implementation. |
| `0.48.2` | PEM And Base64 Public Round-Trip Acceptance | Close bounded PEM encode/decode and admitted Base64 transformations with packaged public fixtures across labels, single and chain containers, streaming, canonical generation, strict parsing, malformed inputs and ordinary versus typed-secret output before private-key container integration. |
| `0.49.0` | Complete PKCS8 Unencrypted Private-Key Containers | Implement bounded PKCS #8 PrivateKeyInfo and OneAsymmetricKey import and export using the v0.33.9 shared DER codecs and hardened output owner, with strict version, algorithm and parameter binding, optional public-key consistency, caller-owned secret arenas, uniform failures, cleanup, and public round-trip APIs. |
| `0.49.1` | Complete SEC1 EC Private-Key Containers | Implement bounded SEC1 ECPrivateKey import and export for every admitted curve with exact named-curve, optional parameters and public-key consistency rules, canonical DER, caller-owned secret arenas, uniform failures, cleanup, and public round-trip APIs. |
| `0.49.2` | Complete PKCS1 RSA Private-Key Containers | Implement bounded PKCS #1 RSAPrivateKey import and export with exact multi-prime disposition, mathematical and CRT consistency, canonical DER, caller-owned secret arenas, uniform failures, cleanup, and public round-trip APIs. |
| `0.49.3` | Complete EncryptedPrivateKeyInfo Integration | Bind the exact v0.46.63 PBES2 and EncryptedPrivateKeyInfo owner to admitted PKCS #8 keys with strict algorithm parameters, hostile-work rejection, uniform password failures, transactional import/export, caller workspaces, cleanup, and public round trips. |
| `0.49.4` | Private-Key Container Public API Usability Acceptance | Exercise PKCS #8, SEC1, PKCS #1, and encrypted PKCS #8 import/export through packaged public round trips for every admitted key family, malformed and inconsistent structures, hostile parameters, wrong passwords, cleanup, no_std, and cross-format substitution failures. |
| `0.49.5` | Reusable SubjectPublicKeyInfo Codecs | Implement standalone public SubjectPublicKeyInfo import/export over the shared DER leaf for every already admitted key family, with exact AlgorithmIdentifier and parameter policy, BIT STRING framing, key validation and modern versus legacy selection independently of X.509 certificate issuance. |
| `0.49.6` | Algorithm-Specific Public-Key Format APIs | Complete public raw-key and PKCS1 RSAPublicKey import/export profiles alongside SubjectPublicKeyInfo, reusing existing SEC1 point and RFC 7748/8032 raw encodings with explicitly typed format selection, canonical constraints and no format guessing. |
| `0.49.7` | Public-Key Container Acceptance | Close SPKI and algorithm-specific public-key import/export with packaged independent-tool round trips, exact encodings, validation-state separation, malformed-input and policy isolation tests; require later algorithm-profile additions to extend this fixture before claiming complete key-format support. |
| `0.50.0` | X.509 Decoder | Decode X.509 Certificate, TBSCertificate, and SPKI under the current RFC 5280 update closure while preserving the exact original signed byte slice, enforcing current RSA, EC, X25519, and Ed25519 AlgorithmIdentifier rules, and rejecting ambiguous algorithms and id-alg-unsigned in every signature-verification context. |
| `0.51.0` | Service Identity And Extensions | Validate SAN and service identity, ASCII IDNA2008 A-label DNS inputs, wildcards, IP, internationalized and ASCII email, and URI names under RFC 9525 and the current RFC 5280 internationalization updates; enforce critical and duplicate extensions while keeping Unicode mapping and presentation caller-owned. |
| `0.52.0` | Bounded Path Construction | Build bounded deterministic paths using caller-supplied pools, loop detection, and hard depth, candidate, comparison, and work limits with no automatic network fetch. |
| `0.53.0` | Core Chain Validation | Validate chain signatures, validity, basic constraints, path length, key usage, and extended key usage. |
| `0.54.0` | Name Constraints | Validate DNS, IP, rfc822Name and SmtpUTF8Mailbox email, URI, and directory-name constraints under the current RFC 5280 internationalization updates with explicit subtree, comparison, normalization, and work budgets. |
| `0.55.0` | Certificate Policy Processing | Implement certificate policies, mappings, anyPolicy, inhibition, policy constraints, and RFC 9618 bounded policy-graph processing with signature-first validation, hard depth, node, edge, output, and work ceilings, and no exponential policy-tree construction. |
| `0.56.0` | Trust Anchors Cross-Signing And Algorithms | Define trust-anchor inputs, cross-signing and alternate-path semantics, deterministic selection, distrust policy, and per-position algorithm constraints. |
| `0.57.0` | CRL Validation | Validate base, delta, and indirect CRLs with issuer authorization, freshness, distribution-point, reason, entry, and work ceilings; for every v3 CRL-issuer certificate require a present keyUsage extension with cRLSign asserted as clarified by RFC 10007. |
| `0.57.1` | No-Revocation-Available Certificate Policy | Implement RFC 9608 noRevAvail parsing and path semantics; reject the extension on CA certificates and every contradictory CRL, Freshest CRL, OCSP AIA, or basicConstraints combination, and skip revocation only for a valid end-entity assertion under explicit caller policy. |
| `0.58.0` | OCSP Validation | Validate stapled and offline OCSP responses, responder authorization, freshness, issuer and serial matching, and RFC 9654 nonce generation, encoding, bounds, echo, mismatch, omission, and malformed-request behavior under explicit hard and soft-fail policy. |
| `0.58.1` | TLS Feature Must-Staple Enforcement | Implement RFC 7633 TLS Feature extension parsing and policy, require a valid applicable stapled OCSP response when status_request is asserted, reject unknown required feature values or unsatisfied declarations, and keep connection validity authoritative and independent of audit-event delivery. |
| `0.58.2` | Lightweight OCSP Message Profile | Implement the RFC 9919 message profile with one SHA-256 CertID request, request extension and signature policy, BasicOCSPResponse and responder-ID handling, mandatory nextUpdate freshness, nonce-to-time fallback, and signed-data-before-request ordering. |
| `0.58.3` | Lightweight OCSP Sans-I/O Transport And Cache Profile | Implement RFC 9919 AIA discovery, exact GET-at-or-below-255 and POST-above-255 selection, Base64 and URI construction, response media and length checks, cache metadata, freshness, retry, and invalidation as typed effects while keeping network and cache implementation caller-owned and signed OCSP data authoritative. |
| `0.59.0` | Versioned Certificate Transparency Contract | Implement strictly version-separated RFC 6962 CT v1 and RFC 9162 CT v2 SCT, log, signed-entry, timestamp, extension, and proof formats; define verifier ownership, log-list and operator updates, disqualification and duplicate handling, and fail closed without a verifier for every required version while never interpreting one version as the other. |
| `0.60.0` | Modern PKI Audit Gate | Complete adversarial, differential, fuzz, path-complexity, revocation, Certificate Transparency, and external audit campaigns for the modern PKI surface before historical and optional algorithm profiles are admitted. |
| `0.60.1` | Legacy And PQ PKIX Profile Schedule Freeze | Regenerate the authenticated PKIX AlgorithmIdentifier, key, certificate, CRL, OCSP, request, issuance, regional, PQ, and hybrid profile register; assign every generation and validation direction to the review-sized rows below, insert additional tagged rows for any newly authenticated independent family, and block implementation rather than enlarging a frozen row. |
| `0.60.2` | Complete Legacy RSA Hash PKIX Profiles | Implement exact PKIX AlgorithmIdentifier, key, certificate, CRL, OCSP, and path-validation profiles for authenticated MD2, MD5, and SHA-1 RSA signatures over exact v0.46 owners with strict parameters, algorithm constraints, warnings, vectors, and no modern-default trust edge. |
| `0.60.3` | Complete DSA And Legacy EC PKIX Profiles | Implement every authenticated historical DSA and legacy EC PKIX AlgorithmIdentifier, key, signature, certificate, CRL, OCSP, and path-validation profile over exact primitive owners with strict parameters, algorithm constraints, warnings, vectors, and no modern-default trust edge. |
| `0.60.4` | Complete GOST PKIX Profiles | Implement every authenticated GOST PKIX AlgorithmIdentifier, key, signature, agreement, certificate, CRL, OCSP, and path-validation profile over v0.46.44-v0.46.54 with exact parameter sets, byte orders, constraints, warnings, vectors, and explicit regional or legacy policy. |
| `0.60.5` | Complete SM2 Regional And Residual Legacy PKIX Profiles | Implement every authenticated SM2 and remaining assigned regional or historical PKIX AlgorithmIdentifier, key, signature, agreement, certificate, CRL, OCSP, and path-validation profile over exact owners with strict parameters, constraints, warnings, vectors, and explicit policy. |
| `0.60.6` | PKIX Canonical Issuance Foundation | Implement bounded public signed-object generation using the existing v0.33.9 DER writer and schema codecs, serial and extension construction, exact AlgorithmIdentifier binding, caller-owned signer, entropy, time and storage effects, transactional output, cleanup, and reusable issuance proof harnesses without duplicating DER or yet combining every object type. |
| `0.60.7` | Complete PKCS10 Certification Request APIs | Implement complete bounded PKCS #10 request generation, canonical DER, attribute and extension-request handling, signing, parsing, validation, malformed and duplicate policy, round trips, and public APIs across every admitted modern and legacy signature profile. |
| `0.60.8` | Complete Certificate Cross-Certificate And Trust-Anchor Issuance | Implement complete bounded certificate, cross-certificate, and trust-anchor-information generation and validation over v0.60.6 with exact versions, names, validity, keys, extensions, constraints, serials, signature profiles, external signer effects, canonical DER, round trips, and public APIs. |
| `0.60.9` | Complete CRL Issuance APIs | Implement complete bounded base, delta, and indirect CRL generation and validation with issuers, distribution points, entries, reasons, freshness, numbers, extensions, signature profiles, external signer effects, canonical DER, round trips, and public APIs. |
| `0.60.10` | Complete OCSP Request And Response Issuance APIs | Implement complete bounded OCSP request, BasicOCSPResponse, and response generation and validation with CertID, nonce, responder identity and authorization, freshness, status, extensions, signature profiles, external signer effects, canonical DER, round trips, and public APIs. |
| `0.60.11` | Unsigned X509 And Structural ML-KEM PKIX Profiles | Implement the authenticated unsigned X.509 profile with explicit non-authentication types and RFC 9935 ML-KEM public-key/certificate encoding and decoding, exact parameter binding, malformed/substitution rejection and public format round trips; keep ML-KEM material structurally typed and non-executable until v0.119.3 and v0.122.1, without implying a KEM is a signature. |
| `0.60.12` | Complete ML-DSA PKIX Profiles | Implement every finalized authenticated ML-DSA PKIX AlgorithmIdentifier, public-key, certificate, CRL, OCSP, request, issuance, and validation profile available to the source closure with exact parameter sets, encodings, constraints, round trips, and public APIs. |
| `0.60.13` | SLH-DSA And Structural Hybrid PKIX Profiles | Implement finalized authenticated SLH-DSA PKIX key, certificate, CRL, OCSP, request, issuance and validation profiles over the complete v0.46 owners; inventory hybrid profile authorities and structural encodings without executing unavailable component algorithms, and assign complete executable hybrid integration to v0.122.2. |
| `0.60.14` | Pre-PQ-Integration PKIX Public Acceptance | Exercise import, export, issuance, request, path construction, validation, revocation, OCSP, CT, unsigned, classical, regional, implemented PQ-signature and legacy profiles through packaged public fixtures and at least two independent toolchains where available; test ML-KEM/hybrid structural codecs without claiming their later executable operation closure, which belongs to v0.122.1-v0.122.3. |
| `0.60.15` | Pre-PQ-Integration PKI Audit And Remediation Gate | Independently audit and cleanly retest every presently executable modern, optional, regional, PQ-signature and legacy PKI profile, including generation/validation symmetry, encrypted containers, constraints, trust separation, resource ceilings and external signer effects; block premature ML-KEM or hybrid execution and require v0.122.3 before complete extended-PKIX claims. |
| `0.61.0` | Shared Recordless TLS 1.3 Handshake Boundary | Implement and freeze the upstream no_std brynja-tls13-handshake crate containing the single record-independent TLS 1.3 handshake state machine consumed by brynja-tls13 and brynja-quic-tls; brynja-tls13 owns stream records, QUIC owns transport, brynja-tls reaches it only through the version-specific engine, and DTLS may reuse codecs, transcript, certificate and key-schedule components but retains its own state machine, epochs, fragmentation, and retransmission. |
| `0.62.0` | Internal Sans-I/O Execution Contract | Define an explicitly unstable deterministic Event-to-Action driver for consumed input, output workspace, timers, entropy and time, certificate, signature and accelerator requests, application data, backpressure, resumable operations, path tokens, cancellation, and terminal states. |
| `0.63.0` | TLS 1.3 Record Protection | Implement TLS 1.3 record protection in brynja-tls13, including checked sequence exhaustion, inner content-type and padding validation, transactional state changes, and fragmentation boundaries, without performing protocol selection or exposing the evergreen router. |
| `0.64.0` | TLS 1.3 Handshake Codec | Implement the complete TLS 1.3 handshake codec with duplicate, ordering, extension-context, unknown and GREASE extension, compatibility ChangeCipherSpec, and resource rules; bound known unsupported ClientHello extensions by outer framing and ignore opaque bodies without parsing, allocation, echo, negotiation, fetching, cryptography, or state mutation while rejecting unsolicited responses. |
| `0.65.0` | Transcript And Key Schedule | Implement transcript and key-schedule states with immediate destruction of obsolete early, handshake, master, exporter, and resumption secrets. |
| `0.66.0` | ClientHello Construction And Offers | Implement bounded ClientHello construction and parsing for supported versions, groups, signature schemes, key shares, GREASE, SNI, ALPN, extension ordering, and exact original-byte preservation. |
| `0.67.0` | HelloRetryRequest And Cookies | Implement HelloRetryRequest validation, transcript message_hash transformation, selected-group rules, cookies, second-ClientHello invariants, and retry resource ceilings. |
| `0.68.0` | TLS Version Negotiation Codec And Policy | Implement shared offer and selection parsing and policy without routing into an engine: servers evaluate one ClientHello, clients evaluate one ServerHello, unknown future offered versions are skipped safely, recognized legacy versions are rejected by policy, highest-version and downgrade-sentinel rules are typed, exact transcript bytes are preserved, and application profiles can require TLS 1.3 so new protocols satisfy RFC 9852 without silently enabling TLS 1.2. |
| `0.69.0` | TLS 1.3 Authenticated Server Flight | Implement ServerHello through the authenticated server flight, certificate presentation, and the sole ALPN and SNI negotiation implementation. |
| `0.70.0` | Certificate Negotiation And Selection | Implement signature_algorithms_cert, certificate_authorities, oid_filters, certificate and public-key compatibility, bounded identity selection, and deterministic external-signer requests. |
| `0.71.0` | Stapled Status And SCT Transport | Implement status_request and stapled OCSP transport plus bounded SCT transport and handoff to admitted PKI and Certificate Transparency policies. |
| `0.72.0` | Client Authentication And Finished | Implement handshake-time client authentication, CertificateVerify, Finished, authenticated application-data transition, and the state boundary later reused by separately versioned post-handshake authentication. |
| `0.73.0` | Alerts Closure And Cancellation | Complete alerts, close-notify, illegal-message handling, backpressure, cancellation, provider failure, terminal states, and terminal secret and handle destruction. |
| `0.74.0` | Stateful Tickets And Resumption PSKs | Implement stateful cache tickets and resumption PSK binders with protocol-specific cache and identity domains, constant-work unknown-identity handling where possible, single-use pending operations, concurrency and crash-consistency contracts, external-storage secrecy, rotation, and lifetime policy. |
| `0.75.0` | Stateless Ticket Protection | Implement an optional versioned AEAD ticket envelope binding protocol version, suite, SNI, ALPN, client-authentication state, PSK and early-data policy, issue and expiry time, key identifier, rotation generation, and deployment domain with nonce uniqueness and uniform failures. |
| `0.76.0` | TLS 1.3-Profile External PSK Policy | Separate external from resumption PSKs; admit external PSKs only for TLS 1.3, DTLS 1.3, and QUIC, never for hardened TLS 1.2 or DTLS 1.2; require psk_dhe_ke, constant-work identity and binder handling, single-use pending lookups, unique per-profile and deployment provisioning for any raw PSK, and no silent psk_ke, cross-domain, binder-failure, or certificate-authentication fallback. |
| `0.76.1` | External PSK Importer And Domain Separation | Implement RFC 9258 imported identities and derived imported PSKs with protocol, KDF, context, application, ALPN, and deployment-domain separation; require the importer whenever provisioned key material could cross an admitted protocol or deployment domain, bind importer metadata into tickets and pending operations, and reject missing, ambiguous, mismatched, or reused import contexts. |
| `0.76.2` | External PSK Provisioning And Role Security | Apply RFC 9257 with a mandatory 128-bit minimum key length, typed entropy provenance, client/server role and logical-node binding, pairwise-or-imported group policy, opaque identity comparison and collision domains, peer-identifier confirmation, privacy warnings, rotation and main-key destruction; reject low-entropy PSKs while reserving exact certificate-plus-external-PSK composition for v0.82.3. |
| `0.77.0` | Zero-RTT | Implement opt-in zero-RTT with an atomic anti-replay check-and-insert contract, concurrency and crash consistency, single-use pending storage operations, freshness, deterministic rejection, secret lifetime, and application side-effect guidance. |
| `0.78.0` | TLS KeyUpdate | Implement KeyUpdate with traffic-secret transition, immediate obsolete-key destruction, request coalescing policy, and long-lived key and record limits. |
| `0.79.0` | Exporters And Modern TLS-Exporter Channel Binding | Implement the RFC 5705 exporter for TLS 1.2 and the RFC 9846 exporter for TLS 1.3, then admit RFC 9266 tls-exporter with exact label, context, transcript, and protocol-version rules; release outputs only after protocol-specific authorization as typed, non-formatting secrets with explicit ownership, use, and zeroization policy, while legacy channel bindings receive a separate v0.148.5 owner. |
| `0.80.0` | TLS 1.3 Core Suite Completion | Admit AES-128-GCM/SHA-256, AES-256-GCM/SHA-384, and ChaCha20-Poly1305/SHA-256 as secure defaults while preserving a typed suite registry for the complete standardized TLS 1.3 suite profiles added at v0.82.1. |
| `0.81.0` | TLS 1.3 Conformance And Interoperability | Pass official vectors, truncation and fragmentation matrices, independent peer implementations, state-model and fuzz gates, and provider fault injection. |
| `0.82.0` | TLS 1.3 Audit Gate | Complete an external TLS 1.3 audit and clean remediation retest. |
| `0.82.1` | Complete TLS 1.3 Cipher-Suite And Regional Profiles | Implement every authenticated standardized TLS 1.3 cipher-suite profile, including AES-128-CCM, AES-128-CCM-8 and admitted SM-family regional suites, with exact hash, HKDF, nonce, record, certificate, negotiation, limit, provider and policy binding; retain only current broadly recommended suites in defaults. |
| `0.82.2` | TLS 1.3 Post-Handshake Authentication | Implement complete client and server post-handshake authentication with transcript, request-context, certificate selection, ordering, concurrency, KeyUpdate interaction, cancellation, resumption-state, application-authorization, resource and terminal-failure semantics through explicit opt-in configuration. |
| `0.82.3` | TLS 1.3 Certificate With External PSK | Implement the complete authenticated RFC 9973 certificate-plus-external-PSK mode with exact binder, certificate, Finished, identity, role, importer, transcript, resumption, early-data, privacy, downgrade and provisioning policy; make it explicit and never substitute it for certificate-only or PSK-only authentication. |
| `0.82.4` | TLS 1.3 Legacy PKCS1 Client Signatures | Implement the authenticated RFC 9963 legacy RSASSA-PKCS1-v1_5 client CertificateVerify code points through the exact v0.46.58 RSA operations, strict signature negotiation, certificate and role binding, explicit server policy, warnings, vectors and interoperability; keep them absent from server signatures and modern defaults. |
| `0.82.5` | Complete TLS 1.3 Optional-Surface Acceptance And Audit | Exercise every core, CCM, regional, post-handshake-authentication, certificate-plus-PSK and legacy-client-signature path through public client/server fixtures, independent peers, state and resource faults, algorithm and downgrade negatives, modern-default isolation and exact legacy selection, then obtain clean independent review before TLS 1.2 integration. |
| `0.83.0` | TLS 1.2 Engine And Policy Boundary | Freeze brynja-tls12 as an engine independent from TLS 1.3 and define its explicit ECDHE-plus-AEAD policy with Extended Main Secret required and static RSA, finite-field DH, static ECDH, CBC, MD5 and SHA-1 signing, compression, renegotiation, and automatic fallback excluded. |
| `0.83.1` | Current TLS And DTLS 1.2 Deprecation Closure | Apply RFC 9155 and RFC 10015 to TLS 1.2 and the later DTLS 1.2 profile: never offer or select MD5/SHA-1 signatures, static RSA, finite-field DH, or static DH/ECDH certificate types; generate exact alerts for forbidden peer selections and prove IANA discouraged entries cannot enter configuration, negotiation, resumption, or imported state. |
| `0.83.2` | TLS 1.2 Feature-Freeze Enforcement | Apply RFC 9851 to TLS 1.2 only: reject post-freeze protocol, cipher, group, signature, extension, alert, and other registry additions unless they are an authenticated urgent-security correction or the RFC-permitted ALPN and exporter-label exceptions; keep DTLS decisions separate and prohibit PQC backports to TLS 1.2. |
| `0.84.0` | TLS 1.2 PRF And Key Block | Implement the TLS 1.2 PRF, main secret, Extended Main Secret input, key-block expansion, label compatibility, separation, and length limits. |
| `0.85.0` | TLS 1.2 Record Nonces And Protection | Implement admitted TLS 1.2 AEAD record nonces, additional data, sequence exhaustion, limits, fragmentation, and failure-atomic open. |
| `0.86.0` | TLS 1.2 Extended Main Secret Transcript Binding | Implement the RFC 9846-renamed Extended Main Secret transcript selection, wire-compatible label, session-hash rules, resumption consistency, API indication for TLS 1.3, and mandatory failure behavior. |
| `0.87.0` | TLS 1.2 Signaling And Renegotiation Semantics | Accept TLS_EMPTY_RENEGOTIATION_INFO_SCSV only as initial secure-renegotiation signaling, accept empty renegotiation_info where required, emit inappropriate_fallback for TLS_FALLBACK_SCSV only when a higher enabled version exists, apply downgrade sentinels, and reject every subsequent renegotiation attempt. |
| `0.88.0` | TLS 1.2 ECDHE State Machines | Implement isolated ECDHE_ECDSA and ECDHE_RSA TLS 1.2 client and server state machines entered only by the one-pass modern selector. |
| `0.89.0` | TLS 1.2 Suite Completion | Admit only the six ECDSA and RSA combinations over AES-128-GCM, AES-256-GCM, and ChaCha20-Poly1305. |
| `0.90.0` | TLS 1.2 Resumption And Interoperability | Complete TLS 1.2 stateful and stateless resumption, protocol-specific tickets, extension hardening, interop, and downgrade corpora. |
| `0.90.1` | TLS 1.2 RFC 6066 Extension Semantics | Implement TLS 1.2-specific SNI resumption association, status_request negotiation and CertificateStatus handling; bound and safely ignore unsupported peer ClientHello extensions without parsing their bodies, while rejecting unsolicited responses and keeping unsupported facilities absent from configuration, offers, echoes, negotiation, tickets, and imported state. |
| `0.91.0` | TLS 1.2 Audit Gate | Complete a separate TLS 1.2 external audit while retaining explicit configuration and independent disablement. |
| `0.92.0` | Integrated Evergreen One-Pass TLS Router | In brynja-tls, after brynja-tls13 and brynja-tls12 exist and pass independent audits, integrate symmetric one-pass routing: one server ClientHello or one client ServerHello selects exactly one highest acceptable offered engine, validates downgrade sentinels, transfers original transcript bytes and version-domain state once, and never retries another engine or crosses credentials, tickets, PSKs, caches, or secrets after failure; preserve an engine-registration boundary for a separately versioned future TLS generation. |
| `0.92.1` | Complete Legacy TLS 1.2 Cipher-Suite Profiles | Create separately selected `brynja-legacy-tls12` suite policy and implement every authenticated TLS 1.2 cipher-suite profile over the exact shared AES, CCM, CBC, Camellia, ARIA, SEED, SM, GOST, DES, TripleDES, RC2, RC4, IDEA and NULL compatibility primitives, with exact MAC, PRF, nonce, IV, padding, limit, downgrade and warning behavior and no modern-router edge. |
| `0.92.2` | Complete Legacy TLS 1.2 Authentication And Key Exchange | Implement complete client and server static RSA, finite-field DHE, static DH, static ECDH, ECDHE, anonymous, PSK, DHE-PSK, RSA-PSK, SRP, Kerberos, GOST, SM and every other authenticated TLS 1.2 key-exchange and authentication profile, using caller-owned external Kerberos credentials where appropriate and never hiding network or realm effects. |
| `0.92.3` | Complete Legacy TLS 1.2 Extensions Compression And Renegotiation | Implement all authenticated TLS 1.2 extension and facility semantics not present in the hardened engine, including secure renegotiation, max_fragment_length, client_certificate_url, trusted_ca_keys, truncated_hmac, status_request_v2, cached information, supplemental data, user mapping, authorization data, Heartbeat and admitted DEFLATE or LZS compression, with bounded resources and explicit dangerous policy. |
| `0.92.4` | Complete Legacy TLS 1.2 Signatures Certificates And Resumption | Bind complete SHA-1, MD5, DSA, legacy RSA, GOST, SM and historical PKIX profiles into client and server Certificate, CertificateRequest, CertificateVerify, Finished, session-ID and ticket resumption behavior, including every authenticated signature code point, exact transcript, failure, cache, cleanup and cross-version isolation rule. |
| `0.92.5` | Complete Legacy TLS 1.2 Client And Server Operations | Close every authenticated TLS 1.2 handshake, record, alert, extension, compression, resumption, renegotiation, exporter and channel-binding send and receive direction through documented `brynja-legacy-tls12` public client and server APIs; reserved and private-use values remain typed but cannot gain authority without caller policy. |
| `0.92.6` | Legacy TLS 1.2 Interoperability Isolation And Audit Gate | Interoperate across complete suite and feature families with independent and archived peers, fuzz every negotiation and record boundary, test downgrade and oracle resistance, prove separate configuration, listeners, credentials, caches and process containment, and obtain clean audit and pentest evidence without weakening modern `brynja-tls12` or the evergreen router. |
| `0.93.0` | Modern Multi-Version Routing Audit Gate | Complete client and server cross-version, downgrade, unknown-version, transcript-preservation, domain-separation, no-retry, interoperability, differential, fuzz, and external audit campaigns for the integrated TLS router. |

## Phase 3: QUIC TLS, DTLS, And Post-Quantum Work

QUIC resumption is transport-aware, DTLS early data is excluded for v1, CID behavior is version-specific, and hybrid policy is explicit.

| Version | Milestone | Exclusive scope and completion context |
| --- | --- | --- |
| `0.94.0` | QUIC Ownership And Encryption Levels | Define distinct QUIC encryption levels and secret install and discard events; consume ordered bytes supplied by QUIC and exclude packet processing, offsets, retransmission, packet numbers, loss recovery, Retry, key phase, TLS records, and TLS KeyUpdate. |
| `0.95.0` | QUIC-Specific TLS Profile | Implement the recordless QUIC TLS profile with no ChangeCipherSpec, EndOfEarlyData, TLS KeyUpdate, or record compatibility mode; enforce handshake-message legality per encryption level, TLS alert to QUIC CRYPTO_ERROR mapping, required ALPN negotiation and failure, and typed handshake and application secret events. |
| `0.96.0` | QUIC Key-Derivation Boundary | Have TLS emit typed handshake and application traffic secrets; optionally derive quic key, quic iv and quic hp in brynja-quic-tls; keep version-specific Initial salts and secrets, packet protection, Retry integrity, key phase, and quic ku in the QUIC transport; verify all admitted derivations with RFC 9001 vectors. |
| `0.97.0` | QUIC Transport Parameters | Implement bounded syntactic transport-parameter parsing and encoding for the QUIC/TLS boundary with typed values, exact received-byte transcript binding, caller-owned semantic enforcement, checked output and role/version policy; expose both directions and close them through v0.97.1 without implementing QUIC packet processing. |
| `0.97.1` | QUIC Transport-Parameter Codec Acceptance | Close the QUIC/TLS integration's public transport-parameter parsing and encoding with exact role/version-aware wire fixtures, checked lengths, duplicate handling and transcript-byte preservation while leaving transport semantics, packet protection and network I/O with the caller's QUIC stack. |
| `0.98.0` | QUIC Sans-I/O Handshake | Implement per-level TLS handshake input and output, alerts, pending providers, bounded future-level data, traffic-secret events, and deterministic rejection of late data. |
| `0.99.0` | QUIC Resumption Lifecycle | Distinguish TLS handshake completion from QUIC handshake confirmation; emit typed completion, confirmation and key-discard outcomes; deliver NewSessionTicket only after handshake completion; bind negotiated QUIC version, remembered transport parameters, ALPN, application state, and deployment domain to tickets; and enforce ticket confidentiality, lifetime, privacy, rotation, and non-reuse policy. |
| `0.99.1` | QUIC Zero-RTT Profile | Require max_early_data_size 0xffffffff for QUIC, validate remembered transport parameters and application policy before offering or accepting early data, map invalid values to the correct QUIC error, expose deterministic authoritative acceptance and rejection, preserve anti-replay and ticket single-use rules, and leave the QUIC transport in control of zero-RTT byte quantity and packet processing. |
| `0.100.0` | Optional QUIC CRYPTO Reassembly Helper | Provide an explicitly optional bounded CRYPTO-offset reassembly helper with conflict and exhaustion handling that is not used implicitly and does not implement retransmission or loss recovery. |
| `0.101.0` | QUIC Conformance And Audit | Pass RFC 9001 vectors plus loss, reorder, discard, 0-RTT, key-derivation, interoperability, ownership-boundary, and external review gates. |
| `0.102.0` | DTLS Path Identity Contract | Introduce an opaque caller-provided path token binding cookie state, amplification accounting, CID routing, migration, PMTU, timers, and datagram metadata so packets cannot transfer validation or budgets between paths. |
| `0.103.0` | DTLS Version Negotiation Codec And Policy | Implement shared DTLS offer and selection parsing and policy without routing into an engine: one ClientHello or ServerHello is evaluated, unknown future versions are skipped, recognized legacy versions are rejected, the highest configured version and downgrade policy are typed, and transcript plus opaque path identity are preserved. |
| `0.104.0` | DTLS Unified Headers And Epochs | Implement DTLS 1.3 unified headers, epochs, compact sequence reconstruction, AEAD nonce construction, and checked sequence exhaustion. |
| `0.105.0` | DTLS Record-Number Encryption | Implement record-number encryption and authenticated reconstruction-failure handling with official vectors and no replay-window mutation before authentication. |
| `0.106.0` | DTLS Replay And Epoch-Key Lifetimes | Implement fixed replay windows across epoch transitions, bounded previous and future retention, transactional key installation, and immediate obsolete-key destruction. |
| `0.107.0` | DTLS 1.2 Connection IDs | Implement RFC 9146 DTLS 1.2 connection-ID negotiation and its version-specific record construction with opaque path-token routing, privacy, replay, rebinding, migration, PMTU, and amplification invariants; do not accept DTLS 1.3 CID-update messages. |
| `0.108.0` | DTLS 1.3 Connection-ID Updates | Implement DTLS 1.3 connection IDs, NewConnectionId and RequestConnectionId post-handshake updates with bounded active and retired IDs, opaque path-token routing, collision, privacy, replay, migration, rotation, PMTU, and amplification invariants. |
| `0.109.0` | DTLS Fragmentation And Reassembly | Implement caller-owned bounded handshake fragmentation and reassembly with canonical transcript messages and overlap and conflicting-fragment rejection. |
| `0.110.0` | DTLS Flights ACKs And Timers | Implement deterministic flights, ACK processing, typed timer actions, cached retransmission, checked backoff, congestion limits, and path-token ownership. |
| `0.111.0` | DTLS Address Validation And Amplification Defense | Implement path-bound cookies, address validation, amplification budgets, deterministic PMTU policy, and cheap rejection before expensive cryptography. |
| `0.111.1` | DTLS Return Routability Check | Implement RFC 9853 negotiation and authenticated basic and enhanced return-routability checks for CID-enabled DTLS 1.2 and DTLS 1.3, including path challenge, response and drop messages, unpredictable cookies, timers, pacing, anti-amplification, PMTU probes, rebinding and voluntary migration, buffered-data suspension, nested-rebinding behavior, unknown-message handling, and exact old/new path binding. |
| `0.112.0` | DTLS 1.3 State Machines | Complete DTLS 1.3 client and server states, key updates, duplicate idempotence, terminal cleanup, and provider cancellation. |
| `0.113.0` | DTLS 1.3 Early-Data Exclusion | Reject DTLS 1.3 early data for v1: never offer or accept it, never derive or retain epoch 1 application-data keys, reject EndOfEarlyData on wire and in transcript, and test reordered or duplicated early records, address validation, amplification accounting, ticket policy, and deterministic peer failure independently from record replay. |
| `0.114.0` | Hardened DTLS 1.2 | Implement DTLS 1.2 using only the admitted TLS 1.2 ECDHE-plus-AEAD profile and isolated epoch, replay, ticket, path, and downgrade state. |
| `0.114.1` | Complete Legacy DTLS 1.2 Profiles | Implement a separately selected `brynja-legacy-dtls12` client and server engine covering every authenticated DTLS 1.2 cipher suite, signature, key exchange, certificate, PSK, compression, extension, Heartbeat, fragmentation, replay, epoch, cookie, retransmission, resumption and failure profile that applies from the complete legacy TLS 1.2 closure. |
| `0.114.2` | Legacy DTLS 1.2 Interoperability Isolation And Audit Gate | Qualify the complete legacy DTLS 1.2 engine against independent and archived peers under reordering, duplication, loss, amplification, migration, fragmentation, compression, weak-suite and oracle campaigns; prove modern DTLS policy and graphs remain unchanged and obtain clean external audit and pentest evidence. |
| `0.115.0` | Integrated One-Pass DTLS Router | After both DTLS engines exist, integrate symmetric one-pass routing: one server ClientHello or one client ServerHello enters exactly one highest acceptable offered engine, preserves transcript and opaque path state, validates downgrade policy, and never retries or crosses credentials, tickets, epochs, replay windows, CIDs, or secrets after failure. |
| `0.116.0` | DTLS Conformance And Audit | Pass loss, reorder, duplicate, fragmentation, replay, path-token, CID, version-selection, hostile-load, fuzz, interoperability, and external audit gates. |
| `0.117.0` | ML-KEM Arithmetic And Encoding | Implement ML-KEM polynomial, NTT, sampling, and canonical encoding and decoding foundations while introducing array-bound, index, reduction, and encoding round-trip proof harnesses beside the implementation. |
| `0.118.0` | ML-KEM Key Generation And Encapsulation | Implement ML-KEM-512, ML-KEM-768 and ML-KEM-1024 key generation and encapsulation with FIPS 203, errata, randomness, stack, and applicable SP 800-227 checks. |
| `0.119.0` | ML-KEM Decapsulation And Implicit Rejection | Implement constant-time ML-KEM decapsulation and implicit rejection with malformed-ciphertext, failure-path, and side-channel campaigns. |
| `0.119.1` | ML-KEM x86_64 And AArch64 Acceleration | Add isolated, benchmark-admitted x86_64 and AArch64 ML-KEM NTT, inverse-NTT, polynomial, sampling, encoding, key-generation, encapsulation, and decapsulation backends for AMD, observed-feature AWS Intel, Apple M2, and AWS Arm; preserve FIPS 203 and errata behavior, canonical encodings, randomness and stack bounds, constant-time implicit rejection, complete secret destruction, per-parameter KATs, scalar differentials, and backend-specific side-channel evidence. |
| `0.119.2` | ML-KEM RISC-V Acceleration Candidate | Add a RISC-V ML-KEM backend only for an exact ratified vector or crypto feature bundle supported by the compiler matrix, and require native qualification on the available RISC-V host when its observed ISA matches; otherwise keep it non-dispatchable with generated-code and emulator evidence, retain scalar ML-KEM support, and make the missing native acceleration evidence explicit through the final platform gate. |
| `0.119.3` | ML-KEM Public API Usability Acceptance | Close ML-KEM-512, ML-KEM-768, and ML-KEM-1024 with packaged public key-generation, encapsulation, and decapsulation fixtures using deterministic vector entropy and production caller entropy, valid and malformed ciphertexts, implicit rejection, key consistency, cleanup, no_std installation, scalar and every admitted accelerated backend, and exact FIPS-versus-unvalidated status. |
| `0.120.0` | Standard Hybrid Groups | Implement only final standardized X25519MLKEM768, SecP256r1MLKEM768, and SecP384r1MLKEM1024 encodings, component order, lengths, identifiers, and concatenated shared-secret construction under RFC 9954 and final Standards Track RFC 10024; provisional drafts and private code points never enter release artifacts. |
| `0.121.0` | Hybrid Protocol Integration | Implement explicit HybridRequired and HybridPreferred policies: Required fails if no admitted hybrid is negotiated; Preferred may select an offered admitted classical group through ordinary one-pass negotiation when the peer lacks hybrids; every selected hybrid must complete both components and partial failure never degrades to its classical component. |
| `0.122.0` | PQ Standards And Audit Gate | Complete PQ external review and standards freeze; bind the complete v0.46 ML-DSA and SLH-DSA families to every finalized authenticated TLS, DTLS, PKIX or OpenPGP mapping present in the source closure, require a separate interoperability milestone for each mapping, and classify only unavailable draft or unauthenticated future mappings as source-blocked rather than algorithmically unsupported. |
| `0.122.1` | Executable ML-KEM PKIX Integration | After complete v0.119.3 ML-KEM acceptance and the PQ standards gate, bind the earlier ML-KEM PKIX encodings to real key generation, validation, import/export and encapsulation/decapsulation consumers; exercise RFC 9935 profiles without treating a KEM as a signature or certificate trust decision. |
| `0.122.2` | Executable Hybrid PKIX Integration | Complete every authenticated hybrid PKIX key and signature profile only after all of its component algorithms and profile authorities are admitted; reuse v0.60.13 structural dispositions and exact primitive owners with distinct encoding, validation, generation and operation semantics and no inferred mapping from TLS hybrid groups. |
| `0.122.3` | Complete PQ PKIX Cross-Family Acceptance | Reconcile the earlier PKIX format and signature acceptance with the now-complete ML-KEM and every authenticated hybrid profile; require packaged generation, import/export, issuance and applicable cryptographic operations across all admitted PKIX families before FIPS architecture and final complete-PKIX claims. |

## Phase 4: FIPS Module Instantiation, Validation, And TLS Profile

Architecture is frozen before implementation; exact artifact identity is frozen only after all module components and self-tests exist. Connection failure remains distinct from the module catastrophic-failure latch.

| Version | Milestone | Exclusive scope and completion context |
| --- | --- | --- |
| `0.123.0` | FIPS 140-3 Level-One Requirements Baseline | Target an overall Security Level 1 software cryptographic module and map every applicable FIPS 140-3 and ISO/IEC 19790 security area and ISO/IEC 24759 test assertion to FIPS 140-3, SP 800-140 and 140A through 140F, the current CMVP Management Manual, current Implementation Guidance, RFG and CMVP resolutions, algorithm transitions, caveats, and lab evidence; record justified non-applicability, pin dated submission baselines, and require review of later guidance without claiming validation. |
| `0.123.1` | FIPS Module Architecture Freeze | Freeze the separately publishable brynja-fips-module boundary, dependency allowlist, approved and non-approved services, ports, roles, authentication applicability, SSP inventory, operational environments, build-reproducibility contract, and downstream brynja-fips facade and optional-module constraints without claiming or freezing an exact binary, certificate, source identity, dispatch table, dependency closure, or validation artifact. |
| `0.124.0` | SP 800-90B Entropy Source And Health Tests | Select each in-boundary, bound-module, or caller-supplied validated entropy-source construction and operational environment; define noise source, conditioning, IID or non-IID assessment, minimum entropy, startup and continuous health tests, failure handling, restart and virtualization assumptions, and complete SP 800-90B and ESV documentation without treating an arbitrary caller RNG as validated entropy. |
| `0.124.1` | Complete Hash_DRBG | Implement every selected SP 800-90A Hash_DRBG instantiation with exact instantiate, generate, reseed, uninstantiate, derivation, security-strength, personalization, additional-input, prediction-resistance, request, fork, rollback, concurrency, state-protection, zeroization, catastrophic-error, vector, and state-machine behavior. |
| `0.124.2` | Complete HMAC_DRBG | Implement every selected SP 800-90A HMAC_DRBG instantiation with exact instantiate, update, generate, reseed, uninstantiate, security-strength, personalization, additional-input, prediction-resistance, request, fork, rollback, concurrency, state-protection, zeroization, catastrophic-error, vector, and state-machine behavior. |
| `0.124.3` | Complete CTR_DRBG | Implement every selected SP 800-90A CTR_DRBG instantiation with exact block-cipher, derivation-function, instantiate, update, generate, reseed, uninstantiate, security-strength, personalization, additional-input, prediction-resistance, request, fork, rollback, concurrency, state-protection, zeroization, catastrophic-error, vector, and state-machine behavior. |
| `0.124.4` | DRBG Public API And Provider Acceptance | Exercise every selected Hash_DRBG, HMAC_DRBG, and CTR_DRBG instantiation through packaged provider fixtures covering entropy and nonce inputs, personalization, additional input, reseed, prediction resistance, request limits, fork and rollback, concurrency, state corruption, uninstantiate, cleanup, KATs, and permanent failure. |
| `0.124.5` | SP 800-90C RBG Construction | Bind only admitted SP 800-90B entropy sources to admitted SP 800-90A DRBGs through selected SP 800-90C RBG constructions; define primary and subordinate topology, entropy and nonce inputs, reseed chains, health and catastrophic propagation, operational-environment identity, and the exact RBG service boundary for ESV testing. |
| `0.125.0` | Approved Provider And Mandatory Service Indicator | Implement the sealed approved-only provider and return an unambiguous per-service approval indicator through each mandatory typed service result or ActionV1, with SecurityEvent only duplicating that status for audit; make execution and approved status require an opaque unforgeable module-owned self-test attestation that no public or application-implementable trait can create, keep that attestation unobtainable until v0.127.0, and permit no additive fips feature or construction before attested self-test success. |
| `0.126.0` | SSP Lifecycle And Zeroization Services | Define SSP entry, output, storage, high-water lifetime, external storage, accelerator handle, cache and DMA completion, and zeroization services with mandatory single-consumption completion indications; SecurityEvent may only duplicate secret-free status for audit. |
| `0.126.1` | Protected Memory Contract And Evidence Model | Add a separate `no_std` `brynja-platform-security` contract for immovable protected regions, exact control requirements, typed enforced/unavailable/not-applicable results, non-forgeable evidence, clear-before-release ordering, and explicit residuals; keep it optional, allocation-free, free of OS calls, and unable to turn register, cache, caller-copy, abort, termination, power-loss, DMA, swap, hibernation, or dump limitations into unsupported guarantees. |
| `0.126.2` | Linux And Android Protected Memory Provider | Implement an optional `brynja-platform-security-std` Linux/Android provider using only first-party Rust and documented operating-system ABIs for page-owned allocation, locking, non-dump advice, fork behavior, clear-before-unlock, and capability reporting; fail closed on quotas, permissions, unsupported kernels, containers, process-policy ambiguity, or partial effects, and introduce no native cryptographic code or third-party dependency. |
| `0.126.3` | Windows Protected Memory Provider | Implement the optional Windows provider with first-party Rust bindings to documented Windows system memory and process-security APIs, exact page ownership, locking, non-dump/crash posture evidence where observable, clear-before-unlock/free, rollback-safe failure, and honest unsupported-control results; use no native cryptographic provider or third-party dependency. |
| `0.126.4` | Apple And BSD Protected Memory Providers | Implement optional macOS, iOS, and supported BSD providers through documented platform ABIs with exact availability and entitlement handling, page-owned immovable regions, locking, non-dump/fork posture where supported, clear-before-release, and fail-closed unsupported results; preserve mobile sandbox rules and use no native cryptographic provider or third-party dependency. |
| `0.126.5` | High-Assurance Deployment Profile Acceptance | Compose the portable contract and every applicable hosted provider into an explicit opt-in high-assurance profile that requires named controls, refuses partial or stale evidence, distinguishes process-verifiable enforcement from operator-owned dump, swap/hibernation, IOMMU/DMA and physical-security duties, exercises packaged deployments on Linux, Android, Windows, macOS, iOS and BSD, and never changes ordinary `no_std`, default, independent-review, FIPS-validation, or caller-owned-copy claims. |
| `0.127.0` | Module Integrity And Pre-Operational Self-Tests | After the final DRBG, provider, SSP, and algorithm implementations are linked, implement module-owned module-integrity verification and every required algorithm, DRBG, and component pre-operational self-test over the complete final image; only that internal path may issue the opaque artifact-, environment-, generation-, and test-plan-bound attestation required by provider execution and approved status, no public or application-implementable runner can issue or substitute it, no cryptographic service or output is available before success, and deterministic fault injection covers every test and integrity path. |
| `0.127.1` | Conditional Self-Tests And Permanent Failure State | Implement required pairwise-consistency, conditional, on-demand, firmware or software load, and continuous health-test coordination; serialize concurrent test requests, destroy affected SSPs, block prohibited services, and enter one module-wide irreversible error state shared by every current or future session exactly for FIPS-defined integrity, self-test, and catastrophic failures, so constructing a sibling session can never clear or bypass failure. |
| `0.128.0` | FIPS Observational Security Event Integration | Duplicate mandatory service indicators, module-state results, SSP lifecycle token completions, and catastrophic failures into the frozen audit schema without making SecurityEvent authoritative; keep payloads and identifiers secret-free, format-safe, and non-correlating, permit optional caller timestamps and later enrichment, preserve ordering and saturating drop accounting, and prove missing or ignored events cannot alter or obscure approval, service results, latching, zeroization, destruction completion, or cryptographic state. |
| `0.129.0` | Exact FIPS Module Artifact Freeze | After the DRBG, approved provider, service indicators, SSP services, algorithms, self-tests, and module security-event integration are final and linked, instantiate and freeze the exact binary, source identity, build inputs, compiler and linker configuration, symbols, dispatch tables, dependency closure, operational-environment mappings, and reproducible artifact hashes; all ACVTS, CAVP, CMVP, and later closure evidence must name this exact artifact. |
| `0.130.0` | ACVTS And CAVP Evidence | Complete ACVTS and CAVP campaigns for every approved implementation, dispatch path, parameter set, and operational environment. |
| `0.130.1` | ESV Entropy And RBG Validation Evidence | Complete production ESVTS evidence for every claimed SP 800-90B entropy source and SP 800-90C RBG construction in each operational environment, bind validation identifiers and caveats to the exact module artifact, and reject unvalidated entropy substitutions or environment drift. |
| `0.131.0` | CMVP Security Policy And Public Documentation | Produce the SP 800-140B Rev. 1 security policy, module specification, ports and interfaces, roles, services, approved-service indicators, SSP inventory and lifecycle, finite-state model, self-tests, installation, initialization, secure-operation, zeroization, operational-environment, mitigation, and guidance documents with exact certificate-ready tables and no unsupported claim. |
| `0.131.1` | CMVP Test Evidence And Lab Submission Package | Produce the SP 800-140A and Derived Test Requirements evidence package, source-to-object and requirements trace, algorithm and entropy certificates, test environment, reproducible artifacts, vendor evidence, responses, and lab handoff package, with every datum mechanically bound to the exact frozen artifact. |
| `0.132.0` | Accredited FIPS Lab Evaluation And Findings | Submit the exact artifact and evidence to an NVLAP-accredited CST laboratory, complete applicable FIPS 140-3 and ISO/IEC 24759 testing, preserve question and evidence provenance, and record every finding without changing or claiming validation for the submitted artifact. |
| `0.132.1` | FIPS Evaluation Remediation And Clean Retest | Classify every lab or CMVP finding, remediate through a new exact artifact identity when code or build inputs change, repeat affected algorithm, entropy, regression, and module tests, update all evidence, and obtain a clean accredited-lab retest with no unresolved finding. |
| `0.132.2` | FIPS Certificate Issuance Caveat And Claim Gate | Make no FIPS 140-3 validated or Inside claim until CMVP issuance; then record the exact certificate number, module version, overall and per-area levels, tested operational environments, approved services, dependencies, caveats, status, sunset, security-policy hash, and permitted wording, and mechanically prevent claims from mismatched, unissued, revoked, or unsupported artifacts. |
| `0.133.0` | Boundary And Package Audit | Complete the final modern, legacy, experimental, and FIPS dependency-boundary, symbol, dispatch, feature, and package-content audit. |
| `0.134.0` | Approved-Only TLS Operating Profile | Implement an internal approved-only connection profile derived from the exact validated-module service manifest and current final NIST TLS, key-strength, algorithm-transition, key-generation, and key-establishment guidance; enforce admitted version, suite, group, signature, certificate, entropy, key provenance, resumption, external PSK, and zero-RTT combinations plus aggregated mandatory per-service indicators; invoking a non-approved service terminates the connection and invalidates its approved configuration claim, while the permanent module error state remains reserved for FIPS-defined integrity, self-test, and catastrophic failures. |
| `0.134.1` | Validated Module Manifest And Operational-Environment Selection | Generate a machine-readable immutable manifest for each validated brynja-fips-module artifact containing hashes, certificate identity and status snapshot, caveats, sunset, security-policy hash, approved services and parameter sets, self-test identity, CPU dispatch, build inputs, and tested operational environments; require exact target and runtime module-identity matching and fail closed without an applicable validated artifact. |
| `0.134.2` | Ergonomic Brynja FIPS Facade And Misconfiguration Gate | Implement and prepare for separate publication at the next scheduled or exceptional release checkpoint a no_std brynja-fips facade with obvious client and server constructors that require a validated-module handle and select the approved-only TLS profile, exact provider, DRBG, algorithms, strengths, certificate policy, resumption, PSK, and early-data rules from the manifest; expose only permitted choices, provide authoritative readiness and per-service results, prohibit a boolean Cargo fips feature, generic-provider injection, silent fallback, and any FIPS claim from ordinary brynja configuration, and compile-fail every mixed or incomplete configuration. |
| `0.134.3` | FIPS Deployment Claim Update And Revalidation Lifecycle | Publish install, initialize, self-test, secure-operation, zeroization, troubleshooting, certificate and caveat verification, approved-mode, indicator, and integration guidance; monitor current CMVP guidance, RFG resolutions, algorithm transitions, certificate status, sunset, CVEs, patches, and operational environments; separate immutable validated artifacts from patched unvalidated lines and require documented change impact, regression testing, revalidation scenario, incident response, rollback, and claim withdrawal. |

## Phase 5: Optional Modules, Composition, Stable Integration, Assurance, And General Availability

Receive and send compression, post-validation closure, and cross-feature composition complete before public API freeze.

| Version | Milestone | Exclusive scope and completion context |
| --- | --- | --- |
| `0.135.0` | Resumption And Anti-Replay State Rotation | Complete stateful cache, stateless ticket-key, resumption-PSK, and anti-replay generation rotation with overlap windows, bounded retention, concurrency, crash consistency, rollback detection, compromise response, transactional failure recovery, and protocol, identity, ALPN, FIPS-profile, ECH, and deployment-domain separation. |
| `0.135.1` | Identity Trust And Transparency Rotation | Complete certificate and private-key rotation, external signer and handle rollover, trust-anchor and distrust updates, noRevAvail, Must-Staple and revocation state, versioned CT log-list and operator-policy updates, ECH identity binding, atomic configuration generations, in-flight connection semantics, rollback and compromise response, and transactional failure recovery. |
| `0.136.0` | Record Size Limit | Implement Record Size Limit negotiation and enforcement with directional limits, fragmentation, buffering, peer-violation, and interoperability tests. |
| `0.137.0` | Raw Public Keys | Implement Raw Public Keys with a dedicated pinning and trust-provider contract, identity and rotation policy, negotiation, and proof that RPK never silently bypasses X.509 requirements. |
| `0.138.0` | Complete HPKE DHKEM(X25519 HKDF-SHA256) | Implement complete RFC 9180 DHKEM(X25519, HKDF-SHA256) with exact key generation, import and validation, serialization, encapsulation, decapsulation, labeled extract/expand, KEM context, domain separation, failure, cleanup, vectors, and public APIs downstream of provider ports. |
| `0.138.1` | Complete HPKE DHKEM(P-256 HKDF-SHA256) | Implement complete RFC 9180 DHKEM(P-256, HKDF-SHA256) over the admitted P-256 owner with exact SEC1 serialization, validation, encapsulation, decapsulation, labeled operations, KEM context, domain separation, failure, cleanup, vectors, and public APIs. |
| `0.138.2` | Complete HPKE DHKEM(P-384 HKDF-SHA384) | Implement complete RFC 9180 DHKEM(P-384, HKDF-SHA384) over the admitted P-384 owner with exact SEC1 serialization, validation, encapsulation, decapsulation, labeled operations, KEM context, domain separation, failure, cleanup, vectors, and public APIs. |
| `0.138.3` | Complete HPKE DHKEM(P-521 HKDF-SHA512) | Implement complete RFC 9180 DHKEM(P-521, HKDF-SHA512) over the admitted P-521 owner with exact SEC1 serialization, validation, encapsulation, decapsulation, labeled operations, KEM context, domain separation, failure, cleanup, vectors, and public APIs. |
| `0.138.4` | Complete HPKE DHKEM(X448 HKDF-SHA512) | Implement complete RFC 9180 DHKEM(X448, HKDF-SHA512) over the admitted X448 owner with exact serialization, validation and all-zero policy, encapsulation, decapsulation, labeled operations, KEM context, domain separation, failure, cleanup, vectors, and public APIs. |
| `0.138.5` | Complete HPKE KDF Identities And Labeled Operations | Implement every RFC 9180 HKDF-SHA256, HKDF-SHA384, and HKDF-SHA512 KDF identity plus exact labeled extract and expand, suite-ID, context, length and 255-block limits, secret cleanup, cross-suite rejection, vectors, and public APIs. |
| `0.138.6` | Complete HPKE AEAD Identities | Bind AES-128-GCM, AES-256-GCM, ChaCha20-Poly1305, and export-only AEAD identities with exact key, nonce, tag, sequence, limit, overlap, failure-atomicity, provider, and cross-suite rules without duplicating any underlying AEAD. |
| `0.138.7` | Complete HPKE Context Foundation | Implement the bounded RFC 9180 suite, mode, role, key-schedule context, nonce derivation, sequence, exporter, invalidation, cancellation, serialization, domain-separation, secret ownership, and provider-token foundations used by later Base, PSK, Auth, and AuthPSK modes. |
| `0.138.8` | HPKE Foundation Public API Usability Acceptance | Exercise every standard DHKEM, KDF, AEAD and export-only identity plus context creation through packaged public fixtures, official vectors, wrong-suite and malformed-key cases, sequence and length limits, cleanup, no_std, scalar and admitted backends, and exact FIPS-module separation before implementing HPKE modes. |
| `0.139.0` | HPKE Base Mode | Implement complete RFC 9180 base mode across every admitted standard KEM/KDF/AEAD combination with sequence and nonce exhaustion, seal and open failure atomicity, official vectors, and independent differential tests. |
| `0.139.1` | HPKE Secret Export And Context Lifecycle | Implement RFC 9180 Context.Export with exact exporter-context and 255*Nh output bounds, role separation, export-only AEAD policy, ordered-open and replay ownership, loss and cancellation invalidation, sequence-exhaustion closure, and immediate destruction of key, base nonce, exporter secret, and failed or discarded contexts for every mode. |
| `0.139.2` | HPKE PSK Mode | Implement RFC 9180 PSK mode with exact PSK and PSK-ID input validation, setup transcript binding, mode separation, empty-or-mismatched input rejection, caller provisioning and lifecycle rules, vectors, and no fallback to base mode. |
| `0.139.3` | HPKE Auth Mode | Implement RFC 9180 authenticated mode for every admitted DHKEM with sender static-key validation and possession, receiver authentication, exact KEM context, role and identity binding, vectors, uniform failure, and no substitution with signatures, certificates, or base-mode acceptance. |
| `0.139.4` | HPKE AuthPSK Mode | Implement RFC 9180 authenticated-PSK mode by composing both independent authentication inputs without collapsing either, binding sender key, PSK, PSK ID, info, KEM/KDF/AEAD suite and role into the exact schedule, and testing every missing, swapped, mismatched, replayed, or cross-mode input. |
| `0.139.5` | Complete HPKE Public API Usability Acceptance | Close RFC 9180 with packaged sender and receiver fixtures for Base, PSK, Auth, and AuthPSK across every standard KEM, KDF, AEAD, and export-only combination; verify official vectors, multi-record contexts, export, exhaustion, tamper, cancellation, wrong keys and PSKs, no_std package use, exact mode reporting, and scalar or admitted accelerated primitive paths. |
| `0.140.0` | ECH Origin And Downgrade Policy | Keep DNS, SVCB, HTTPS resolution, network access, and cache ownership outside protocol crates; type the intended origin, caller-asserted source and trust status, EchRequired, EchPreferred, and GreaseOnly intent, public-name exposure, retry authority, and fallback result so missing, stripped, rejected, or unusable ECH can never silently violate caller policy or establish the wrong public-SNI identity. |
| `0.140.1` | ECH Configuration Bootstrap And Cache Lifecycle | Accept bounded hostile ECHConfigList bytes from RFC 9848 bootstrap with typed provenance, origin, generation, receipt and expiry; implement RFC 9849 bounded parsing, mandatory-extension handling, version and HPKE-suite selection, public-name and key validation, GREASE inputs, stale replacement, retry-configuration precedence, cache partitioning and invalidation, and deterministic behavior for malformed, unknown, expired, or rotated configurations. |
| `0.141.0` | ECH Client Construction | Implement client inner and outer ClientHello construction, outer-extension references, AAD inputs, GREASE, padding, transcript preservation, and configuration and resource policy. |
| `0.142.0` | ECH Server Opening And Acceptance | Implement server configuration lookup, HPKE opening, inner and outer consistency checks, acceptance confirmation, identity selection, uniform rejection, and no fallback to attacker-modified state. |
| `0.143.0` | ECH HRR Retry And Rotation | Implement ECH HelloRetryRequest interaction, retry configurations, configuration rotation, second-ClientHello invariants, downgrade detection, and client and server interoperability. |
| `0.144.0` | Delegated Credentials | Implement delegated credentials as an independent optional module with authorization, lifetime, signature, selection, revocation interaction, and downgrade policy. |
| `0.145.0` | Certificate Compression Receive Provider | Treat decompression as strictly bounded hostile pre-authentication work; retain wire CompressedCertificate bytes for the transcript, pass decompressed Certificate bytes to PKI, release no identity or application data before decompression, X.509, CertificateVerify and Finished succeed, and terminate on provider error, overrun, short output, trailing compressed data, or algorithm mismatch. |
| `0.146.0` | Precompressed Certificate Artifact Validation | Validate each caller-supplied compressed server or client-authentication artifact at configuration by bounded decompression and byte comparison with the complete canonical Certificate message, including certificate_request_context and every per-certificate extension; bind exact algorithm, compressed and uncompressed lengths, identity, configuration generation, and all encoded inputs, and invalidate on any OCSP, SCT, delegated-credential, request-context, extension, chain, or RPK-versus-X.509 change. |
| `0.146.1` | Certificate Compression Send Negotiation And Transcript Integration | Advertise only locally validated compression algorithms and select only a peer-advertised algorithm with a current validated artifact; integrate server and client-authentication sends, preserve the exact CompressedCertificate wire bytes in the transcript, enforce direction and message-context legality, rotation and cancellation behavior, deterministic uncompressed fallback only when policy permits, and fail closed on missing, stale, mismatched, or over-budget artifacts. |
| `0.147.0` | Validated FIPS Closure Preservation Gate | After HPKE, ECH and every optional module exists, prove they remain downstream of validated provider ports and cannot add module symbols, dependencies, features, dispatch entries, build inputs, non-approved algorithms, or source changes; any module change invalidates prior artifact identity and validation claims and requires a new validation line. |
| `0.148.0` | Generated Optional-Feature Composition Foundation | Generate a compatibility matrix for every pair of admitted optional features and their explicit stream TLS, DTLS, QUIC, modern, regional and legacy applicability, plus targeted higher-order combinations across ECH, authentication, credentials, compression, revocation, PSKs, early data, hybrid groups, FIPS profiles, record limits, fragmentation and return routability; bind identity and ticket state exactly and make forbidden combinations unrepresentable before the remaining separately selected standardized facilities are added. |
| `0.148.1` | Complete TLS And DTLS Heartbeat Facility | Implement RFC 6520 Heartbeat request, response and peer-not-allowed handling for every applicable TLS and DTLS version with exact payload and padding bounds, response-length derivation from authenticated input, rate and amplification limits, path ownership, no secret or adjacent-memory disclosure, public opt-in APIs, interoperability and permanent regression evidence; keep it disabled by default. |
| `0.148.2` | Complete Status Request V2 And Cached Information | Implement complete RFC 6961 multi-status request and response negotiation, certificate-status association and resumption behavior plus every authenticated Cached Information type and hash, cache, mismatch, freshness and fallback rule; expose bounded opt-in configuration and exact TLS-version applicability without weakening ordinary OCSP or certificate validation. |
| `0.148.3` | Complete Supplemental User-Mapping And Authorization Data | Implement every authenticated TLS Supplemental Data, User Mapping and Authorization Data message, extension, registry, direction, ordering, criticality, resumption, privacy, resource and application-authorization effect with typed public opt-in APIs and no implicit trust or identity authority. |
| `0.148.4` | Complete RFC 6066 Legacy Extension Facilities | Implement max_fragment_length, client_certificate_url, trusted_ca_keys and truncated_hmac in their exact applicable protocol versions with fetch and trust remaining typed caller effects, bounded URLs and lists, fragment and MAC limits, resumption association, downgrade policy, public legacy configuration, and no modern default negotiation. |
| `0.148.5` | Complete Historical TLS Channel Bindings | Implement every authenticated applicable tls-unique and tls-server-end-point channel-binding profile beside tls-exporter, including renegotiation, resumption, signature-hash substitution, version, endpoint, authorization, ownership and deprecation rules; expose them only through exact legacy protocol contexts and never fabricate unavailable binding material. |
| `0.148.6` | First-Party Certificate Compression Algorithms | Bind the exact v0.46 DEFLATE, Brotli and Zstandard implementations into complete TLS certificate-compression send and receive profiles, force each algorithm and direction through public client/server APIs, preserve transcript and artifact validation, enforce bomb and workspace limits, and retain external providers only as an optional separately evidenced effect rather than the sole usable implementation. |
| `0.148.7` | Complete Optional TLS Facility Composition And Audit Gate | Regenerate the full feature matrix including Heartbeat, status_request_v2, Cached Information, supplemental data, user mapping, authorization data, every RFC 6066 facility, historical channel bindings and all certificate compressors; run pairwise and risk-selected higher-order interop, fuzz, resource, downgrade, resumption, FIPS and legacy-isolation campaigns and obtain clean independent review before facade freeze. |
| `0.149.0` | Facade Configuration Typestates | After every planned v1 optional module has exercised the internal effects model, freeze ordinary brynja typestates for exact versions, integrated one-pass routing, suites, trust, RPK, ECH, delegated credentials, compression, resources, revocation, PSK, zero-RTT, Certificate Transparency, and providers, and separately freeze brynja-fips typestates around the certificate-bound validated-module handle and closed approved-only profile; neither facade re-exports raw cryptography or admits a legacy range. |
| `0.150.0` | Versioned Stable Sans-I/O V1 API | Freeze EngineV1, EventV1, and ActionV1 with exhaustive mandatory entropy, signing, storage, timer, path-validation, OCSP transport and cache, decompression, trust, revocation-feature, external-PSK provisioning, CT-version, HPKE-context, provider, transport, service-approval, external-destruction, authentication, ECH, early-data, anti-replay, and policy results; applications cannot wildcard-ignore mandatory effects, and unhandled or mismatched effects fail closed; new mandatory effects require V2 interfaces and a major SemVer release; only bounded secret-free observational SecurityEvent values are non-exhaustive, and ignoring every such event still leaves accepted, rejected, approved, non-approved, and destruction-complete states unambiguous through mandatory state and results. |
| `0.151.0` | Caller-Provided Host Capability Integration | Keep protocol-facing contracts upstream and require caller-provided entropy and OS integration for v1; provide no built-in OS entropy FFI. Supply reviewed examples for safe std clocks, transport and storage and for caller or kernel entropy, while documenting that any future Windows, macOS, BSD, mobile, or bare-metal unsafe adapter requires its own crate, versioned unsafe and FFI milestone, audit, and platform evidence. |
| `0.151.1` | Optional Ecosystem Adapter And Dependency Boundary | Freeze separately locked downstream companion workspaces for future brynja-rustls, brynja-tokio, brynja-rustcrypto and brynja-io packages; keep all companions absent from the core workspace lockfile and every brynja facade, engine, crypto, default, all-features, legacy, bare-metal and FIPS-module edge; admit third-party Rust dependencies only inside the adapter that implements their API, with exact minimal features, freshness, advisory, license, MSRV, SBOM and native-code closure policy; and make the first-party Rust cryptography golden rule permanent and machine-enforced without implementing an adapter yet. |
| `0.151.2` | Brynja Rustls Custom Provider Adapter | Implement and prepare for separate publication brynja-rustls as an explicitly selected rustls custom provider backed completely by Brynja primitives, groups, cipher suites, signature verification, signing, secure randomness and key loading; use rustls with defaults and built-in providers disabled, never enable rustls's fips feature or resolve or delegate to AWS-LC, ring or another cryptographic provider, distinguish rustls TLS evidence from Brynja TLS evidence, and report no FIPS status from ordinary Brynja. |
| `0.151.3` | Brynja Tokio TLS Stream Adapter | Implement and prepare for separate publication brynja-tokio with connector, acceptor and TlsStream types that implement Tokio AsyncRead and AsyncWrite over the stable Brynja TLS EngineV1 contract; use caller-owned bounded buffers and minimal Tokio I/O features, preserve full-duplex progress, partial-write, wakeup, cancellation, backpressure, flush, close_notify and shutdown semantics, and forbid raw AEAD-over-stream framing, tokio-rustls and every second TLS or cryptographic implementation. |
| `0.151.4` | Rustls And Tokio Adapter Isolation And Interoperability Gate | Qualify brynja-rustls and the TLS-stream portion of brynja-tokio for dependency direction, exact lockfiles, feature allowlists, native-code absence, package archives, SBOMs, MSRV/latest-stable compatibility, cancellation, hostile-I/O behavior, independent-peer interoperability and framework upgrades; prove the main Brynja graph remains byte-for-byte independent of both, keep both outside brynja-fips-module, and permit a later adapter-level approved-operation claim only through an exact certificate-bound module handle, numbered review and applicable operational-environment evidence without enabling rustls's fips feature or changing the validated artifact. |
| `0.151.5` | Brynja RustCrypto Trait Compatibility Adapter | Implement and prepare for separate publication a separately locked `brynja-rustcrypto` adapter over the then-current stable RustCrypto digest, MAC, cipher, AEAD, signature, KEM and randomness trait packages using only exact Brynja implementations; map ordinary and hardened ownership honestly, never implement Clone, reset or serializable-state traits for hardened owners, retain caller-buffer and failure-atomic guarantees, disable alloc and default features where possible, and admit no RustCrypto algorithm implementation, native code, reverse dependency, implicit provider or FIPS claim. |
| `0.151.6` | Standard And Async Cryptographic I/O Adapters | Implement first-party `brynja-io` std Read/Write and separately locked `brynja-tokio` AsyncRead/AsyncWrite utilities for ordinary and hardened hashing, XOF output, signing/prehash and verification workflows over bounded caller-owned buffers; preserve partial I/O, cancellation, backpressure, cleanup and exact output ownership, provide file and stream conveniences without allocation in underlying cryptography, and prohibit unauthenticated plaintext streaming, raw AEAD-over-stream framing or a second cryptographic implementation. |
| `0.151.7` | Complete Ecosystem API Adapter Acceptance | Package and qualify brynja-rustls, brynja-tokio, brynja-rustcrypto and brynja-io through public downstream applications across supported Rust, OS and no_std-applicable profiles; verify exact dependency isolation, latest-compatible framework surfaces, ordinary versus hardened state semantics, hostile I/O, cancellation, cleanup, interoperability, documentation and absence from the main, legacy, bare-metal and FIPS-module graphs before final API freeze. |
| `0.152.0` | Zero-Allocation And Resource Proof | Prove the caller-owned zero-allocation profile with exact workspace sizes, non-overlapping arenas, stack ceilings, concurrency limits, and hostile-load budgets. |
| `0.153.0` | Aesynx ABI And Emulator Qualification | Make the stable Aesynx adapter contract a v1 requirement and pass an executable target-ABI or emulator harness for entropy, randomness, time, transport, storage, acceleration, boot-to-handshake, and lifecycle behavior; allow real-hardware qualification after v1 without weakening the contract. |
| `0.154.0` | Protocol State And Resource Formal Harnesses | Complete Kani or equivalent harnesses for cursors, lengths, state reachability, exhaustion, replay, transactional transitions, one-pass selectors, secret-release invariants, zeroization and obsolete-key transitions, X.509 path-work and policy-graph ceilings, DTLS return-routability path binding, HPKE context invalidation, and single-consumption pending-operation tokens using pinned external tools. |
| `0.155.0` | Cryptographic Formal Coverage And Residual-Gap Gate | Complete and audit the proof harnesses introduced with every arithmetic and cryptographic milestone: use symbolic full-width proofs where tractable, limb-count-parameterized proofs where sound, and reduced-width exhaustive models only to validate algorithms and harness structure; treat production-width official vectors and at least two independent external differential processes as evidence rather than proof; publish a machine-readable claim register mapping every primitive, implementation symbol, proven property, supported width or parameter, verification method, and residual gap without claiming reduced-to-production-width equivalence. |
| `0.156.0` | External-Process Fuzz And Differential Campaign | Do not use cargo-fuzz or libfuzzer-sys; drive first-party corpus and stdin harness binaries with pinned external process-level mutation and instrumentation, deterministic replay, differential corpora, and crash minimization without third-party repository crates. |
| `0.157.0` | Memory And Side-Channel Evidence | Complete Miri and sanitizer evidence plus compiler, target and CPU-backend constant-time assembly, owned-region zeroization-store survival, cache and branch, statistical side-channel, spill and register-residual matrices; test every forced scalar and accelerated path without extending owned-memory destruction claims to registers or OS context state; complete a resumable exact-source local/SSH evidence manager, isolated parallel Miri shards, authenticated retrieval, fail-closed bundle validation and complete-inventory aggregation so long public-checkpoint runs may use headless workers without weakening the all-groups requirement. |
| `0.158.0` | Sustained Platform And Hostile-Load Qualification | Sustain Linux, Windows, macOS, BSD, Android, iOS, bare-metal, and Aesynx ABI or emulator qualification under concurrency, provider failure, resource exhaustion, and hostile load; sustain native AMD x86_64, observed-feature AWS Intel x86_64, Apple M2, AWS AArch64, and qualifying RISC-V scalar and admitted acceleration lanes with explicit candidate status where hardware is absent; separately qualify every claimed FIPS artifact only on its certificate-listed operational environments and exact dispatch paths. |
| `0.159.0` | Consolidated External Audits | Complete exact-commit external standards-closure and normative-traceability, crypto, PKI, TLS, DTLS, QUIC, PQ, FIPS boundary, entropy, self-test, manifest, facade, profile, deployment and claim lifecycle, optional-module, zeroization, and systems-integration audits. |
| `0.160.0` | Audit Remediation And Clean Retest | Remediate every admitted finding, add permanent regressions, and obtain clean independent retests with no unresolved critical or high findings. |
| `0.161.0` | Public API Requirements And Documentation Freeze | Freeze public APIs, features, package inventory, current and compatibility source closure, normative-requirement and protocol-surface ledgers, admitted algorithms and extensions, migration guidance, deployment profiles, incident procedures, limitations, non-goals, and exact FIPS certificate, manifest, operational-environment, caveat, claim, update, and revalidation documentation. |
| `0.162.0` | Clean-Room Release Rehearsal | Pass reproducible clean-room builds, package installation, artifact comparison, rollback, key-compromise, incident, and disaster-recovery exercises. |

## Phase 6: OpenPGP, Final Integration, And General Availability

RFC 9580 is admitted as an independent modern protocol family. It reuses exact
reviewed primitive crates but never TLS state, PKIX trust, implicit platform
effects, deprecated algorithm fallback, or a FIPS-approved-service claim.

| `0.163.0` | OpenPGP Authority, Registry, Threat Model, And Package Boundary | Pin RFC 9580, its obsoleted lineage, errata, and the OpenPGP Parameters registry; classify every packet, version, algorithm, signature type, subpacket, armor form, criticality rule, compatibility surface and trust responsibility; freeze separate package boundaries with no implicit external effects; and before v0.163.1 replace any later OpenPGP umbrella row that combines independent packet families, algorithms, directions, pipelines, or acceptance work with explicit review-sized tagged child rows. |
| `0.163.1` | OpenPGP Sans-I/O Resource And Effect Model | Define allocation-free caller-owned packet, message, certificate, keyring, compression, plaintext and output arenas; bound nesting, lengths, partial-body sequences, packet counts, decompressed output, recipients, signatures, keys, work and diagnostic detail; model entropy, time, key lookup, external signing, persistence and output as resumable typed effects with transactional failure and no unauthenticated plaintext release. |
| `0.164.0` | OpenPGP Packet Header And Length Codec | Implement old- and new-format packet headers, one-, two- and five-octet lengths, bounded partial-body lengths and concatenation, exact consumption, truncation-at-every-byte, canonical writer policy and preservation of authenticated bytes without allocating or interpreting packet bodies. |
| `0.164.1` | OpenPGP Packet Values, Unknowns, And Criticality | Implement typed packet tags, versions, algorithm identifiers, signature types, subpacket headers and registries with exhaustive modern, explicit legacy-only, rejected, safely ignored, critical-failure, private-use and experimental dispositions; retain unknown values without stringly dispatch or accidental capability admission, while every authenticated assigned capability remains bound to its complete later implementation owner. |
| `0.164.2` | OpenPGP Packet Body Semantic Closure | Implement or explicitly disposition every RFC 9580 packet body and version not owned by a later cryptographic engine, including Marker, Trust, User ID, User Attribute and Image, MDC and Padding packets; preserve bounded opaque and private-use values where forward compatibility permits, prevent Trust or Marker data from gaining authority, and machine-check that every registered packet/version has an implementation, later owner, safe-ignore rule or normative rejection. |
| `0.165.0` | OpenPGP Wire And ASCII Armor Conformance Gate | Implement RFC 9580 ASCII Armor and CRC-24 policy through the admitted Base64 boundary, including multi-block, label, header, line, whitespace, checksum and trailing-data rules; differentially test packet framing and armor against independent implementations and pentest the cumulative v0.160.0-through-v0.165.0 delta before publication. |
| `0.166.0` | OpenPGP Key Material, Fingerprints, And Key IDs | Implement bounded v4 and v6 public- and secret-key packet fields, algorithm-specific material framing, exact v4 fingerprint preimages, executable v6 fingerprints and both key-ID derivation rules, collision-aware lookup domains, exact-byte retention and uniform malformed-key rejection; defer the v4 SHA-1 consumer review to v0.169.2 and fingerprint execution to v0.169.3, and do not yet authorize certificate validity or private-key use. |
| `0.166.1` | OpenPGP Signature Packets, Subpackets, And Hashing | Implement v4 and v6 signature packet framing, hashed and unhashed subpacket areas, required v6 salt and literal metadata, issuer and fingerprint rules, critical-subpacket handling, canonical hash preimages and trailers, and explicit SHA-256, SHA-384 and SHA-512 selection through existing first-party hash packages. |
| `0.166.2` | OpenPGP Signature-Subpacket Semantic Closure | Implement exact length, placement, multiplicity, authentication-domain and authority semantics for every RFC 9580 signature subpacket, including all preferred-algorithm sets, Features, Key Flags, revocation fields, Intended Recipient Fingerprint, Signature Target, Embedded Signature, Notation Data, Policy URI, Regular Expression and private or unknown values; require safe defaults to mandatory algorithms, preserve forward compatibility without trusting unhashed data, and prove every registry value has a tested disposition. |
| `0.167.0` | OpenPGP Certificates, Bindings, Revocation, And Validity | Build bounded transferable public-key and secret-key certificate models; validate direct-key, user-ID, user-attribute and subkey bindings, designated revokers, revocations, expirations, primary-key rules, cross-certification and capability flags while separating structural and cryptographic validity from application identity trust and Web-of-Trust policy. |
| `0.167.1` | OpenPGP Secret-Key Protection Envelope | Implement secret-key packet protection framing, checksum and AEAD integrity states, protected-material lifetime, locked and unlocked representations, uniform password and corruption failures, caller-owned workspaces and immediate destruction; prohibit implicit clear secret export, defer every S2K or cipher use to admitted providers, and reserve explicit protected/unprotected key export for v0.176.2. |
| `0.167.2` | Complete Reusable BLAKE2b | Implement the complete RFC 7693 sequential BLAKE2b byte-oriented hash and keyed mode in a reusable first-party no_std brynja-hash-blake2 leaf before Argon2, with all admitted digest/key lengths, one-shot and streaming APIs, exact counters, final-block behavior and official/independent vectors. |
| `0.167.3` | Hardened Unkeyed BLAKE2b Profiles | Complete secret-bearing unkeyed BLAKE2b ownership over the same compression implementation for Argon2 password-derived preprocessing, with sealed states, typed secret outputs, exact variable digest lengths and complete internal cleanup independent of optional adapters. |
| `0.167.4` | BLAKE2b Portable Public Acceptance | Freeze a package-external fixture for sequential BLAKE2b ordinary, keyed and hardened unkeyed profiles with all digest/key lengths, real streamed data, exact independent outputs, typed output and cleanup; make the accepted owner the sole BLAKE2b dependency of Argon2. |
| `0.167.5` | BLAKE2b Backend And Final Acceptance | Qualify optional first-party BLAKE2b acceleration only after portable acceptance, replay the unchanged fixture through portable and every admitted backend, and close the exact sequential BLAKE2b profiles before Argon2 uses them; retain explicit scalar-only or unadmitted dispositions where hardware or cleanup evidence is unavailable. |
| `0.167.6` | Complete Reusable AES-CMAC | Implement standalone AES-CMAC generation and constant-time verification over all admitted AES key widths with exact subkey derivation, complete/partial/empty final blocks, tag-length policy, byte and canonical arbitrary-bit one-shot and streaming APIs and hardened cleanup before EAX composes its domain-separated OMAC operations. |
| `0.167.7` | AES-CMAC Public And Backend Acceptance | Close AES-CMAC generation and verification through packaged public fixtures and every admitted underlying AES backend, preserving constant-time verification, exact tag policy and complete state cleanup; require EAX to reuse this accepted owner. |
| `0.168.0` | Complete First-Party Argon2i | Implement complete RFC 9106 Argon2i version 0x13 in first-party Rust using only the accepted hardened v0.167.5 BLAKE2b owner, including exact H-prime variable-length hashing, the shared block and indexing foundation, lanes, segments, passes, memory rounding, caller workspace, bounds, KATs, proofs, data-independent access claims, cancellation, cleanup, and public APIs. |
| `0.168.1` | Complete First-Party Argon2d | Implement complete RFC 9106 Argon2d version 0x13 over the reviewed Argon2 foundation with distinct type and indexing mode, all lanes, segments, passes, memory and output rules, caller workspace, bounds, KATs, proofs, data-dependent access disclosure, cancellation, cleanup, and public APIs. |
| `0.168.2` | Complete First-Party Argon2id | Implement complete RFC 9106 Argon2id version 0x13 over the reviewed Argon2 foundation with distinct hybrid indexing, all lanes, segments, passes, memory and output rules, caller workspace, bounds, KATs, proofs, cancellation, cleanup, and public APIs suitable for the later OpenPGP profile. |
| `0.168.3` | Argon2 CPU Acceleration And Memory-Hardness Evidence | Benchmark and admit architecture-specific Argon2 block-processing paths only when Argon2d/i/id indexing, memory fill order, lane synchronization, cancellation, cleanup, memory-hardness, side-channel disclosures, output, and scalar equivalence remain exact; retain scalar-only decisions where hardware paths do not justify their complexity. |
| `0.168.4` | Argon2 Public API Usability Acceptance | Exercise Argon2d, Argon2i, and Argon2id through packaged public fixtures for passwords, salts, secrets, associated data, variable outputs, every parameter boundary, caller workspaces, cancellation, cleanup, KATs, differentials, no_std, scalar, and every admitted backend without yet binding OpenPGP. |
| `0.168.5` | Complete OpenPGP S2K Profiles | Bind only the exact RFC 9580 Argon2 S2K profile to v0.168.2 and implement Simple, Salted, Iterated, and Argon2 S2K parsing and derivation with exact algorithm-use separation, deprecated-read policy, hostile-parameter rejection, cleanup, vectors, public APIs, and no password-dependent diagnostics. |
| `0.168.6` | Complete First-Party OCB3 | Implement complete first-party OCB3 over every admitted AES width with exact nonce, tag, AAD, empty-input, streaming, in-place/disjoint, length, key-use, failure-atomicity, verification, cleanup, vector, differential, proof, and standalone public API behavior. |
| `0.168.7` | Complete First-Party EAX | Implement complete first-party EAX over every admitted AES width using the accepted v0.167.7 AES-CMAC owner for exact domain-separated OMAC processing, with nonce, tag, AAD, empty-input, streaming, in-place/disjoint, length, key-use, failure-atomicity, verification, cleanup, vector, differential, proof, and standalone public seal/open APIs. |
| `0.168.8` | OpenPGP OCB EAX And GCM AEAD Profiles | Bind the complete OCB3, EAX, and existing GCM constructions to exact RFC 9580 identifiers, nonces, associated data, chunk indices, final tags, message and key limits, transactional output, uniform failure, and selection policy while keeping all OpenPGP profiles outside approved-service claims. |
| `0.168.9` | OCB3 And EAX Public API Usability Acceptance | Exercise packaged standalone OCB3 and EAX APIs against authoritative vectors across every admitted AES width, nonce, tag, AAD, empty, partial, multi-block, in-place, disjoint, tamper, cleanup, scalar and accelerated AES path, no_std, and failure-atomicity case. |
| `0.168.10` | OpenPGP S2K And AEAD Usability Acceptance | Exercise packaged Simple, Salted, Iterated, and Argon2 S2Ks plus OpenPGP OCB, EAX, and GCM profiles against authoritative vectors and real multi-chunk messages, hostile parameters, wrong passwords, tamper, truncation, cleanup, package isolation, and no unauthenticated plaintext release. |
| `0.169.0` | OpenPGP Ed25519 X25519 Ed448 And X448 Profiles | Bind RFC 9580 Ed25519 signature, X25519 encryption, Ed448 signature, and X448 encryption requirements solely to the already complete first-party RFC 8032 and RFC 7748 symbols; validate exact OpenPGP encodings, mode selection, subgroup and low-order rules, ephemeral-key lifecycle, KDF context and signature formatting without private curve copies or generic cross-protocol key reuse. |
| `0.169.1` | OpenPGP Complete Compatibility Algorithm Register | Assign every authenticated OpenPGP public-key, symmetric, AEAD, hash, compression and S2K algorithm plus every defined operation and parameter to one complete modern or `brynja-openpgp-legacy` owner; distinguish secure defaults from explicit dangerous compatibility without using age or weakness as a reason to omit generation, signing, encryption, import or export, and reject only reserved, unassigned, private-use-without-authority, standard-forbidden or source-blocked values. |
| `0.169.2` | OpenPGP Legacy SHA-1 Consumer Admission Review | Re-audit the exact `brynja-legacy-sha1` implementation completed at v0.24.18 for RFC 9580 v4 fingerprint, protected-key, and v1 SEIPD/MDC use; freeze separate consumer identities, collision-risk policy, input domains, cleanup, dependency direction, package warnings, and proof that no modern OpenPGP, facade, default, TLS, PKIX, password, MAC, or FIPS edge is introduced. |
| `0.169.3` | OpenPGP V4 Fingerprint SHA-1 Integration | Admit the first reviewed `brynja-openpgp-legacy` SHA-1 consumer solely for RFC 9580 v4 fingerprint and key-ID derivation; bind exact key-packet preimages, require full-fingerprint collision-aware comparisons, prohibit SHA-1 signatures and generation, and reserve protected-key and v1 SEIPD consumers for v0.169.5 and v0.171.2 without reimplementing SHA-1. |
| `0.169.4` | Complete First-Party AES Key Wrap And OpenPGP Curve Wrapping Profiles | Implement complete RFC 3394 AES-128, AES-192, and AES-256 key wrap and unwrap in first-party Rust with every valid input length, caller-owned buffers, exact integrity checks, uniform failures, zeroization, authoritative vectors and public usability; bind the exact RFC 9580 X25519 and X448 HKDF inputs, labels, output sizes and wrapped-session-key formats, and provide the separately typed ECDH wrapping primitive needed by admitted v4 compatibility profiles without generic cross-protocol key reuse. |
| `0.169.5` | OpenPGP CFB And Protected Secret-Key Compatibility | Implement first-party OpenPGP conventional CFB over admitted AES widths and the exact version 4 protected-secret-key usage-254 SHA-1 and usage-255 checksum formats inside `brynja-openpgp-legacy`; admit those narrowly defined additional `brynja-legacy-sha1` and checksum consumers, require verify-before-release, uniform password/corruption failures and immediate secret destruction, and prohibit weak S2K hashes, obsolete ciphers, automatic fallback or any modern/FIPS edge. |
| `0.169.6` | OpenPGP Legacy RSA PKCS1 V1.5 Profiles | Bind the exact complete v0.46.58-v0.46.59 RSA PKCS1 v1.5 signing, verification, encryption and decryption operations into every authenticated OpenPGP RSA compatibility profile with exact digest, session-key encoding, modulus, message, randomness, fingerprint, key-version, failure and policy rules; do not reimplement RSA or admit implicit modern selection. |
| `0.169.7` | OpenPGP Strong V4 Public-Key Compatibility Profiles | Bind the complete isolated RSA PKCS1 v1.5 operations and existing Ed25519Legacy, Curve25519Legacy and admitted ECDSA/ECDH curves into explicitly selected strong-algorithm v4 compatibility profiles in `brynja-openpgp-legacy`; permit existing-key signing or encryption only through conspicuous compatibility policy, never generate deprecated key forms, and give every omitted optional or obsolete algorithm an explicit tested rejection. |
| `0.169.8` | OpenPGP Legacy Primitive Usability And Isolation Acceptance | Exercise complete AES key wrap, OpenPGP CFB, SHA-1 consumers, RSA PKCS1 v1.5 signing/encryption/decryption and every admitted strong-v4 profile through packaged public compatibility fixtures and archived interoperability cases, while proving uniform failures, no unauthenticated plaintext, complete cleanup, conspicuous policy, and absence from every modern, default, facade, TLS, PKIX, FIPS and implicit-negotiation graph. |
| `0.169.9` | Complete OpenPGP DSA Profiles | Bind the exact v0.46.6-v0.46.7 DSA implementation into every authenticated OpenPGP key, signature, certificate, import, export, generation, signing and verification profile with exact parameter, hash, encoding, fingerprint and key-version rules, explicit dangerous policy, archived vectors and no modern-default edge. |
| `0.169.10` | Complete OpenPGP ElGamal Profiles | Bind the exact v0.46.8-v0.46.9 ElGamal implementation into every authenticated OpenPGP key, session-key encryption, decryption, import, export and generation profile with exact encoding, randomness, subgroup, fingerprint, version, uniform-failure and plaintext-release rules and explicit dangerous policy. |
| `0.169.11` | Complete OpenPGP Legacy Symmetric Profiles | Bind complete TripleDES, IDEA, CAST5, Blowfish, Twofish, Camellia and every other authenticated historical OpenPGP block cipher to exact CFB, S2K, secret-key, SKESK, SEIPD and applicable AEAD profiles, covering key generation, encryption, decryption, import, export, limits, checksums, MDCs, warnings and no implicit fallback. |
| `0.169.12` | Complete OpenPGP Legacy Hash And S2K Profiles | Bind complete RIPEMD-160, SHA-1, MD5 and every other authenticated historical OpenPGP digest to exact signature, fingerprint, S2K, MDC or checksum consumers; implement all defined Simple, Salted, Iterated and private-or-experimental-with-authority parameter semantics, generation and verification directions, collision-aware policy, cleanup and exact algorithm negotiation. |
| `0.169.13` | Complete OpenPGP V3 V4 And Historical Key Operations | Implement every authenticated historical OpenPGP key and signature version, packet operation, key-ID and fingerprint rule, key generation, import, export, binding, revocation, expiration, signing, verification, encryption and decryption direction that remains representable under the pinned source closure, isolated from modern v6 defaults. |
| `0.169.14` | OpenPGP Dangerous Compatibility API And Policy | Provide documented public compatibility constructors requiring explicit algorithm and dangerous-operation policy for weak key generation, signing and encryption while retaining safe parse, verify and decrypt entry points, hard warnings, no automatic preference or fallback, exact capability reporting and compile-time separation from `brynja-openpgp`. |
| `0.169.15` | Complete OpenPGP Legacy Algorithm Acceptance Gate | Exercise every authenticated legacy algorithm, key version, operation direction, S2K, cipher, digest, signature, encryption and packet profile through packaged public fixtures and archived independent implementations; prove exact single-owner reuse, deterministic failure, bounded resources, cleanup, modern/default/FIPS isolation and no registry entry silently reduced to recognition-only rejection. |
| `0.170.0` | OpenPGP Key And Cryptography Audit Gate | Independently audit OpenPGP key parsing, fingerprints, certificate validity, signature hashing, secret-key protection, S2K, Argon2, OCB, EAX, RFC 3394 key wrap, modern curve profiles, OpenPGP CFB, protected-v4-key handling, strong v4 public-key profiles, compatibility isolation, constant-time behavior, zeroization and provider composition; remediate all admitted findings before message encryption or signature execution. |
| `0.171.0` | OpenPGP Session-Key Packets And Recipient Selection | Implement v3 and v6 Public-Key and Symmetric-Key Encrypted Session Key packets, hidden recipients, checksums, KDF and wrapping rules, bounded multi-key lookup, deterministic preferences, exact algorithm matching and uniform recipient, password, unwrap and unsupported-algorithm failures without releasing a session key early. |
| `0.171.1` | OpenPGP V2 SEIPD Chunked AEAD | Implement version 2 Symmetrically Encrypted Integrity Protected Data with RFC 9580 chunk sizing, nonce construction, associated data, chunk and final tags, checked indices and lengths, bounded streaming, truncation and reordering rejection, failure-atomic state, and withholding of every plaintext byte until its authentication decision is authoritative. |
| `0.171.2` | OpenPGP V1 SEIPD And MDC Compatibility | Implement RFC 9580 version 1 SEIPD decryption with OpenPGP CFB prefix handling, quick-check oracle resistance, exact MDC construction and constant-time SHA-1 verification entirely inside `brynja-openpgp-legacy`; allow AES-only generation solely through an explicit compatibility operation for recipients that cannot consume v2 SEIPD, never as automatic fallback, and withhold the complete plaintext until MDC, packet grammar and final-length validation succeed. |
| `0.172.0` | OpenPGP Signed, One-Pass, And Detached Messages | Implement binary and canonical-text document signatures, standalone and timestamp signatures, one-pass nesting, detached signatures, multi-signature ordering, exact signed-data construction, time and policy effects, and authoritative mandatory verification outcomes that cannot be replaced by informational events. |
| `0.172.1` | OpenPGP Cleartext Signature Framework | Implement dash-escaping, canonical text conversion, Hash headers, whitespace and line-ending rules, armor integration, streaming verification and generation, multiple-signature policy, ambiguity rejection and exact recovered-text boundaries without conflating display normalization with authenticated content. |
| `0.173.0` | OpenPGP Literal And Compressed Packet Sequences | Implement bounded literal-data metadata and recursive message grammar plus Uncompressed, ZIP and ZLIB compression identifiers, nesting and content policies; require streaming output ceilings, expansion-ratio and total-work budgets before a decompressor can be selected. |
| `0.173.1` | Complete OpenPGP DEFLATE ZIP And ZLIB Profiles | Bind the exact complete v0.46.71-v0.46.72 DEFLATE and ZLIB implementations into OpenPGP ZIP and ZLIB Compressed Data generation and parsing with exact algorithm identifiers, reset, nesting, deterministic-generation, workspace, bomb, checksum, malformed-stream and public API behavior without a private codec copy. |
| `0.173.2` | Complete OpenPGP BZip2 Profiles | Bind the exact complete v0.46.79-v0.46.80 BZip2 decoder and encoder into optional `brynja-openpgp-legacy` Compressed Data generation and parsing with exact identifiers, nesting, block and stream checks, caller workspaces, bomb defenses, archived interoperability, public APIs and no default modern edge. |
| `0.174.0` | OpenPGP Encrypt, Decrypt, Sign, And Verify Pipelines | Compose packet, armor, compression, key, signature and encryption components into allocation-free Sans-I/O APIs for encryption, decryption, signing and verification; enforce verify-before-release, exact operation tokens, no implicit algorithm fallback, rollback-safe outputs, cancellation and terminal secret destruction. |
| `0.174.1` | OpenPGP Multi-Recipient And Preference Negotiation | Implement bounded multi-recipient encryption, shared-session-key handling, algorithm and feature preference intersection, anonymous-recipient policy, deterministic conflict resolution, downgrade rejection and constant-work recipient search where feasible; expose residual recipient-privacy and traffic-analysis limits explicitly. |
| `0.175.0` | OpenPGP Message Conformance And Audit Gate | Complete RFC 9580 vectors, generated edge cases, malformed and adversarial corpora, independent differential tests and interoperability for armored, signed, compressed, password-encrypted, public-key-encrypted and multi-recipient messages across v2 SEIPD and the isolated v1 SEIPD/MDC compatibility path; audit no-plaintext-release and cumulative resource guarantees before checkpoint publication. |
| `0.176.0` | OpenPGP Keyring Import, Merge, And Trust Boundary | Implement transactional bounded certificate and keyring import, duplicate and conflict handling, self-signature selection, update and revocation merge, rollback resistance, provenance and application policy hooks; pair import with v0.176.2 explicit export while keeping keyserver retrieval, WKD, DNS, TOFU and Web-of-Trust decisions caller-owned and impossible to activate implicitly. |
| `0.176.1` | OpenPGP Key Generation, Rotation, Expiration, And Revocation | Implement modern v6 key and subkey generation, binding, expiration, designated usage, rotation, revocation certificate creation and recovery workflows with caller entropy, time and storage effects, safe defaults, deterministic failure recovery and immediate obsolete-secret destruction; do not generate v4 or deprecated algorithms by default. |
| `0.176.2` | OpenPGP Public And Secret-Key Export | Implement explicit public-certificate, secret-key and keyring export paired with existing import and merge APIs, including binary and armored output, protected-key export and a separately authorized typed-secret unprotected export profile; preserve exact authenticated packets, modern/legacy policy and caller-owned storage. |
| `0.177.0` | OpenPGP External-Key And Sanitization Integration | Integrate opaque external signing and decryption handles, pending-operation tokens, cancellation and irreversible completion with OpenPGP workflows; optionally bridge caller-owned secret storage through `brynja-sanitization` without making it a protocol dependency or weakening the native destruction contract. |
| `0.177.1` | OpenPGP no_std Resource And Hostile-Input Qualification | Prove allocation-free operation on supported bare-metal and OS-less targets with exact workspace sizing, stack ceilings, maximum packet, nesting, recipient, signature, certificate, S2K and decompression work; exercise truncation, mutation, bombs, exhaustion, cancellation and provider failure without panic, partial output or secret leakage. |
| `0.178.0` | OpenPGP External Interoperability Campaign | Interoperate through process-isolated harnesses with at least two independent current OpenPGP implementations across modern v6 keys, armor, signatures, encryption, compression, password protection, multi-recipient messages and negative cases; pin tool versions and retain exact transcripts without adding their libraries to Cargo graphs. |
| `0.178.1` | OpenPGP V4 Compatibility And Legacy Isolation Gate | Qualify strong-algorithm v4 certificates, protected secret keys, public-key profiles, v1 SEIPD/MDC messages and BZip2 in both standardized directions; verify every admitted deprecated operation against archived corpora and independently generated artifacts, prove modern generation never emits deprecated forms without an explicit legacy API, and prove `brynja-openpgp-legacy` and optional compression compatibility are absent from the `brynja` facade, defaults, all modern protocol graphs and FIPS artifacts. |
| `0.178.2` | OpenPGP Downstream Client-Foundation Gate | Build and test independent downstream no_std and hosted client fixtures that use only published Brynja APIs to import and export keys, generate and revoke modern keys, encrypt and decrypt public-key and passphrase messages, sign and verify embedded, detached and cleartext content, process modern and admitted v4 compatibility data, and recover from bounded failures; document that UI, persistence, networking, key discovery, identity trust and PGP/MIME remain application-owned rather than hidden Brynja effects. |
| `0.179.0` | OpenPGP Public API And Documentation Freeze | Freeze OpenPGP package APIs, feature graph, complete assigned algorithm ownership, reserved/private/source-blocked dispositions, modern and compatibility profiles, resource formulas, trust boundary, key lifecycle, error semantics, security guidance, interoperability limits, verification status and migration policy without claiming independent verification or FIPS validation. |
| `0.179.1` | OpenPGP Formal, Fuzz, Memory, And Side-Channel Evidence | Complete applicable Kani proofs, first-party external-process fuzzing, Miri and sanitizer runs, differential corpora, constant-time and zeroization emitted-code review, statistical timing tests and hostile-load campaigns for every OpenPGP package, provider path and supported target; publish precise proof and residual-gap claims. |
| `0.179.2` | Independent OpenPGP Standards And Cryptography Audit | Obtain an exact-commit independent review of RFC 9580 requirement closure, packet and subpacket dispositions, registry decisions, message grammars, cryptographic composition, key wrap, key validity, trust separation, protected-key and v1 SEIPD compatibility, strong v4 profiles, Base64-ng and compression boundaries, downstream client completeness, resource limits, plaintext release, constant-time behavior, zeroization and documentation claims. |
| `0.179.3` | OpenPGP Audit Remediation And Clean Retest | Remediate every OpenPGP audit finding, add permanent regressions, update requirement and claim registers, repeat affected interoperability and side-channel campaigns, and obtain a clean independent retest with no unresolved critical or high finding. |
| `0.180.0` | OpenPGP Production-Readiness Gate | Freeze the independently reviewed OpenPGP artifact set and package publication closure, complete cumulative pentest from v0.175.0 through v0.180.0, prove installability and modern/legacy/FIPS isolation, and publish only claims linked to exact standards, test and audit evidence. |
| `0.180.1` | Named Legacy Protocol Authority And Completeness Register | Authenticate and lawfully admit the complete available specifications, errata, registries, archives and interoperability corpora for TLS 1.1, TLS 1.0, DTLS 1.0, SSL 3.0, SSL 2.0, WTLS, PCT and SNP; assign every message, version, suite, primitive, compression method, extension, certificate, key format, role and send/receive operation to one complete owner; insert review-sized tagged child rows whenever a listed protocol row combines independent codecs, state-machine roles, suites, operations, or acceptance work; and block 1.0 rather than silently ship a subset. |
| `0.180.2` | Complete TLS 1.1 Codec And State Machines | Implement the complete bounded TLS 1.1 record, handshake, alert, extension, compression, renegotiation, resumption and shutdown codecs and client/server state machines in `brynja-legacy-tls11`, with exact transcripts, versions, errors, resources, cancellation and no modern routing or fallback. |
| `0.180.3` | Complete TLS 1.1 Suites Credentials And Operations | Bind every authenticated TLS 1.1 cipher suite, MAC, PRF, key exchange, signature, certificate, PSK or external-credential profile and every generation, import, export, send and receive direction to the exact shared primitive owners with explicit dangerous policy and complete key lifecycle. |
| `0.180.4` | TLS 1.1 Public Usability Interoperability And Audit Gate | Exercise complete TLS 1.1 public client and server APIs across every assigned suite and facility against independent and archived peers, hostile records, renegotiation, compression, downgrade and oracle cases; prove isolation and obtain clean protocol-specific audit and pentest evidence. |
| `0.180.5` | Complete TLS 1.0 Codec And State Machines | Implement the complete bounded TLS 1.0 record, implicit-IV, handshake, alert, extension, compression, renegotiation, resumption and shutdown codecs and client/server state machines in `brynja-legacy-tls10`, with exact transcripts, resources, cancellation and no modern routing or fallback. |
| `0.180.6` | Complete TLS 1.0 Suites Credentials And Operations | Bind every authenticated TLS 1.0 cipher suite, export profile, MAC, MD5-plus-SHA-1 PRF, key exchange, signature, certificate, PSK or external-credential profile and every operation direction to exact shared primitive owners with explicit dangerous policy and complete lifecycle. |
| `0.180.7` | TLS 1.0 Public Usability Interoperability And Audit Gate | Exercise complete TLS 1.0 public client and server APIs across all assigned suites and facilities against independent and archived peers, BEAST-relevant record behavior, renegotiation, compression, downgrade and oracle cases; prove isolation and obtain clean audit and pentest evidence. |
| `0.180.8` | Complete DTLS 1.0 Codec And State Machines | Implement complete DTLS 1.0 records, epochs, replay, cookies, fragmentation, reassembly, flights, timers, retransmission, alerts, extensions, compression, resumption, path and client/server state in `brynja-legacy-dtls10`, preserving exact version-specific semantics and bounded Sans-I/O effects. |
| `0.180.9` | Complete DTLS 1.0 Suites Credentials And Operations | Bind every authenticated DTLS 1.0 suite, MAC, PRF, key exchange, signature, certificate, PSK, compression and operation direction to exact shared legacy primitive and TLS-profile owners while preserving datagram-specific limits, replay and amplification policy. |
| `0.180.10` | DTLS 1.0 Public Usability Interoperability And Audit Gate | Qualify complete DTLS 1.0 client and server APIs against independent and archived peers under loss, duplication, reordering, fragmentation, migration, compression, weak-suite and oracle campaigns; prove isolation and obtain clean audit and pentest evidence. |
| `0.180.11` | Complete SSL 3.0 Codec And State Machines | Implement complete SSL 3.0 record, handshake, alert, compression, renegotiation, resumption and shutdown codecs and client/server state machines in `brynja-legacy-ssl3`, including exact padding and MAC-then-encrypt semantics, with no TLS state reuse or fallback. |
| `0.180.12` | Complete SSL 3.0 Suites Credentials And Operations | Bind every authenticated SSL 3.0 cipher suite, export profile, SSL MAC and key schedule, key exchange, certificate, signature and operation direction to exact shared primitive owners with explicit POODLE, downgrade and cryptanalytic warnings and complete secret lifecycle. |
| `0.180.13` | SSL 3.0 Public Usability Interoperability And Audit Gate | Exercise complete SSL 3.0 client and server APIs across all assigned suites and facilities against archived peers and oracle, padding, truncation, renegotiation, downgrade and exhaustion campaigns; prove process and graph containment and obtain clean audit and pentest evidence. |
| `0.180.14` | Complete SSL 2.0 Authority Codec And State Machines | Authenticate the non-RFC SSL 2.0 specification and archives, then implement complete records, handshakes, errors, cipher-spec negotiation, session reuse, client and server state and bounded effects in `brynja-legacy-ssl2`; if authority or lawful implementation cannot be established, remove the production package claim rather than ship a subset. |
| `0.180.15` | Complete SSL 2.0 Suites Credentials And Operations | Bind every authenticated SSL 2.0 cipher kind, export derivation, MAC, challenge, connection ID, certificate, key exchange and client/server operation to exact shared primitives, including NULL or weak profiles only behind explicit dangerous policy and uniform oracle-resistant wrappers where possible. |
| `0.180.16` | SSL 2.0 Public Usability Interoperability And Audit Gate | Exercise complete SSL 2.0 public client and server APIs against archived independent peers and downgrade, rollback, truncation, export, oracle and resource campaigns; publish unavoidable protocol insecurity, prove isolation and obtain clean implementation audit and pentest evidence. |
| `0.180.17` | Complete WTLS Codec And State Machines | Authenticate each admitted WAP WTLS specification and implement every defined record, handshake, alert, sequence, retransmission, certificate, abbreviated-handshake, key-refresh and client/server state in `brynja-legacy-wtls` with bounded datagram effects and exact version separation. |
| `0.180.18` | Complete WTLS Suites Credentials And Operations | Bind every authenticated WTLS cipher, MAC, hash, key exchange, curve, RSA, certificate, anonymous, shared-secret, compression and operation direction to exact shared or isolated primitive owners with explicit dangerous policy, constrained-device resource models and complete lifecycle. |
| `0.180.19` | WTLS Public Usability Interoperability And Audit Gate | Exercise complete WTLS public client and server APIs across every assigned profile on constrained and hosted targets against archived corpora or peers, hostile datagrams and resource exhaustion; prove isolation and obtain clean source, cryptography, protocol audit and pentest evidence. |
| `0.180.20` | Complete PCT Codec State And Operations | Authenticate the complete PCT specification lineage and implement every defined record, handshake, error, cipher and hash negotiation, certificate, key exchange, client-authentication, connection and client/server operation in `brynja-legacy-pct`, binding exact shared primitives and explicit dangerous policy without TLS fallback. |
| `0.180.21` | PCT Public Usability Interoperability And Audit Gate | Exercise complete PCT client and server APIs across every authenticated profile against archived implementations or exact corpus replays, hostile parsing, downgrade, export, oracle and resource campaigns; prove process and graph isolation and obtain clean audit and pentest evidence. |
| `0.180.22` | Complete SNP Codec State And Operations | Authenticate the complete SNP specification lineage and implement every defined message, state, role, negotiation, credential, cryptographic profile, error and operation in `brynja-legacy-snp`, using exact shared primitive owners, explicit dangerous policy and no hidden transport, trust or fallback effects. |
| `0.180.23` | SNP Public Usability Interoperability And Audit Gate | Exercise complete SNP public APIs across all authenticated roles and profiles against archived implementations or exact corpus replays, hostile inputs, downgrade and resource campaigns; prove package and process isolation and obtain clean source, protocol and cryptographic audit and pentest evidence. |
| `0.180.24` | Complete Named Legacy Ecosystem Acceptance Gate | Regenerate the legacy register and prove TLS 1.2 compatibility, DTLS 1.2 compatibility, TLS 1.1, TLS 1.0, DTLS 1.0, SSL 3.0, SSL 2.0, WTLS, PCT and SNP each provide complete public operations rather than subsets; run cross-protocol key, credential, cache, listener, downgrade and fallback negatives and obtain a cumulative independent review and pentest with no unresolved critical or high finding. |
| `0.181.0` | Complete Catalogue Admission And Migration Register | Freeze all 104 migrated inventory rows and every named variant in the catalogue scope register, reuse prior owners, authenticate exact primary sources and rights per family, and block omitted, ambiguous or source-blocked capabilities instead of guessing implementations. |
| `0.181.1` | Semantic Domains And Minimal Family Package Contracts | Freeze separate fixed-digest, XOF, MAC, universal, rolling, checksum, check-digit, password, field, perceptual and placement public contracts; prohibit noncrypto or research values satisfying cryptographic policy and keep the curated hash facade default-off. |
| `0.181.2` | Catalogue Secret Lifecycle And Capability Closure | Extend the sealed hardened-owner and secret-state registers to every new secret-bearing catalogue operation, including keyed utility seeds, password workspaces and field witnesses; mandatory cleanup cannot depend on an optional sanitization adapter. |
| `0.181.3` | Catalogue Resource And Public API Fixture Infrastructure | Provide bounded external consumer fixtures for every semantic domain with caller workspaces, validated lengths and parameter limits, no hidden I/O, explicit streaming or length-required one-shot contracts and reproducible public commands. |
| `0.181.4` | Catalogue Standards Lifecycle And Scope Change Gate | Extend the existing standards lifecycle monitor to all catalogue authorities, including future SHA-2, FIPS 202 and SP 800-185 revisions; compare current publications, errata, withdrawals and attacks before each implementation and final release freeze. |
| `0.181.5` | Legacy Isolation And Retirement Enforcement | Consolidate all legacy protocol rules into the main plan and enforce separate types, enums, configurations, listeners, credentials, caches, tickets, PSKs, effects and fallback graphs for every legacy package. |
| `0.182.0` | Threefish Authority Parameters And Public Cipher Contract | Freeze complete Threefish-256/512/1024 key, tweak, block, endian and round identities before Skein; use the small first-party `brynja-cipher-threefish` owner with complete encrypt/decrypt APIs rather than a private hash-only copy. |
| `0.182.1` | Complete Threefish-256 | Implement complete first-party no_std Threefish-256 key/tweak setup, encrypt/decrypt and compiler-resistant private-state cleanup with standalone public APIs. |
| `0.182.2` | Complete Threefish-512 | Implement complete Threefish-512 encrypt/decrypt, tweak lifecycle and hardened key/schedule cleanup without aliasing Threefish-256 or duplicating the shared parameter owner. |
| `0.182.3` | Complete Threefish-1024 | Implement complete Threefish-1024 encrypt/decrypt, tweak lifecycle and hardened key/schedule cleanup with explicit fixed-size resources. |
| `0.182.4` | Threefish Portable Acceptance And Scalar Evidence | Close all three Threefish members through packaged encrypt/decrypt examples, official vectors, independent differentials, cleanup MIR/LLVM/assembly evidence and explicit scalar-only disposition before Skein. |
| `0.182.5` | Research Binary-Field And Transform Dependency Inventory | Freeze exact binary fields, curves, transforms, prime tables and arithmetic owners required by ECOH, FSB, SWIFFT, VSH and field-hash profiles; reuse existing limb/curve code where identities match. |
| `0.182.6` | Complete Research Binary-Field Operations | Implement the exact admitted binary-field add/multiply/square/invert and canonical encoding operations for ECOH through separate parameterized first-party owners; split additional field profiles into patches before consumption. |
| `0.182.7` | Complete Research Curve And Transform Operations | Complete admitted ECOH point/map operations and SWIFFT transform/FSB matrix primitives over exact owners, with separate curve and transform APIs and child patches per independently sized parameter set. |
| `0.182.8` | Research Arithmetic Public Acceptance | Accept the exact binary-field, curve, matrix, transform and modular arithmetic dependency profiles with public consumer tests before ECOH, FSB, SWIFFT, VSH or field-hash implementations proceed. |
| `0.183.0` | CRC - Authority And API Freeze | Authenticate POSIX cksum and original profile standards, independently cross-checked CRC catalogue; freeze every Unix cksum; CRC-8; CRC-16; CRC-32; CRC-64; CRC-32C; named CRC catalogue profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-crc` before implementation. |
| `0.183.1` | CRC - Complete checked byte/bit polynomial engine | After v0.183.0, implement complete checked byte/bit polynomial engine in `brynja-checksum-crc` with usable public operations, exact parameters and the frozen CRC contract: Checked polynomial/profile constructors bind width, init, refin/refout, xorout, augmentation, residue and serialization; byte/bit update, finalize, reset and length-aware combine; cksum appends length exactly, never aliases CRC-32. |
| `0.183.2` | CRC - Complete named CRC profiles and canonical residues | After v0.183.0, implement complete named CRC profiles and canonical residues in `brynja-checksum-crc` with usable public operations, exact parameters and the frozen CRC contract: Checked polynomial/profile constructors bind width, init, refin/refout, xorout, augmentation, residue and serialization; byte/bit update, finalize, reset and length-aware combine; cksum appends length exactly, never aliases CRC-32. |
| `0.183.3` | CRC - Complete Unix cksum and length-aware combine | After v0.183.0, implement complete Unix cksum and length-aware combine in `brynja-checksum-crc` with usable public operations, exact parameters and the frozen CRC contract: Checked polynomial/profile constructors bind width, init, refin/refout, xorout, augmentation, residue and serialization; byte/bit update, finalize, reset and length-aware combine; cksum appends length exactly, never aliases CRC-32. |
| `0.183.4` | CRC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by CRC: Checked polynomial/profile constructors bind width, init, refin/refout, xorout, augmentation, residue and serialization; byte/bit update, finalize, reset and length-aware combine; cksum appends length exactly, never aliases CRC-32; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-crc`. |
| `0.183.5` | CRC - Portable Public API Acceptance | Freeze an independently runnable package-external CRC fixture covering every declared profile and operation in `brynja-checksum-crc` before any optimized backend or final evidence sweep. |
| `0.183.6` | CRC - Optional Backend Implementation And Dispatch | Only after v0.183.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for CRC, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.183.7` | CRC - Final Evidence And Usability Closure | Rerun v0.183.5 public acceptance for every CRC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-crc`. |
| `0.184.0` | Unix sums - Authority And API Freeze | Authenticate POSIX and authenticated historical BSD/System V sources; freeze every BSD Unix checksum; System V Unix checksum; sum8; sum24; sum32; xor8 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-sum` before implementation. |
| `0.184.1` | Unix sums - Complete BSD Unix checksum; System V Unix checksum; sum8; sum24; sum32; xor8 | After v0.184.0, implement complete BSD Unix checksum; System V Unix checksum; sum8; sum24; sum32; xor8 in `brynja-checksum-sum` with usable public operations, exact parameters and the frozen Unix sums contract: Distinct BSD rotation and System V folding profiles, explicit arithmetic widths, byte update/finalize/reset and canonical output; checked byte count, no platform-word-dependent identity. |
| `0.184.2` | Unix sums - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Unix sums: Distinct BSD rotation and System V folding profiles, explicit arithmetic widths, byte update/finalize/reset and canonical output; checked byte count, no platform-word-dependent identity; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-sum`. |
| `0.184.3` | Unix sums - Portable Public API Acceptance | Freeze an independently runnable package-external Unix sums fixture covering every declared profile and operation in `brynja-checksum-sum` before any optimized backend or final evidence sweep. |
| `0.184.4` | Unix sums - Optional Backend Implementation And Dispatch | Only after v0.184.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Unix sums, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.184.5` | Unix sums - Final Evidence And Usability Closure | Rerun v0.184.3 public acceptance for every Unix sums profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-sum`. |
| `0.185.0` | Internet checksum - Authority And API Freeze | Authenticate RFC 1071 and RFC 1624; freeze every Internet checksum profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-internet` before implementation. |
| `0.185.1` | Internet checksum - Complete Internet checksum | After v0.185.0, implement complete Internet checksum in `brynja-checksum-internet` with usable public operations, exact parameters and the frozen Internet checksum contract: One-shot and streaming ones-complement checksum preserving odd-byte carry across chunks; verify and incremental replacement with explicit network byte order and pseudo-header input owned by caller. |
| `0.185.2` | Internet checksum - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Internet checksum: One-shot and streaming ones-complement checksum preserving odd-byte carry across chunks; verify and incremental replacement with explicit network byte order and pseudo-header input owned by caller; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-internet`. |
| `0.185.3` | Internet checksum - Portable Public API Acceptance | Freeze an independently runnable package-external Internet checksum fixture covering every declared profile and operation in `brynja-checksum-internet` before any optimized backend or final evidence sweep. |
| `0.185.4` | Internet checksum - Optional Backend Implementation And Dispatch | Only after v0.185.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Internet checksum, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.185.5` | Internet checksum - Final Evidence And Usability Closure | Rerun v0.185.3 public acceptance for every Internet checksum profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-internet`. |
| `0.186.0` | Fletcher - Authority And API Freeze | Authenticate Original Fletcher paper and individual protocol/storage profile authorities; freeze every Fletcher-4; Fletcher-8; Fletcher-16; Fletcher-32; Fletcher-64 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-fletcher` before implementation. |
| `0.186.1` | Fletcher - Complete Fletcher-4; Fletcher-8; Fletcher-16; Fletcher-32; Fletcher-64 | After v0.186.0, implement complete Fletcher-4; Fletcher-8; Fletcher-16; Fletcher-32; Fletcher-64 in `brynja-checksum-fletcher` with usable public operations, exact parameters and the frozen Fletcher contract: Name each exact word-width/modulus/init/reduction/endian profile; streaming state, finalize/reset and validated combine only where derived; do not guess what Fletcher-4 means. |
| `0.186.2` | Fletcher - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Fletcher: Name each exact word-width/modulus/init/reduction/endian profile; streaming state, finalize/reset and validated combine only where derived; do not guess what Fletcher-4 means; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-fletcher`. |
| `0.186.3` | Fletcher - Portable Public API Acceptance | Freeze an independently runnable package-external Fletcher fixture covering every declared profile and operation in `brynja-checksum-fletcher` before any optimized backend or final evidence sweep. |
| `0.186.4` | Fletcher - Optional Backend Implementation And Dispatch | Only after v0.186.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Fletcher, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.186.5` | Fletcher - Final Evidence And Usability Closure | Rerun v0.186.3 public acceptance for every Fletcher profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-fletcher`. |
| `0.187.0` | Adler - Authority And API Freeze | Authenticate RFC 1950 and original Adler specification; freeze every Adler-32 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-adler` before implementation. |
| `0.187.1` | Adler - Complete Adler-32 | After v0.187.0, implement complete Adler-32 in `brynja-checksum-adler` with usable public operations, exact parameters and the frozen Adler contract: Byte streaming, fixed output, reset and checked length-aware concatenation over the specified modulus and initial value. |
| `0.187.2` | Adler - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Adler: Byte streaming, fixed output, reset and checked length-aware concatenation over the specified modulus and initial value; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-adler`. |
| `0.187.3` | Adler - Portable Public API Acceptance | Freeze an independently runnable package-external Adler fixture covering every declared profile and operation in `brynja-checksum-adler` before any optimized backend or final evidence sweep. |
| `0.187.4` | Adler - Optional Backend Implementation And Dispatch | Only after v0.187.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Adler, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.187.5` | Adler - Final Evidence And Usability Closure | Rerun v0.187.3 public acceptance for every Adler profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-adler`. |
| `0.188.0` | Check digits - Authority And API Freeze | Authenticate Original Luhn, Verhoeff and Damm publications and applicable identifier standards; freeze every Luhn; Verhoeff; Damm profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-checksum-checkdigit` before implementation. |
| `0.188.1` | Check digits - Complete Luhn; Verhoeff; Damm | After v0.188.0, implement complete Luhn; Verhoeff; Damm in `brynja-checksum-checkdigit` with usable public operations, exact parameters and the frozen Check digits contract: Generate check digits and validate complete identifiers, exact decimal alphabet and leading-zero policy, streaming where direction permits and caller-buffer construction; reject invalid symbols instead of normalizing silently. |
| `0.188.2` | Check digits - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Check digits: Generate check digits and validate complete identifiers, exact decimal alphabet and leading-zero policy, streaming where direction permits and caller-buffer construction; reject invalid symbols instead of normalizing silently; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-checksum-checkdigit`. |
| `0.188.3` | Check digits - Portable Public API Acceptance | Freeze an independently runnable package-external Check digits fixture covering every declared profile and operation in `brynja-checksum-checkdigit` before any optimized backend or final evidence sweep. |
| `0.188.4` | Check digits - Optional Backend Implementation And Dispatch | Only after v0.188.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Check digits, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.188.5` | Check digits - Final Evidence And Usability Closure | Rerun v0.188.3 public acceptance for every Check digits profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-checksum-checkdigit`. |
| `0.189.0` | Rabin fingerprints - Authority And API Freeze | Authenticate Original Rabin fingerprint construction; freeze every Rabin fingerprint profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-universal-rabin` before implementation. |
| `0.189.1` | Rabin fingerprints - Complete Rabin fingerprint | After v0.189.0, implement complete Rabin fingerprint in `brynja-hash-universal-rabin` with usable public operations, exact parameters and the frozen Rabin fingerprints contract: Typed irreducible-polynomial and window parameters, streaming and rolling remove/add with caller-owned bounded workspace and canonical fingerprint output. |
| `0.189.2` | Rabin fingerprints - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Rabin fingerprints: Typed irreducible-polynomial and window parameters, streaming and rolling remove/add with caller-owned bounded workspace and canonical fingerprint output; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-universal-rabin`. |
| `0.189.3` | Rabin fingerprints - Portable Public API Acceptance | Freeze an independently runnable package-external Rabin fingerprints fixture covering every declared profile and operation in `brynja-hash-universal-rabin` before any optimized backend or final evidence sweep. |
| `0.189.4` | Rabin fingerprints - Optional Backend Implementation And Dispatch | Only after v0.189.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Rabin fingerprints, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.189.5` | Rabin fingerprints - Final Evidence And Usability Closure | Rerun v0.189.3 public acceptance for every Rabin fingerprints profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-universal-rabin`. |
| `0.190.0` | Tabulation - Authority And API Freeze | Authenticate Original simple and twisted tabulation publications; freeze every Tabulation hashing profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-universal-tabulation` before implementation. |
| `0.190.1` | Tabulation - Complete Tabulation hashing | After v0.190.0, implement complete Tabulation hashing in `brynja-hash-universal-tabulation` with usable public operations, exact parameters and the frozen Tabulation contract: Exact simple/twisted profiles only after individual admission; caller-provided random tables, bounded key-width hashing and redacted table owner; distinguish seeded reproducibility from adversarial guarantees. |
| `0.190.2` | Tabulation - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Tabulation: Exact simple/twisted profiles only after individual admission; caller-provided random tables, bounded key-width hashing and redacted table owner; distinguish seeded reproducibility from adversarial guarantees; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-universal-tabulation`. |
| `0.190.3` | Tabulation - Portable Public API Acceptance | Freeze an independently runnable package-external Tabulation fixture covering every declared profile and operation in `brynja-hash-universal-tabulation` before any optimized backend or final evidence sweep. |
| `0.190.4` | Tabulation - Optional Backend Implementation And Dispatch | Only after v0.190.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Tabulation, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.190.5` | Tabulation - Final Evidence And Usability Closure | Rerun v0.190.3 public acceptance for every Tabulation profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-universal-tabulation`. |
| `0.191.0` | Zobrist - Authority And API Freeze | Authenticate Original Zobrist publication and frozen table-generation profile; freeze every Zobrist hashing profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-universal-zobrist` before implementation. |
| `0.191.1` | Zobrist - Complete Zobrist hashing | After v0.191.0, implement complete Zobrist hashing in `brynja-hash-universal-zobrist` with usable public operations, exact parameters and the frozen Zobrist contract: Caller-owned table/profile, full-state and reversible position/feature update, explicit key generation port and non-cryptographic result type. |
| `0.191.2` | Zobrist - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Zobrist: Caller-owned table/profile, full-state and reversible position/feature update, explicit key generation port and non-cryptographic result type; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-universal-zobrist`. |
| `0.191.3` | Zobrist - Portable Public API Acceptance | Freeze an independently runnable package-external Zobrist fixture covering every declared profile and operation in `brynja-hash-universal-zobrist` before any optimized backend or final evidence sweep. |
| `0.191.4` | Zobrist - Optional Backend Implementation And Dispatch | Only after v0.191.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Zobrist, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.191.5` | Zobrist - Final Evidence And Usability Closure | Rerun v0.191.3 public acceptance for every Zobrist profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-universal-zobrist`. |
| `0.192.0` | Rolling and chunking - Authority And API Freeze | Authenticate Original Buzhash, rsync, Gear and FastCDC publications; freeze every Buzhash; rolling rsync checksum; Gear; FastCDC profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-rolling` before implementation. |
| `0.192.1` | Rolling and chunking - Complete Buzhash rolling window | After v0.192.0, implement complete Buzhash rolling window in `brynja-hash-noncrypto-rolling` with usable public operations, exact parameters and the frozen Rolling and chunking contract: Separate rolling fingerprint from chunk-boundary policy; push/evict/reset, min/normal/max chunk bounds, caller buffers and deterministic EOF flush; exact table and FastCDC revision, never hidden I/O. |
| `0.192.2` | Rolling and chunking - Complete rsync rolling checksum | After v0.192.0, implement complete rsync rolling checksum in `brynja-hash-noncrypto-rolling` with usable public operations, exact parameters and the frozen Rolling and chunking contract: Separate rolling fingerprint from chunk-boundary policy; push/evict/reset, min/normal/max chunk bounds, caller buffers and deterministic EOF flush; exact table and FastCDC revision, never hidden I/O. |
| `0.192.3` | Rolling and chunking - Complete Gear fingerprint | After v0.192.0, implement complete Gear fingerprint in `brynja-hash-noncrypto-rolling` with usable public operations, exact parameters and the frozen Rolling and chunking contract: Separate rolling fingerprint from chunk-boundary policy; push/evict/reset, min/normal/max chunk bounds, caller buffers and deterministic EOF flush; exact table and FastCDC revision, never hidden I/O. |
| `0.192.4` | Rolling and chunking - Complete FastCDC chunk policy and EOF lifecycle | After v0.192.0, implement complete FastCDC chunk policy and EOF lifecycle in `brynja-hash-noncrypto-rolling` with usable public operations, exact parameters and the frozen Rolling and chunking contract: Separate rolling fingerprint from chunk-boundary policy; push/evict/reset, min/normal/max chunk bounds, caller buffers and deterministic EOF flush; exact table and FastCDC revision, never hidden I/O. |
| `0.192.5` | Rolling and chunking - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Rolling and chunking: Separate rolling fingerprint from chunk-boundary policy; push/evict/reset, min/normal/max chunk bounds, caller buffers and deterministic EOF flush; exact table and FastCDC revision, never hidden I/O; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-rolling`. |
| `0.192.6` | Rolling and chunking - Portable Public API Acceptance | Freeze an independently runnable package-external Rolling and chunking fixture covering every declared profile and operation in `brynja-hash-noncrypto-rolling` before any optimized backend or final evidence sweep. |
| `0.192.7` | Rolling and chunking - Optional Backend Implementation And Dispatch | Only after v0.192.6, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Rolling and chunking, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.192.8` | Rolling and chunking - Final Evidence And Usability Closure | Rerun v0.192.6 public acceptance for every Rolling and chunking profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-rolling`. |
| `0.193.0` | Classic table hashes - Authority And API Freeze | Authenticate Original algorithm publications and versioned author implementations; freeze every Pearson hashing; SuperFastHash; Bernstein djb2 variants; PJW; ELF hash; SDBM profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-classic` before implementation. |
| `0.193.1` | Classic table hashes - Complete Pearson and SuperFastHash | After v0.193.0, implement complete Pearson and SuperFastHash in `brynja-hash-noncrypto-classic` with usable public operations, exact parameters and the frozen Classic table hashes contract: Distinct Pearson tables, SuperFastHash, djb2 add/XOR, PJW/ELF and SDBM-32/64 types; explicit seed, word arithmetic, input-byte signedness and output encoding; streaming only when exact algorithm permits. |
| `0.193.2` | Classic table hashes - Complete djb2 add/XOR and SDBM-32/64 | After v0.193.0, implement complete djb2 add/XOR and SDBM-32/64 in `brynja-hash-noncrypto-classic` with usable public operations, exact parameters and the frozen Classic table hashes contract: Distinct Pearson tables, SuperFastHash, djb2 add/XOR, PJW/ELF and SDBM-32/64 types; explicit seed, word arithmetic, input-byte signedness and output encoding; streaming only when exact algorithm permits. |
| `0.193.3` | Classic table hashes - Complete PJW and ELF | After v0.193.0, implement complete PJW and ELF in `brynja-hash-noncrypto-classic` with usable public operations, exact parameters and the frozen Classic table hashes contract: Distinct Pearson tables, SuperFastHash, djb2 add/XOR, PJW/ELF and SDBM-32/64 types; explicit seed, word arithmetic, input-byte signedness and output encoding; streaming only when exact algorithm permits. |
| `0.193.4` | Classic table hashes - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Classic table hashes: Distinct Pearson tables, SuperFastHash, djb2 add/XOR, PJW/ELF and SDBM-32/64 types; explicit seed, word arithmetic, input-byte signedness and output encoding; streaming only when exact algorithm permits; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-classic`. |
| `0.193.5` | Classic table hashes - Portable Public API Acceptance | Freeze an independently runnable package-external Classic table hashes fixture covering every declared profile and operation in `brynja-hash-noncrypto-classic` before any optimized backend or final evidence sweep. |
| `0.193.6` | Classic table hashes - Optional Backend Implementation And Dispatch | Only after v0.193.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Classic table hashes, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.193.7` | Classic table hashes - Final Evidence And Usability Closure | Rerun v0.193.5 public acceptance for every Classic table hashes profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-classic`. |
| `0.194.0` | FNV - Authority And API Freeze | Authenticate Current authenticated FNV specification and author test vectors; freeze every FNV-1; FNV-1a; FNV-32; FNV-64; FNV-128; FNV-256; FNV-512; FNV-1024 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-fnv` before implementation. |
| `0.194.1` | FNV - Complete FNV-1/1a widths 32/64/128 | After v0.194.0, implement complete FNV-1/1a widths 32/64/128 in `brynja-hash-noncrypto-fnv` with usable public operations, exact parameters and the frozen FNV contract: Separate 1/1a and all six widths, exact primes/offset bases, fixed and incremental APIs, seed constructors and stable little-endian interchange; wide words use checked fixed limb storage. |
| `0.194.2` | FNV - Complete FNV-1/1a widths 256/512/1024 | After v0.194.0, implement complete FNV-1/1a widths 256/512/1024 in `brynja-hash-noncrypto-fnv` with usable public operations, exact parameters and the frozen FNV contract: Separate 1/1a and all six widths, exact primes/offset bases, fixed and incremental APIs, seed constructors and stable little-endian interchange; wide words use checked fixed limb storage. |
| `0.194.3` | FNV - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by FNV: Separate 1/1a and all six widths, exact primes/offset bases, fixed and incremental APIs, seed constructors and stable little-endian interchange; wide words use checked fixed limb storage; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-fnv`. |
| `0.194.4` | FNV - Portable Public API Acceptance | Freeze an independently runnable package-external FNV fixture covering every declared profile and operation in `brynja-hash-noncrypto-fnv` before any optimized backend or final evidence sweep. |
| `0.194.5` | FNV - Optional Backend Implementation And Dispatch | Only after v0.194.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for FNV, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.194.6` | FNV - Final Evidence And Usability Closure | Rerun v0.194.4 public acceptance for every FNV profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-fnv`. |
| `0.195.0` | Jenkins - Authority And API Freeze | Authenticate Bob Jenkins original publications and frozen reference revisions; freeze every Jenkins families profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-jenkins` before implementation. |
| `0.195.1` | Jenkins - Complete one-at-a-time | After v0.195.0, implement complete one-at-a-time in `brynja-hash-noncrypto-jenkins` with usable public operations, exact parameters and the frozen Jenkins contract: Separate one-at-a-time, lookup2 and lookup3/hashlittle/hashbig/hashword profiles; explicit seeds, endian and length semantics; no generic Jenkins alias. |
| `0.195.2` | Jenkins - Complete lookup2 | After v0.195.0, implement complete lookup2 in `brynja-hash-noncrypto-jenkins` with usable public operations, exact parameters and the frozen Jenkins contract: Separate one-at-a-time, lookup2 and lookup3/hashlittle/hashbig/hashword profiles; explicit seeds, endian and length semantics; no generic Jenkins alias. |
| `0.195.3` | Jenkins - Complete lookup3 byte and word profiles | After v0.195.0, implement complete lookup3 byte and word profiles in `brynja-hash-noncrypto-jenkins` with usable public operations, exact parameters and the frozen Jenkins contract: Separate one-at-a-time, lookup2 and lookup3/hashlittle/hashbig/hashword profiles; explicit seeds, endian and length semantics; no generic Jenkins alias. |
| `0.195.4` | Jenkins - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Jenkins: Separate one-at-a-time, lookup2 and lookup3/hashlittle/hashbig/hashword profiles; explicit seeds, endian and length semantics; no generic Jenkins alias; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-jenkins`. |
| `0.195.5` | Jenkins - Portable Public API Acceptance | Freeze an independently runnable package-external Jenkins fixture covering every declared profile and operation in `brynja-hash-noncrypto-jenkins` before any optimized backend or final evidence sweep. |
| `0.195.6` | Jenkins - Optional Backend Implementation And Dispatch | Only after v0.195.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Jenkins, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.195.7` | Jenkins - Final Evidence And Usability Closure | Rerun v0.195.5 public acceptance for every Jenkins profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-jenkins`. |
| `0.196.0` | Murmur - Authority And API Freeze | Authenticate Austin Appleby original versioned sources and licenses; freeze every MurmurHash variants profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-murmur` before implementation. |
| `0.196.1` | Murmur - Complete Murmur1 and Murmur2 | After v0.196.0, implement complete Murmur1 and Murmur2 in `brynja-hash-noncrypto-murmur` with usable public operations, exact parameters and the frozen Murmur contract: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics. |
| `0.196.2` | Murmur - Complete Murmur2A/64A/64B | After v0.196.0, implement complete Murmur2A/64A/64B in `brynja-hash-noncrypto-murmur` with usable public operations, exact parameters and the frozen Murmur contract: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics. |
| `0.196.3` | Murmur - Complete Murmur Neutral2/Aligned2 | After v0.196.0, implement complete Murmur Neutral2/Aligned2 in `brynja-hash-noncrypto-murmur` with usable public operations, exact parameters and the frozen Murmur contract: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics. |
| `0.196.4` | Murmur - Complete Murmur3 x86-32/x86-128 | After v0.196.0, implement complete Murmur3 x86-32/x86-128 in `brynja-hash-noncrypto-murmur` with usable public operations, exact parameters and the frozen Murmur contract: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics. |
| `0.196.5` | Murmur - Complete Murmur3 x64-128 | After v0.196.0, implement complete Murmur3 x64-128 in `brynja-hash-noncrypto-murmur` with usable public operations, exact parameters and the frozen Murmur contract: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics. |
| `0.196.6` | Murmur - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Murmur: Enumerate Murmur1, Murmur2/2A/64A/64B/Neutral2/Aligned2 and Murmur3 x86-32/x86-128/x64-128; explicit seed and total-length requirements, endian-stable variants separated from historical machine semantics; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-murmur`. |
| `0.196.7` | Murmur - Portable Public API Acceptance | Freeze an independently runnable package-external Murmur fixture covering every declared profile and operation in `brynja-hash-noncrypto-murmur` before any optimized backend or final evidence sweep. |
| `0.196.8` | Murmur - Optional Backend Implementation And Dispatch | Only after v0.196.7, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Murmur, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.196.9` | Murmur - Final Evidence And Usability Closure | Rerun v0.196.7 public acceptance for every Murmur profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-murmur`. |
| `0.197.0` | Fast-Hash - Authority And API Freeze | Authenticate Original referenced Fast-Hash author source, identity unresolved until admission; freeze every Fast-Hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-fast` before implementation. |
| `0.197.1` | Fast-Hash - Complete Fast-Hash | After v0.197.0, implement complete Fast-Hash in `brynja-research-hash-fast` with usable public operations, exact parameters and the frozen Fast-Hash contract: Authenticate the intended design before exposing versioned fasthash32/64 byte APIs and seed semantics; preserve any total-length prerequisite rather than invent streaming. |
| `0.197.2` | Fast-Hash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Fast-Hash: Authenticate the intended design before exposing versioned fasthash32/64 byte APIs and seed semantics; preserve any total-length prerequisite rather than invent streaming; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-fast`. |
| `0.197.3` | Fast-Hash - Portable Public API Acceptance | Freeze an independently runnable package-external Fast-Hash fixture covering every declared profile and operation in `brynja-research-hash-fast` before any optimized backend or final evidence sweep. |
| `0.197.4` | Fast-Hash - Optional Backend Implementation And Dispatch | Only after v0.197.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Fast-Hash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.197.5` | Fast-Hash - Final Evidence And Usability Closure | Rerun v0.197.3 public acceptance for every Fast-Hash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-fast`. |
| `0.198.0` | SpookyHash - Authority And API Freeze | Authenticate Bob Jenkins SpookyHash V1/V2 sources; freeze every SpookyHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-spooky` before implementation. |
| `0.198.1` | SpookyHash - Complete SpookyHash | After v0.198.0, implement complete SpookyHash in `brynja-hash-noncrypto-spooky` with usable public operations, exact parameters and the frozen SpookyHash contract: Distinct V1/V2 and 32/64/128 output/seed profiles, streaming short/long transition and fixed-length canonical output. |
| `0.198.2` | SpookyHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by SpookyHash: Distinct V1/V2 and 32/64/128 output/seed profiles, streaming short/long transition and fixed-length canonical output; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-spooky`. |
| `0.198.3` | SpookyHash - Portable Public API Acceptance | Freeze an independently runnable package-external SpookyHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-spooky` before any optimized backend or final evidence sweep. |
| `0.198.4` | SpookyHash - Optional Backend Implementation And Dispatch | Only after v0.198.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for SpookyHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.198.5` | SpookyHash - Final Evidence And Usability Closure | Rerun v0.198.3 public acceptance for every SpookyHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-spooky`. |
| `0.199.0` | CityHash - Authority And API Freeze | Authenticate Google CityHash versioned source and license; freeze every CityHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-city` before implementation. |
| `0.199.1` | CityHash - Complete CityHash | After v0.199.0, implement complete CityHash in `brynja-hash-noncrypto-city` with usable public operations, exact parameters and the frozen CityHash contract: Exact CityHash32/64/128, seeded and CRC-accelerated profiles as separate identities; slice APIs with explicit length, no fabricated streaming state. |
| `0.199.2` | CityHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by CityHash: Exact CityHash32/64/128, seeded and CRC-accelerated profiles as separate identities; slice APIs with explicit length, no fabricated streaming state; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-city`. |
| `0.199.3` | CityHash - Portable Public API Acceptance | Freeze an independently runnable package-external CityHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-city` before any optimized backend or final evidence sweep. |
| `0.199.4` | CityHash - Optional Backend Implementation And Dispatch | Only after v0.199.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for CityHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.199.5` | CityHash - Final Evidence And Usability Closure | Rerun v0.199.3 public acceptance for every CityHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-city`. |
| `0.200.0` | FarmHash - Authority And API Freeze | Authenticate Google FarmHash versioned source and license; freeze every FarmHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-farm` before implementation. |
| `0.200.1` | FarmHash - Complete FarmHash | After v0.200.0, implement complete FarmHash in `brynja-hash-noncrypto-farm` with usable public operations, exact parameters and the frozen FarmHash contract: Separate stable Fingerprint32/64/128 from implementation-dependent Hash profiles; pin revision/architecture/seed and never promise cross-process stability for unspecified profiles. |
| `0.200.2` | FarmHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by FarmHash: Separate stable Fingerprint32/64/128 from implementation-dependent Hash profiles; pin revision/architecture/seed and never promise cross-process stability for unspecified profiles; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-farm`. |
| `0.200.3` | FarmHash - Portable Public API Acceptance | Freeze an independently runnable package-external FarmHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-farm` before any optimized backend or final evidence sweep. |
| `0.200.4` | FarmHash - Optional Backend Implementation And Dispatch | Only after v0.200.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for FarmHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.200.5` | FarmHash - Final Evidence And Usability Closure | Rerun v0.200.3 public acceptance for every FarmHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-farm`. |
| `0.201.0` | MetroHash - Authority And API Freeze | Authenticate Original MetroHash author specification and sources; freeze every MetroHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-metro` before implementation. |
| `0.201.1` | MetroHash - Complete MetroHash | After v0.201.0, implement complete MetroHash in `brynja-hash-noncrypto-metro` with usable public operations, exact parameters and the frozen MetroHash contract: Separate MetroHash64/128 and authenticated legacy/CRC variants; seeded one-shot and exact incremental forms with canonical output. |
| `0.201.2` | MetroHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MetroHash: Separate MetroHash64/128 and authenticated legacy/CRC variants; seeded one-shot and exact incremental forms with canonical output; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-metro`. |
| `0.201.3` | MetroHash - Portable Public API Acceptance | Freeze an independently runnable package-external MetroHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-metro` before any optimized backend or final evidence sweep. |
| `0.201.4` | MetroHash - Optional Backend Implementation And Dispatch | Only after v0.201.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MetroHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.201.5` | MetroHash - Final Evidence And Usability Closure | Rerun v0.201.3 public acceptance for every MetroHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-metro`. |
| `0.202.0` | Numeric hash - Authority And API Freeze | Authenticate Original Numeric hash source referred to by inventory, source admission required; freeze every Numeric hash nhash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-nhash` before implementation. |
| `0.202.1` | Numeric hash - Complete Numeric hash nhash | After v0.202.0, implement complete Numeric hash nhash in `brynja-research-hash-nhash` with usable public operations, exact parameters and the frozen Numeric hash contract: Resolve nhash source identity and numeric input domain first; typed integer/float encodings only as specified, including NaN/signed-zero policy; no guessed universal byte digest. |
| `0.202.2` | Numeric hash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Numeric hash: Resolve nhash source identity and numeric input domain first; typed integer/float encodings only as specified, including NaN/signed-zero policy; no guessed universal byte digest; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-nhash`. |
| `0.202.3` | Numeric hash - Portable Public API Acceptance | Freeze an independently runnable package-external Numeric hash fixture covering every declared profile and operation in `brynja-research-hash-nhash` before any optimized backend or final evidence sweep. |
| `0.202.4` | Numeric hash - Optional Backend Implementation And Dispatch | Only after v0.202.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Numeric hash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.202.5` | Numeric hash - Final Evidence And Usability Closure | Rerun v0.202.3 public acceptance for every Numeric hash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-nhash`. |
| `0.203.0` | xxHash - Authority And API Freeze | Authenticate Official xxHash specification and versioned vectors; freeze every xxHash; XXH32; XXH64; XXH3-64; XXH3-128 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-xxhash` before implementation. |
| `0.203.1` | xxHash - Complete XXH32 and XXH64 | After v0.203.0, implement complete XXH32 and XXH64 in `brynja-hash-noncrypto-xxhash` with usable public operations, exact parameters and the frozen xxHash contract: Exact XXH32/64 and XXH3-64/128 streaming, seeded/custom-secret profiles, reset/copy policies and canonical encoding; redacted custom-secret owner without claiming cryptographic MAC security. |
| `0.203.2` | xxHash - Complete XXH3-64 | After v0.203.0, implement complete XXH3-64 in `brynja-hash-noncrypto-xxhash` with usable public operations, exact parameters and the frozen xxHash contract: Exact XXH32/64 and XXH3-64/128 streaming, seeded/custom-secret profiles, reset/copy policies and canonical encoding; redacted custom-secret owner without claiming cryptographic MAC security. |
| `0.203.3` | xxHash - Complete XXH3-128 and custom-secret profiles | After v0.203.0, implement complete XXH3-128 and custom-secret profiles in `brynja-hash-noncrypto-xxhash` with usable public operations, exact parameters and the frozen xxHash contract: Exact XXH32/64 and XXH3-64/128 streaming, seeded/custom-secret profiles, reset/copy policies and canonical encoding; redacted custom-secret owner without claiming cryptographic MAC security. |
| `0.203.4` | xxHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by xxHash: Exact XXH32/64 and XXH3-64/128 streaming, seeded/custom-secret profiles, reset/copy policies and canonical encoding; redacted custom-secret owner without claiming cryptographic MAC security; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-xxhash`. |
| `0.203.5` | xxHash - Portable Public API Acceptance | Freeze an independently runnable package-external xxHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-xxhash` before any optimized backend or final evidence sweep. |
| `0.203.6` | xxHash - Optional Backend Implementation And Dispatch | Only after v0.203.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for xxHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.203.7` | xxHash - Final Evidence And Usability Closure | Rerun v0.203.5 public acceptance for every xxHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-xxhash`. |
| `0.204.0` | t1ha - Authority And API Freeze | Authenticate Original t1ha versioned specification and sources; freeze every t1ha profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-t1ha` before implementation. |
| `0.204.1` | t1ha - Complete t1ha | After v0.204.0, implement complete t1ha in `brynja-hash-noncrypto-t1ha` with usable public operations, exact parameters and the frozen t1ha contract: Separate t1ha0 architecture variants, t1ha1 endian profiles and t1ha2 at-once/streaming 64/128; explicit stability, seed and AES capability identities. |
| `0.204.2` | t1ha - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by t1ha: Separate t1ha0 architecture variants, t1ha1 endian profiles and t1ha2 at-once/streaming 64/128; explicit stability, seed and AES capability identities; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-t1ha`. |
| `0.204.3` | t1ha - Portable Public API Acceptance | Freeze an independently runnable package-external t1ha fixture covering every declared profile and operation in `brynja-hash-noncrypto-t1ha` before any optimized backend or final evidence sweep. |
| `0.204.4` | t1ha - Optional Backend Implementation And Dispatch | Only after v0.204.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for t1ha, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.204.5` | t1ha - Final Evidence And Usability Closure | Rerun v0.204.3 public acceptance for every t1ha profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-t1ha`. |
| `0.205.0` | GxHash - Authority And API Freeze | Authenticate Original GxHash source and license, security classification review required; freeze every GxHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-gx` before implementation. |
| `0.205.1` | GxHash - Complete GxHash | After v0.205.0, implement complete GxHash in `brynja-research-hash-gx` with usable public operations, exact parameters and the frozen GxHash contract: Pin exact AES-based GxHash revision and 32/64/128 profiles; portable first-party AES-round oracle before optional kernels, typed seed and explicit unsupported-backend errors. |
| `0.205.2` | GxHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by GxHash: Pin exact AES-based GxHash revision and 32/64/128 profiles; portable first-party AES-round oracle before optional kernels, typed seed and explicit unsupported-backend errors; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-gx`. |
| `0.205.3` | GxHash - Portable Public API Acceptance | Freeze an independently runnable package-external GxHash fixture covering every declared profile and operation in `brynja-research-hash-gx` before any optimized backend or final evidence sweep. |
| `0.205.4` | GxHash - Optional Backend Implementation And Dispatch | Only after v0.205.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for GxHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.205.5` | GxHash - Final Evidence And Usability Closure | Rerun v0.205.3 public acceptance for every GxHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-gx`. |
| `0.206.0` | OSDB - Authority And API Freeze | Authenticate Original OSDB/OpenSubtitles specification after identity confirmation; freeze every OSDB hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-osdb` before implementation. |
| `0.206.1` | OSDB - Complete OSDB hash | After v0.206.0, implement complete OSDB hash in `brynja-research-hash-osdb` with usable public operations, exact parameters and the frozen OSDB contract: Authenticate OpenSubtitles/OSDB identity, first/last-region sampling, size mixing and minimum-file rules; caller supplies exact slices and length, no filesystem effects. |
| `0.206.2` | OSDB - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by OSDB: Authenticate OpenSubtitles/OSDB identity, first/last-region sampling, size mixing and minimum-file rules; caller supplies exact slices and length, no filesystem effects; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-osdb`. |
| `0.206.3` | OSDB - Portable Public API Acceptance | Freeze an independently runnable package-external OSDB fixture covering every declared profile and operation in `brynja-research-hash-osdb` before any optimized backend or final evidence sweep. |
| `0.206.4` | OSDB - Optional Backend Implementation And Dispatch | Only after v0.206.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for OSDB, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.206.5` | OSDB - Final Evidence And Usability Closure | Rerun v0.206.3 public acceptance for every OSDB profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-osdb`. |
| `0.207.0` | komihash - Authority And API Freeze | Authenticate Original komihash versioned author source; freeze every komihash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-komi` before implementation. |
| `0.207.1` | komihash - Complete komihash | After v0.207.0, implement complete komihash in `brynja-hash-noncrypto-komi` with usable public operations, exact parameters and the frozen komihash contract: Pin algorithm revision, 64-bit seed/output and authenticated streaming profiles; explicit byte order, tail and snapshot policy. |
| `0.207.2` | komihash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by komihash: Pin algorithm revision, 64-bit seed/output and authenticated streaming profiles; explicit byte order, tail and snapshot policy; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-komi`. |
| `0.207.3` | komihash - Portable Public API Acceptance | Freeze an independently runnable package-external komihash fixture covering every declared profile and operation in `brynja-hash-noncrypto-komi` before any optimized backend or final evidence sweep. |
| `0.207.4` | komihash - Optional Backend Implementation And Dispatch | Only after v0.207.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for komihash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.207.5` | komihash - Final Evidence And Usability Closure | Rerun v0.207.3 public acceptance for every komihash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-komi`. |
| `0.208.0` | wyhash - Authority And API Freeze | Authenticate Original wyhash versioned specification; freeze every wyhash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-wy` before implementation. |
| `0.208.1` | wyhash - Complete wyhash | After v0.208.0, implement complete wyhash in `brynja-hash-noncrypto-wy` with usable public operations, exact parameters and the frozen wyhash contract: Version-specific seeded one-shot profiles, exact secret-parameter validation/generation port and canonical output; no implicit entropy or cross-version stability. |
| `0.208.2` | wyhash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by wyhash: Version-specific seeded one-shot profiles, exact secret-parameter validation/generation port and canonical output; no implicit entropy or cross-version stability; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-wy`. |
| `0.208.3` | wyhash - Portable Public API Acceptance | Freeze an independently runnable package-external wyhash fixture covering every declared profile and operation in `brynja-hash-noncrypto-wy` before any optimized backend or final evidence sweep. |
| `0.208.4` | wyhash - Optional Backend Implementation And Dispatch | Only after v0.208.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for wyhash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.208.5` | wyhash - Final Evidence And Usability Closure | Rerun v0.208.3 public acceptance for every wyhash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-wy`. |
| `0.209.0` | rapidhash - Authority And API Freeze | Authenticate Original rapidhash versioned source; freeze every rapidhash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-rapid` before implementation. |
| `0.209.1` | rapidhash - Complete rapidhash | After v0.209.0, implement complete rapidhash in `brynja-hash-noncrypto-rapid` with usable public operations, exact parameters and the frozen rapidhash contract: Exact admitted revision and seeded/unseeded fast profiles, fixed-input API with explicit stable encoding and no hidden process seed. |
| `0.209.2` | rapidhash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by rapidhash: Exact admitted revision and seeded/unseeded fast profiles, fixed-input API with explicit stable encoding and no hidden process seed; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-rapid`. |
| `0.209.3` | rapidhash - Portable Public API Acceptance | Freeze an independently runnable package-external rapidhash fixture covering every declared profile and operation in `brynja-hash-noncrypto-rapid` before any optimized backend or final evidence sweep. |
| `0.209.4` | rapidhash - Optional Backend Implementation And Dispatch | Only after v0.209.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for rapidhash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.209.5` | rapidhash - Final Evidence And Usability Closure | Rerun v0.209.3 public acceptance for every rapidhash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-rapid`. |
| `0.210.0` | SeaHash - Authority And API Freeze | Authenticate Original SeaHash algorithm specification; freeze every SeaHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-sea` before implementation. |
| `0.210.1` | SeaHash - Complete SeaHash | After v0.210.0, implement complete SeaHash in `brynja-hash-noncrypto-sea` with usable public operations, exact parameters and the frozen SeaHash contract: Exact four-seed state, incremental and one-shot interfaces, reset and stable output encoding; document invertible mixing is not cryptographic security. |
| `0.210.2` | SeaHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by SeaHash: Exact four-seed state, incremental and one-shot interfaces, reset and stable output encoding; document invertible mixing is not cryptographic security; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-sea`. |
| `0.210.3` | SeaHash - Portable Public API Acceptance | Freeze an independently runnable package-external SeaHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-sea` before any optimized backend or final evidence sweep. |
| `0.210.4` | SeaHash - Optional Backend Implementation And Dispatch | Only after v0.210.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for SeaHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.210.5` | SeaHash - Final Evidence And Usability Closure | Rerun v0.210.3 public acceptance for every SeaHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-sea`. |
| `0.211.0` | FxHash - Authority And API Freeze | Authenticate Versioned upstream FxHash definitions, not an unversioned Rust trait promise; freeze every FxHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-fx` before implementation. |
| `0.211.1` | FxHash - Complete FxHash | After v0.211.0, implement complete FxHash in `brynja-hash-noncrypto-fx` with usable public operations, exact parameters and the frozen FxHash contract: Freeze original/revised FxHash profiles and 32/64-bit word behavior; distinguish bytes from Rust Hasher integer-write encodings and require explicit stable serialization. |
| `0.211.2` | FxHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by FxHash: Freeze original/revised FxHash profiles and 32/64-bit word behavior; distinguish bytes from Rust Hasher integer-write encodings and require explicit stable serialization; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-fx`. |
| `0.211.3` | FxHash - Portable Public API Acceptance | Freeze an independently runnable package-external FxHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-fx` before any optimized backend or final evidence sweep. |
| `0.211.4` | FxHash - Optional Backend Implementation And Dispatch | Only after v0.211.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for FxHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.211.5` | FxHash - Final Evidence And Usability Closure | Rerun v0.211.3 public acceptance for every FxHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-fx`. |
| `0.212.0` | aHash - Authority And API Freeze | Authenticate Versioned aHash source and documented security scope; freeze every aHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-ahash` before implementation. |
| `0.212.1` | aHash - Complete aHash | After v0.212.0, implement complete aHash in `brynja-hash-noncrypto-ahash` with usable public operations, exact parameters and the frozen aHash contract: Freeze keyed AES and fallback profiles, random-state port and deterministic seeded constructor; no stable persisted fingerprint or cryptographic MAC claim; secret-owned seed cleanup. |
| `0.212.2` | aHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by aHash: Freeze keyed AES and fallback profiles, random-state port and deterministic seeded constructor; no stable persisted fingerprint or cryptographic MAC claim; secret-owned seed cleanup; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-ahash`. |
| `0.212.3` | aHash - Portable Public API Acceptance | Freeze an independently runnable package-external aHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-ahash` before any optimized backend or final evidence sweep. |
| `0.212.4` | aHash - Optional Backend Implementation And Dispatch | Only after v0.212.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for aHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.212.5` | aHash - Final Evidence And Usability Closure | Rerun v0.212.3 public acceptance for every aHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-ahash`. |
| `0.213.0` | MeowHash - Authority And API Freeze | Authenticate Original MeowHash source and published attack status; freeze every MeowHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-meow` before implementation. |
| `0.213.1` | MeowHash - Complete MeowHash | After v0.213.0, implement complete MeowHash in `brynja-research-hash-meow` with usable public operations, exact parameters and the frozen MeowHash contract: Exact AES-based 128-bit hash with explicit seed owner and scalar AES-round reference; pinned revision, no MAC or collision-security trait. |
| `0.213.2` | MeowHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MeowHash: Exact AES-based 128-bit hash with explicit seed owner and scalar AES-round reference; pinned revision, no MAC or collision-security trait; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-meow`. |
| `0.213.3` | MeowHash - Portable Public API Acceptance | Freeze an independently runnable package-external MeowHash fixture covering every declared profile and operation in `brynja-research-hash-meow` before any optimized backend or final evidence sweep. |
| `0.213.4` | MeowHash - Optional Backend Implementation And Dispatch | Only after v0.213.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MeowHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.213.5` | MeowHash - Final Evidence And Usability Closure | Rerun v0.213.3 public acceptance for every MeowHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-meow`. |
| `0.214.0` | CLHash - Authority And API Freeze | Authenticate Original CLHash paper and versioned reference; freeze every CLHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-universal-cl` before implementation. |
| `0.214.1` | CLHash - Complete CLHash | After v0.214.0, implement complete CLHash in `brynja-hash-universal-cl` with usable public operations, exact parameters and the frozen CLHash contract: Exact carry-less universal family, caller-generated key material and bounded input, portable polynomial oracle and optional CLMUL backend; state exact probabilistic assumptions. |
| `0.214.2` | CLHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by CLHash: Exact carry-less universal family, caller-generated key material and bounded input, portable polynomial oracle and optional CLMUL backend; state exact probabilistic assumptions; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-universal-cl`. |
| `0.214.3` | CLHash - Portable Public API Acceptance | Freeze an independently runnable package-external CLHash fixture covering every declared profile and operation in `brynja-hash-universal-cl` before any optimized backend or final evidence sweep. |
| `0.214.4` | CLHash - Optional Backend Implementation And Dispatch | Only after v0.214.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for CLHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.214.5` | CLHash - Final Evidence And Usability Closure | Rerun v0.214.3 public acceptance for every CLHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-universal-cl`. |
| `0.215.0` | PolymurHash - Authority And API Freeze | Authenticate Original PolymurHash specification; freeze every PolymurHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-universal-polymur` before implementation. |
| `0.215.1` | PolymurHash - Complete PolymurHash | After v0.215.0, implement complete PolymurHash in `brynja-hash-universal-polymur` with usable public operations, exact parameters and the frozen PolymurHash contract: Pinned polynomial hash profile, parameter/seed generation with explicit entropy and checked arithmetic; no collision-resistant digest coercion. |
| `0.215.2` | PolymurHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by PolymurHash: Pinned polynomial hash profile, parameter/seed generation with explicit entropy and checked arithmetic; no collision-resistant digest coercion; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-universal-polymur`. |
| `0.215.3` | PolymurHash - Portable Public API Acceptance | Freeze an independently runnable package-external PolymurHash fixture covering every declared profile and operation in `brynja-hash-universal-polymur` before any optimized backend or final evidence sweep. |
| `0.215.4` | PolymurHash - Optional Backend Implementation And Dispatch | Only after v0.215.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for PolymurHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.215.5` | PolymurHash - Final Evidence And Usability Closure | Rerun v0.215.3 public acceptance for every PolymurHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-universal-polymur`. |
| `0.216.0` | MumHash - Authority And API Freeze | Authenticate Original MUM hash versioned source; freeze every MumHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-mum` before implementation. |
| `0.216.1` | MumHash - Complete MumHash | After v0.216.0, implement complete MumHash in `brynja-hash-noncrypto-mum` with usable public operations, exact parameters and the frozen MumHash contract: Exact MUM revision, seed and output widths, explicit multiply-mix arithmetic and byte encoding; distinguish hardware-dependent historical profiles. |
| `0.216.2` | MumHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MumHash: Exact MUM revision, seed and output widths, explicit multiply-mix arithmetic and byte encoding; distinguish hardware-dependent historical profiles; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-mum`. |
| `0.216.3` | MumHash - Portable Public API Acceptance | Freeze an independently runnable package-external MumHash fixture covering every declared profile and operation in `brynja-hash-noncrypto-mum` before any optimized backend or final evidence sweep. |
| `0.216.4` | MumHash - Optional Backend Implementation And Dispatch | Only after v0.216.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MumHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.216.5` | MumHash - Final Evidence And Usability Closure | Rerun v0.216.3 public acceptance for every MumHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-mum`. |
| `0.217.0` | CrapWow - Authority And API Freeze | Authenticate Original CrapWow source and rights review; freeze every CrapWow profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-noncrypto-crapwow` before implementation. |
| `0.217.1` | CrapWow - Complete CrapWow | After v0.217.0, implement complete CrapWow in `brynja-hash-noncrypto-crapwow` with usable public operations, exact parameters and the frozen CrapWow contract: Exact 32-bit seeded byte interface with documented tail and little-endian semantics; weak non-cryptographic use only. |
| `0.217.2` | CrapWow - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by CrapWow: Exact 32-bit seeded byte interface with documented tail and little-endian semantics; weak non-cryptographic use only; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-noncrypto-crapwow`. |
| `0.217.3` | CrapWow - Portable Public API Acceptance | Freeze an independently runnable package-external CrapWow fixture covering every declared profile and operation in `brynja-hash-noncrypto-crapwow` before any optimized backend or final evidence sweep. |
| `0.217.4` | CrapWow - Optional Backend Implementation And Dispatch | Only after v0.217.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for CrapWow, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.217.5` | CrapWow - Final Evidence And Usability Closure | Rerun v0.217.3 public acceptance for every CrapWow profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-noncrypto-crapwow`. |
| `0.218.0` | Original BLAKE - Authority And API Freeze | Authenticate Final original BLAKE submission and errata; freeze every BLAKE-224; BLAKE-256; BLAKE-384; BLAKE-512 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-blake` before implementation. |
| `0.218.1` | Original BLAKE - Complete BLAKE-224/256 | After v0.218.0, implement complete BLAKE-224/256 in `brynja-hash-blake` with usable public operations, exact parameters and the frozen Original BLAKE contract: Four distinct outputs/IVs, byte and normative bit input, salt parameters, checked counters, one-shot/incremental/consuming finalize; no accidental BLAKE2 alias. |
| `0.218.2` | Original BLAKE - Complete BLAKE-384/512 | After v0.218.0, implement complete BLAKE-384/512 in `brynja-hash-blake` with usable public operations, exact parameters and the frozen Original BLAKE contract: Four distinct outputs/IVs, byte and normative bit input, salt parameters, checked counters, one-shot/incremental/consuming finalize; no accidental BLAKE2 alias. |
| `0.218.3` | Original BLAKE - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Original BLAKE: Four distinct outputs/IVs, byte and normative bit input, salt parameters, checked counters, one-shot/incremental/consuming finalize; no accidental BLAKE2 alias; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-blake`. |
| `0.218.4` | Original BLAKE - Portable Public API Acceptance | Freeze an independently runnable package-external Original BLAKE fixture covering every declared profile and operation in `brynja-hash-blake` before any optimized backend or final evidence sweep. |
| `0.218.5` | Original BLAKE - Optional Backend Implementation And Dispatch | Only after v0.218.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Original BLAKE, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.218.6` | Original BLAKE - Final Evidence And Usability Closure | Rerun v0.218.4 public acceptance for every Original BLAKE profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-blake`. |
| `0.219.0` | BLAKE2s - Authority And API Freeze | Authenticate RFC 7693 plus complete BLAKE2 author specification; freeze every BLAKE2s; keyed BLAKE2s profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-blake2` before implementation. |
| `0.219.1` | BLAKE2s - Complete BLAKE2s; keyed BLAKE2s | After v0.219.0, implement complete BLAKE2s; keyed BLAKE2s in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2s contract: Output-size/key/salt/personalization parameter builders, complete sequential byte APIs and separate keyed versus public/hardened states; reuse existing BLAKE2b owner rather than duplicate. |
| `0.219.2` | BLAKE2s - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by BLAKE2s: Output-size/key/salt/personalization parameter builders, complete sequential byte APIs and separate keyed versus public/hardened states; reuse existing BLAKE2b owner rather than duplicate; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-blake2`. |
| `0.219.3` | BLAKE2s - Portable Public API Acceptance | Freeze an independently runnable package-external BLAKE2s fixture covering every declared profile and operation in `brynja-hash-blake2` before any optimized backend or final evidence sweep. |
| `0.219.4` | BLAKE2s - Optional Backend Implementation And Dispatch | Only after v0.219.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for BLAKE2s, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.219.5` | BLAKE2s - Final Evidence And Usability Closure | Rerun v0.219.3 public acceptance for every BLAKE2s profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-blake2`. |
| `0.220.0` | BLAKE2 tree profiles - Authority And API Freeze | Authenticate Complete BLAKE2 tree and bp/sp author specifications; freeze every BLAKE2 tree; BLAKE2bp; BLAKE2sp profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-blake2` before implementation. |
| `0.220.1` | BLAKE2 tree profiles - Complete parameterized BLAKE2b/s node APIs | After v0.220.0, implement complete parameterized BLAKE2b/s node APIs in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2 tree profiles contract: Typed fanout/depth/leaf-length/node-offset/node-depth/inner-length/last-node parameters, bp/sp schedulers and caller-owned bounded workspaces; exact keyed tree root behavior. |
| `0.220.2` | BLAKE2 tree profiles - Complete BLAKE2bp | After v0.220.0, implement complete BLAKE2bp in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2 tree profiles contract: Typed fanout/depth/leaf-length/node-offset/node-depth/inner-length/last-node parameters, bp/sp schedulers and caller-owned bounded workspaces; exact keyed tree root behavior. |
| `0.220.3` | BLAKE2 tree profiles - Complete BLAKE2sp | After v0.220.0, implement complete BLAKE2sp in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2 tree profiles contract: Typed fanout/depth/leaf-length/node-offset/node-depth/inner-length/last-node parameters, bp/sp schedulers and caller-owned bounded workspaces; exact keyed tree root behavior. |
| `0.220.4` | BLAKE2 tree profiles - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by BLAKE2 tree profiles: Typed fanout/depth/leaf-length/node-offset/node-depth/inner-length/last-node parameters, bp/sp schedulers and caller-owned bounded workspaces; exact keyed tree root behavior; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-blake2`. |
| `0.220.5` | BLAKE2 tree profiles - Portable Public API Acceptance | Freeze an independently runnable package-external BLAKE2 tree profiles fixture covering every declared profile and operation in `brynja-hash-blake2` before any optimized backend or final evidence sweep. |
| `0.220.6` | BLAKE2 tree profiles - Optional Backend Implementation And Dispatch | Only after v0.220.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for BLAKE2 tree profiles, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.220.7` | BLAKE2 tree profiles - Final Evidence And Usability Closure | Rerun v0.220.5 public acceptance for every BLAKE2 tree profiles profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-blake2`. |
| `0.221.0` | BLAKE2X - Authority And API Freeze | Authenticate BLAKE2X author specification, not RFC 7693 alone; freeze every BLAKE2X; BLAKE2Xb; BLAKE2Xs profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-blake2` before implementation. |
| `0.221.1` | BLAKE2X - Complete BLAKE2Xb | After v0.221.0, implement complete BLAKE2Xb in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2X contract: Distinct Xb/Xs output-length domains and keyed parameters, consuming root finalization and bounded XOF reader with seek only where specification permits; no alias to bp/sp. |
| `0.221.2` | BLAKE2X - Complete BLAKE2Xs | After v0.221.0, implement complete BLAKE2Xs in `brynja-hash-blake2` with usable public operations, exact parameters and the frozen BLAKE2X contract: Distinct Xb/Xs output-length domains and keyed parameters, consuming root finalization and bounded XOF reader with seek only where specification permits; no alias to bp/sp. |
| `0.221.3` | BLAKE2X - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by BLAKE2X: Distinct Xb/Xs output-length domains and keyed parameters, consuming root finalization and bounded XOF reader with seek only where specification permits; no alias to bp/sp; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-blake2`. |
| `0.221.4` | BLAKE2X - Portable Public API Acceptance | Freeze an independently runnable package-external BLAKE2X fixture covering every declared profile and operation in `brynja-hash-blake2` before any optimized backend or final evidence sweep. |
| `0.221.5` | BLAKE2X - Optional Backend Implementation And Dispatch | Only after v0.221.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for BLAKE2X, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.221.6` | BLAKE2X - Final Evidence And Usability Closure | Rerun v0.221.4 public acceptance for every BLAKE2X profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-blake2`. |
| `0.222.0` | BLAKE3 - Authority And API Freeze | Authenticate BLAKE3 authors specification and official versioned corpus; freeze every BLAKE3; keyed BLAKE3; BLAKE3 derive-key profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-blake3` before implementation. |
| `0.222.1` | BLAKE3 - Complete public hashing and streaming tree stack | After v0.222.0, implement complete public hashing and streaming tree stack in `brynja-hash-blake3` with usable public operations, exact parameters and the frozen BLAKE3 contract: Hash, keyed and derive-key context/material modes separated; streaming and seekable XOF, checked output offsets, caller-owned tree stack and bounded parallel chunk jobs with domain-bound results. |
| `0.222.2` | BLAKE3 - Complete seekable XOF output | After v0.222.0, implement complete seekable XOF output in `brynja-hash-blake3` with usable public operations, exact parameters and the frozen BLAKE3 contract: Hash, keyed and derive-key context/material modes separated; streaming and seekable XOF, checked output offsets, caller-owned tree stack and bounded parallel chunk jobs with domain-bound results. |
| `0.222.3` | BLAKE3 - Complete keyed and derive-key owners | After v0.222.0, implement complete keyed and derive-key owners in `brynja-hash-blake3` with usable public operations, exact parameters and the frozen BLAKE3 contract: Hash, keyed and derive-key context/material modes separated; streaming and seekable XOF, checked output offsets, caller-owned tree stack and bounded parallel chunk jobs with domain-bound results. |
| `0.222.4` | BLAKE3 - Complete bounded parallel chunk jobs and root composition | After v0.222.0, implement complete bounded parallel chunk jobs and root composition in `brynja-hash-blake3` with usable public operations, exact parameters and the frozen BLAKE3 contract: Hash, keyed and derive-key context/material modes separated; streaming and seekable XOF, checked output offsets, caller-owned tree stack and bounded parallel chunk jobs with domain-bound results. |
| `0.222.5` | BLAKE3 - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by BLAKE3: Hash, keyed and derive-key context/material modes separated; streaming and seekable XOF, checked output offsets, caller-owned tree stack and bounded parallel chunk jobs with domain-bound results; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-blake3`. |
| `0.222.6` | BLAKE3 - Portable Public API Acceptance | Freeze an independently runnable package-external BLAKE3 fixture covering every declared profile and operation in `brynja-hash-blake3` before any optimized backend or final evidence sweep. |
| `0.222.7` | BLAKE3 - Optional Backend Implementation And Dispatch | Only after v0.222.6, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for BLAKE3, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.222.8` | BLAKE3 - Final Evidence And Usability Closure | Rerun v0.222.6 public acceptance for every BLAKE3 profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-blake3`. |
| `0.223.0` | TurboSHAKE - Authority And API Freeze | Authenticate RFC 9861 and authenticated errata; freeze every TurboSHAKE128; TurboSHAKE256 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-turboshake` before implementation. |
| `0.223.1` | TurboSHAKE - Complete TurboSHAKE128 | After v0.223.0, implement complete TurboSHAKE128 in `brynja-hash-turboshake` with usable public operations, exact parameters and the frozen TurboSHAKE contract: Distinct 128/256 XOF types over exact 12-round permutation, validated domain byte and byte input; consuming squeeze, checked reader output and typed secret destination. |
| `0.223.2` | TurboSHAKE - Complete TurboSHAKE256 | After v0.223.0, implement complete TurboSHAKE256 in `brynja-hash-turboshake` with usable public operations, exact parameters and the frozen TurboSHAKE contract: Distinct 128/256 XOF types over exact 12-round permutation, validated domain byte and byte input; consuming squeeze, checked reader output and typed secret destination. |
| `0.223.3` | TurboSHAKE - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by TurboSHAKE: Distinct 128/256 XOF types over exact 12-round permutation, validated domain byte and byte input; consuming squeeze, checked reader output and typed secret destination; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-turboshake`. |
| `0.223.4` | TurboSHAKE - Portable Public API Acceptance | Freeze an independently runnable package-external TurboSHAKE fixture covering every declared profile and operation in `brynja-hash-turboshake` before any optimized backend or final evidence sweep. |
| `0.223.5` | TurboSHAKE - Optional Backend Implementation And Dispatch | Only after v0.223.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for TurboSHAKE, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.223.6` | TurboSHAKE - Final Evidence And Usability Closure | Rerun v0.223.4 public acceptance for every TurboSHAKE profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-turboshake`. |
| `0.224.0` | KangarooTwelve - Authority And API Freeze | Authenticate RFC 9861, Sakura framing and author vectors; freeze every KT128; KT256; KangarooTwelve profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-kangaroo` before implementation. |
| `0.224.1` | KangarooTwelve - Complete KT128 | After v0.224.0, implement complete KT128 in `brynja-hash-kangaroo` with usable public operations, exact parameters and the frozen KangarooTwelve contract: Distinct customization-aware KT128/256 tree XOF, exact length_encode and chunk framing, streaming customization lifecycle and bounded parallel jobs; reuse TurboSHAKE owner. |
| `0.224.2` | KangarooTwelve - Complete KT256 | After v0.224.0, implement complete KT256 in `brynja-hash-kangaroo` with usable public operations, exact parameters and the frozen KangarooTwelve contract: Distinct customization-aware KT128/256 tree XOF, exact length_encode and chunk framing, streaming customization lifecycle and bounded parallel jobs; reuse TurboSHAKE owner. |
| `0.224.3` | KangarooTwelve - Complete bounded parallel tree and customization lifecycle | After v0.224.0, implement complete bounded parallel tree and customization lifecycle in `brynja-hash-kangaroo` with usable public operations, exact parameters and the frozen KangarooTwelve contract: Distinct customization-aware KT128/256 tree XOF, exact length_encode and chunk framing, streaming customization lifecycle and bounded parallel jobs; reuse TurboSHAKE owner. |
| `0.224.4` | KangarooTwelve - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by KangarooTwelve: Distinct customization-aware KT128/256 tree XOF, exact length_encode and chunk framing, streaming customization lifecycle and bounded parallel jobs; reuse TurboSHAKE owner; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-kangaroo`. |
| `0.224.5` | KangarooTwelve - Portable Public API Acceptance | Freeze an independently runnable package-external KangarooTwelve fixture covering every declared profile and operation in `brynja-hash-kangaroo` before any optimized backend or final evidence sweep. |
| `0.224.6` | KangarooTwelve - Optional Backend Implementation And Dispatch | Only after v0.224.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for KangarooTwelve, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.224.7` | KangarooTwelve - Final Evidence And Usability Closure | Rerun v0.224.5 public acceptance for every KangarooTwelve profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-kangaroo`. |
| `0.225.0` | Ascon - Authority And API Freeze | Authenticate NIST SP 800-232 final and current errata; freeze every Ascon-Hash256; Ascon-XOF128; Ascon-CXOF128 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-ascon` before implementation. |
| `0.225.1` | Ascon - Complete Ascon-Hash256 | After v0.225.0, implement complete Ascon-Hash256 in `brynja-hash-ascon` with usable public operations, exact parameters and the frozen Ascon contract: Final SP 800-232 identities and bit ordering, fixed/XOF/customized types, streaming and specified bit input/output, typed customization limits; separate from submission-era Ascon variants. |
| `0.225.2` | Ascon - Complete Ascon-XOF128 | After v0.225.0, implement complete Ascon-XOF128 in `brynja-hash-ascon` with usable public operations, exact parameters and the frozen Ascon contract: Final SP 800-232 identities and bit ordering, fixed/XOF/customized types, streaming and specified bit input/output, typed customization limits; separate from submission-era Ascon variants. |
| `0.225.3` | Ascon - Complete Ascon-CXOF128 | After v0.225.0, implement complete Ascon-CXOF128 in `brynja-hash-ascon` with usable public operations, exact parameters and the frozen Ascon contract: Final SP 800-232 identities and bit ordering, fixed/XOF/customized types, streaming and specified bit input/output, typed customization limits; separate from submission-era Ascon variants. |
| `0.225.4` | Ascon - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Ascon: Final SP 800-232 identities and bit ordering, fixed/XOF/customized types, streaming and specified bit input/output, typed customization limits; separate from submission-era Ascon variants; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-ascon`. |
| `0.225.5` | Ascon - Portable Public API Acceptance | Freeze an independently runnable package-external Ascon fixture covering every declared profile and operation in `brynja-hash-ascon` before any optimized backend or final evidence sweep. |
| `0.225.6` | Ascon - Optional Backend Implementation And Dispatch | Only after v0.225.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Ascon, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.225.7` | Ascon - Final Evidence And Usability Closure | Rerun v0.225.5 public acceptance for every Ascon profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-ascon`. |
| `0.226.0` | Grostl - Authority And API Freeze | Authenticate Final Grøstl SHA-3 submission; freeze every Grostl; Grøstl profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-grostl` before implementation. |
| `0.226.1` | Grostl - Complete Grostl; Grøstl | After v0.226.0, implement complete Grostl; Grøstl in `brynja-hash-grostl` with usable public operations, exact parameters and the frozen Grostl contract: All final-submission output lengths, 512/1024-bit state domains, bit input and checked block counters, one-shot/streaming/consuming output. |
| `0.226.2` | Grostl - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Grostl: All final-submission output lengths, 512/1024-bit state domains, bit input and checked block counters, one-shot/streaming/consuming output; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-grostl`. |
| `0.226.3` | Grostl - Portable Public API Acceptance | Freeze an independently runnable package-external Grostl fixture covering every declared profile and operation in `brynja-hash-grostl` before any optimized backend or final evidence sweep. |
| `0.226.4` | Grostl - Optional Backend Implementation And Dispatch | Only after v0.226.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Grostl, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.226.5` | Grostl - Final Evidence And Usability Closure | Rerun v0.226.3 public acceptance for every Grostl profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-grostl`. |
| `0.227.0` | JH - Authority And API Freeze | Authenticate Final JH SHA-3 submission and errata; freeze every JH profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-jh` before implementation. |
| `0.227.1` | JH - Complete JH | After v0.227.0, implement complete JH in `brynja-hash-jh` with usable public operations, exact parameters and the frozen JH contract: Distinct 224/256/384/512 IV/output profiles, normative bit input, checked length and streaming/consuming digest APIs. |
| `0.227.2` | JH - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by JH: Distinct 224/256/384/512 IV/output profiles, normative bit input, checked length and streaming/consuming digest APIs; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-jh`. |
| `0.227.3` | JH - Portable Public API Acceptance | Freeze an independently runnable package-external JH fixture covering every declared profile and operation in `brynja-hash-jh` before any optimized backend or final evidence sweep. |
| `0.227.4` | JH - Optional Backend Implementation And Dispatch | Only after v0.227.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for JH, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.227.5` | JH - Final Evidence And Usability Closure | Rerun v0.227.3 public acceptance for every JH profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-jh`. |
| `0.228.0` | LSH - Authority And API Freeze | Authenticate Authenticated LSH national standard and author reference; freeze every LSH; LSH-256; LSH-512 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-lsh` before implementation. |
| `0.228.1` | LSH - Complete LSH-256 profiles | After v0.228.0, implement complete LSH-256 profiles in `brynja-hash-lsh` with usable public operations, exact parameters and the frozen LSH contract: Typed LSH-256/512 and every standard output-length profile, canonical bit input/output, checked lengths and incremental state; preserve non-byte output masks. |
| `0.228.2` | LSH - Complete LSH-512 profiles | After v0.228.0, implement complete LSH-512 profiles in `brynja-hash-lsh` with usable public operations, exact parameters and the frozen LSH contract: Typed LSH-256/512 and every standard output-length profile, canonical bit input/output, checked lengths and incremental state; preserve non-byte output masks. |
| `0.228.3` | LSH - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by LSH: Typed LSH-256/512 and every standard output-length profile, canonical bit input/output, checked lengths and incremental state; preserve non-byte output masks; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-lsh`. |
| `0.228.4` | LSH - Portable Public API Acceptance | Freeze an independently runnable package-external LSH fixture covering every declared profile and operation in `brynja-hash-lsh` before any optimized backend or final evidence sweep. |
| `0.228.5` | LSH - Optional Backend Implementation And Dispatch | Only after v0.228.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for LSH, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.228.6` | LSH - Final Evidence And Usability Closure | Rerun v0.228.4 public acceptance for every LSH profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-lsh`. |
| `0.229.0` | Kupyna - Authority And API Freeze | Authenticate DSTU 7564 authenticated edition and lawful vectors; freeze every Kupyna; DSTU 7564 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-kupyna` before implementation. |
| `0.229.1` | Kupyna - Complete Kupyna; DSTU 7564 | After v0.229.0, implement complete Kupyna; DSTU 7564 in `brynja-hash-kupyna` with usable public operations, exact parameters and the frozen Kupyna contract: Exact 256/384/512 and any additional standard output profiles, distinct permutation sizes, bit/endian conventions, streaming and checked length domain. |
| `0.229.2` | Kupyna - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Kupyna: Exact 256/384/512 and any additional standard output profiles, distinct permutation sizes, bit/endian conventions, streaming and checked length domain; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-kupyna`. |
| `0.229.3` | Kupyna - Portable Public API Acceptance | Freeze an independently runnable package-external Kupyna fixture covering every declared profile and operation in `brynja-hash-kupyna` before any optimized backend or final evidence sweep. |
| `0.229.4` | Kupyna - Optional Backend Implementation And Dispatch | Only after v0.229.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Kupyna, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.229.5` | Kupyna - Final Evidence And Usability Closure | Rerun v0.229.3 public acceptance for every Kupyna profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-kupyna`. |
| `0.230.0` | Skein - Authority And API Freeze | Authenticate Final Skein specification and Threefish authority; freeze every Skein profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-skein` before implementation. |
| `0.230.1` | Skein - Complete UBI and Skein-256 | After v0.230.0, implement complete UBI and Skein-256 in `brynja-hash-skein` with usable public operations, exact parameters and the frozen Skein contract: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners. |
| `0.230.2` | Skein - Complete Skein-512 | After v0.230.0, implement complete Skein-512 in `brynja-hash-skein` with usable public operations, exact parameters and the frozen Skein contract: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners. |
| `0.230.3` | Skein - Complete Skein-1024 | After v0.230.0, implement complete Skein-1024 in `brynja-hash-skein` with usable public operations, exact parameters and the frozen Skein contract: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners. |
| `0.230.4` | Skein - Complete keyed and optional-parameter domains | After v0.230.0, implement complete keyed and optional-parameter domains in `brynja-hash-skein` with usable public operations, exact parameters and the frozen Skein contract: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners. |
| `0.230.5` | Skein - Complete bounded tree and output modes | After v0.230.0, implement complete bounded tree and output modes in `brynja-hash-skein` with usable public operations, exact parameters and the frozen Skein contract: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners. |
| `0.230.6` | Skein - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Skein: Skein-256/512/1024 with complete UBI configuration, variable output and typed key/personalization/public-key/key-id/nonce parameters; separate sequential/tree and keyed owners; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-skein`. |
| `0.230.7` | Skein - Portable Public API Acceptance | Freeze an independently runnable package-external Skein fixture covering every declared profile and operation in `brynja-hash-skein` before any optimized backend or final evidence sweep. |
| `0.230.8` | Skein - Optional Backend Implementation And Dispatch | Only after v0.230.7, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Skein, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.230.9` | Skein - Final Evidence And Usability Closure | Rerun v0.230.7 public acceptance for every Skein profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-skein`. |
| `0.231.0` | Whirlpool - Authority And API Freeze | Authenticate Authenticated ISO edition and Whirlpool author revision history; freeze every Whirlpool profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-hash-whirlpool` before implementation. |
| `0.231.1` | Whirlpool - Complete Whirlpool | After v0.231.0, implement complete Whirlpool in `brynja-hash-whirlpool` with usable public operations, exact parameters and the frozen Whirlpool contract: Current Whirlpool revision distinct from Whirlpool-0/T; normative bit input, checked 256-bit length encoding and streaming/fixed output; historical revisions separately isolated. |
| `0.231.2` | Whirlpool - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Whirlpool: Current Whirlpool revision distinct from Whirlpool-0/T; normative bit input, checked 256-bit length encoding and streaming/fixed output; historical revisions separately isolated; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-hash-whirlpool`. |
| `0.231.3` | Whirlpool - Portable Public API Acceptance | Freeze an independently runnable package-external Whirlpool fixture covering every declared profile and operation in `brynja-hash-whirlpool` before any optimized backend or final evidence sweep. |
| `0.231.4` | Whirlpool - Optional Backend Implementation And Dispatch | Only after v0.231.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Whirlpool, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.231.5` | Whirlpool - Final Evidence And Usability Closure | Rerun v0.231.3 public acceptance for every Whirlpool profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-hash-whirlpool`. |
| `0.232.0` | Xoodyak - Authority And API Freeze | Authenticate Final Xoodyak submission and Xoodoo permutation definition; freeze every Xoodyak; Xoodoo-based hashing profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-xoodyak` before implementation. |
| `0.232.1` | Xoodyak - Complete Xoodyak; Xoodoo-based hashing | After v0.232.0, implement complete Xoodyak; Xoodoo-based hashing in `brynja-research-hash-xoodyak` with usable public operations, exact parameters and the frozen Xoodyak contract: Exact submitted Xoodyak hash mode over shared first-party Xoodoo, byte/bit domain only as specified, absorb/squeeze lifecycle; keyed Cyclist modes require separately numbered construction, not implicit hash capability. |
| `0.232.2` | Xoodyak - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Xoodyak: Exact submitted Xoodyak hash mode over shared first-party Xoodoo, byte/bit domain only as specified, absorb/squeeze lifecycle; keyed Cyclist modes require separately numbered construction, not implicit hash capability; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-xoodyak`. |
| `0.232.3` | Xoodyak - Portable Public API Acceptance | Freeze an independently runnable package-external Xoodyak fixture covering every declared profile and operation in `brynja-research-hash-xoodyak` before any optimized backend or final evidence sweep. |
| `0.232.4` | Xoodyak - Optional Backend Implementation And Dispatch | Only after v0.232.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Xoodyak, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.232.5` | Xoodyak - Final Evidence And Usability Closure | Rerun v0.232.3 public acceptance for every Xoodyak profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-xoodyak`. |
| `0.233.0` | Haraka - Authority And API Freeze | Authenticate Haraka original/revised specification and attacks; freeze every Haraka profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-haraka` before implementation. |
| `0.233.1` | Haraka - Complete Haraka | After v0.233.0, implement complete Haraka in `brynja-research-hash-haraka` with usable public operations, exact parameters and the frozen Haraka contract: Separate versioned Haraka-256/256 and 512/256 fixed-input functions; reject arbitrary lengths, no streaming Digest implementation; scalar AES-round reference. |
| `0.233.2` | Haraka - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Haraka: Separate versioned Haraka-256/256 and 512/256 fixed-input functions; reject arbitrary lengths, no streaming Digest implementation; scalar AES-round reference; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-haraka`. |
| `0.233.3` | Haraka - Portable Public API Acceptance | Freeze an independently runnable package-external Haraka fixture covering every declared profile and operation in `brynja-research-hash-haraka` before any optimized backend or final evidence sweep. |
| `0.233.4` | Haraka - Optional Backend Implementation And Dispatch | Only after v0.233.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Haraka, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.233.5` | Haraka - Final Evidence And Usability Closure | Rerun v0.233.3 public acceptance for every Haraka profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-haraka`. |
| `0.234.0` | Gimli hash - Authority And API Freeze | Authenticate Gimli authors permutation and exact hash-mode publication; freeze every Gimli-based hash modes profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-gimli` before implementation. |
| `0.234.1` | Gimli hash - Complete Gimli-based hash modes | After v0.234.0, implement complete Gimli-based hash modes in `brynja-research-hash-gimli` with usable public operations, exact parameters and the frozen Gimli hash contract: Freeze exact Gimli permutation and hash/sponge mode, rate/padding/output before APIs; no generic permutation-to-secure-hash inference. |
| `0.234.2` | Gimli hash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Gimli hash: Freeze exact Gimli permutation and hash/sponge mode, rate/padding/output before APIs; no generic permutation-to-secure-hash inference; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-gimli`. |
| `0.234.3` | Gimli hash - Portable Public API Acceptance | Freeze an independently runnable package-external Gimli hash fixture covering every declared profile and operation in `brynja-research-hash-gimli` before any optimized backend or final evidence sweep. |
| `0.234.4` | Gimli hash - Optional Backend Implementation And Dispatch | Only after v0.234.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Gimli hash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.234.5` | Gimli hash - Final Evidence And Usability Closure | Rerun v0.234.3 public acceptance for every Gimli hash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-gimli`. |
| `0.235.0` | ECOH - Authority And API Freeze | Authenticate ECOH submission and public cryptanalysis; freeze every ECOH profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-ecoh` before implementation. |
| `0.235.1` | ECOH - Complete ECOH | After v0.235.0, implement complete ECOH in `brynja-research-hash-ecoh` with usable public operations, exact parameters and the frozen ECOH contract: Each authenticated ECOH submission parameter set with exact binary-field curve owner, point mapping, counters and digest API; no prime-field substitution. |
| `0.235.2` | ECOH - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by ECOH: Each authenticated ECOH submission parameter set with exact binary-field curve owner, point mapping, counters and digest API; no prime-field substitution; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-ecoh`. |
| `0.235.3` | ECOH - Portable Public API Acceptance | Freeze an independently runnable package-external ECOH fixture covering every declared profile and operation in `brynja-research-hash-ecoh` before any optimized backend or final evidence sweep. |
| `0.235.4` | ECOH - Optional Backend Implementation And Dispatch | Only after v0.235.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for ECOH, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.235.5` | ECOH - Final Evidence And Usability Closure | Rerun v0.235.3 public acceptance for every ECOH profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-ecoh`. |
| `0.236.0` | FSB - Authority And API Freeze | Authenticate Final FSB submission and parameter-generation sources; freeze every FSB profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-fsb` before implementation. |
| `0.236.1` | FSB - Complete FSB | After v0.236.0, implement complete FSB in `brynja-research-hash-fsb` with usable public operations, exact parameters and the frozen FSB contract: Exact FSB parameter sets and the accepted Whirlpool finalization owner, bounded matrix generation/storage and normative input/digest profile. |
| `0.236.2` | FSB - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by FSB: Exact FSB parameter sets and the accepted Whirlpool finalization owner, bounded matrix generation/storage and normative input/digest profile; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-fsb`. |
| `0.236.3` | FSB - Portable Public API Acceptance | Freeze an independently runnable package-external FSB fixture covering every declared profile and operation in `brynja-research-hash-fsb` before any optimized backend or final evidence sweep. |
| `0.236.4` | FSB - Optional Backend Implementation And Dispatch | Only after v0.236.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for FSB, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.236.5` | FSB - Final Evidence And Usability Closure | Rerun v0.236.3 public acceptance for every FSB profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-fsb`. |
| `0.237.0` | RadioGatun - Authority And API Freeze | Authenticate RadioGatún original specification and security analysis; freeze every RadioGatún profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-radiogatun` before implementation. |
| `0.237.1` | RadioGatun - Complete RadioGatún | After v0.237.0, implement complete RadioGatún in `brynja-research-hash-radiogatun` with usable public operations, exact parameters and the frozen RadioGatun contract: Separate 32/64-bit word profiles, exact belt/mill padding and streaming output domain; no inferred MAC API. |
| `0.237.2` | RadioGatun - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by RadioGatun: Separate 32/64-bit word profiles, exact belt/mill padding and streaming output domain; no inferred MAC API; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-radiogatun`. |
| `0.237.3` | RadioGatun - Portable Public API Acceptance | Freeze an independently runnable package-external RadioGatun fixture covering every declared profile and operation in `brynja-research-hash-radiogatun` before any optimized backend or final evidence sweep. |
| `0.237.4` | RadioGatun - Optional Backend Implementation And Dispatch | Only after v0.237.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for RadioGatun, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.237.5` | RadioGatun - Final Evidence And Usability Closure | Rerun v0.237.3 public acceptance for every RadioGatun profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-radiogatun`. |
| `0.238.0` | Spectral Hash - Authority And API Freeze | Authenticate Original Spectral Hash publication, rights and specification admission required; freeze every Spectral Hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-spectral` before implementation. |
| `0.238.1` | Spectral Hash - Complete Spectral Hash | After v0.238.0, implement complete Spectral Hash in `brynja-research-hash-spectral` with usable public operations, exact parameters and the frozen Spectral Hash contract: Authenticate exact Spectral Hash design/revision and parameters before byte/bit and output contracts; source-blocked means no invented compatibility implementation. |
| `0.238.2` | Spectral Hash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Spectral Hash: Authenticate exact Spectral Hash design/revision and parameters before byte/bit and output contracts; source-blocked means no invented compatibility implementation; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-spectral`. |
| `0.238.3` | Spectral Hash - Portable Public API Acceptance | Freeze an independently runnable package-external Spectral Hash fixture covering every declared profile and operation in `brynja-research-hash-spectral` before any optimized backend or final evidence sweep. |
| `0.238.4` | Spectral Hash - Optional Backend Implementation And Dispatch | Only after v0.238.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Spectral Hash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.238.5` | Spectral Hash - Final Evidence And Usability Closure | Rerun v0.238.3 public acceptance for every Spectral Hash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-spectral`. |
| `0.239.0` | SWIFFT - Authority And API Freeze | Authenticate Original SWIFFT publication and reference parameters; freeze every SWIFFT profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-swifft` before implementation. |
| `0.239.1` | SWIFFT - Complete SWIFFT | After v0.239.0, implement complete SWIFFT in `brynja-research-hash-swifft` with usable public operations, exact parameters and the frozen SWIFFT contract: Fixed-domain compression with exact ring/modulus/matrix and output encoding, checked input widths; expose no general streaming digest absent a separately authenticated construction. |
| `0.239.2` | SWIFFT - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by SWIFFT: Fixed-domain compression with exact ring/modulus/matrix and output encoding, checked input widths; expose no general streaming digest absent a separately authenticated construction; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-swifft`. |
| `0.239.3` | SWIFFT - Portable Public API Acceptance | Freeze an independently runnable package-external SWIFFT fixture covering every declared profile and operation in `brynja-research-hash-swifft` before any optimized backend or final evidence sweep. |
| `0.239.4` | SWIFFT - Optional Backend Implementation And Dispatch | Only after v0.239.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for SWIFFT, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.239.5` | SWIFFT - Final Evidence And Usability Closure | Rerun v0.239.3 public acceptance for every SWIFFT profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-swifft`. |
| `0.240.0` | VSH - Authority And API Freeze | Authenticate Original VSH paper and security revisions; freeze every VSH profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-vsh` before implementation. |
| `0.240.1` | VSH - Complete VSH | After v0.240.0, implement complete VSH in `brynja-research-hash-vsh` with usable public operations, exact parameters and the frozen VSH contract: Exact VSH modulus/prime table/length and compression modes with bounded first-party modular arithmetic; distinguish collision assumptions from general hash security. |
| `0.240.2` | VSH - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by VSH: Exact VSH modulus/prime table/length and compression modes with bounded first-party modular arithmetic; distinguish collision assumptions from general hash security; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-vsh`. |
| `0.240.3` | VSH - Portable Public API Acceptance | Freeze an independently runnable package-external VSH fixture covering every declared profile and operation in `brynja-research-hash-vsh` before any optimized backend or final evidence sweep. |
| `0.240.4` | VSH - Optional Backend Implementation And Dispatch | Only after v0.240.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for VSH, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.240.5` | VSH - Final Evidence And Usability Closure | Rerun v0.240.3 public acceptance for every VSH profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-vsh`. |
| `0.241.0` | MD4 and SHA-0 - Authority And API Freeze | Authenticate RFC 1320 and original Secure Hash Standard SHA-0 edition; freeze every MD4; SHA-0 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-classic` before implementation. |
| `0.241.1` | MD4 and SHA-0 - Complete MD4 | After v0.241.0, implement complete MD4 in `brynja-legacy-hash-classic` with usable public operations, exact parameters and the frozen MD4 and SHA-0 contract: Distinct complete MD4 and SHA-0 types, one-shot/streaming/normative bit handling and consuming output, explicit compatibility policy; never alias MD5/SHA-1 owners. |
| `0.241.2` | MD4 and SHA-0 - Complete SHA-0 | After v0.241.0, implement complete SHA-0 in `brynja-legacy-hash-classic` with usable public operations, exact parameters and the frozen MD4 and SHA-0 contract: Distinct complete MD4 and SHA-0 types, one-shot/streaming/normative bit handling and consuming output, explicit compatibility policy; never alias MD5/SHA-1 owners. |
| `0.241.3` | MD4 and SHA-0 - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MD4 and SHA-0: Distinct complete MD4 and SHA-0 types, one-shot/streaming/normative bit handling and consuming output, explicit compatibility policy; never alias MD5/SHA-1 owners; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-classic`. |
| `0.241.4` | MD4 and SHA-0 - Portable Public API Acceptance | Freeze an independently runnable package-external MD4 and SHA-0 fixture covering every declared profile and operation in `brynja-legacy-hash-classic` before any optimized backend or final evidence sweep. |
| `0.241.5` | MD4 and SHA-0 - Optional Backend Implementation And Dispatch | Only after v0.241.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MD4 and SHA-0, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.241.6` | MD4 and SHA-0 - Final Evidence And Usability Closure | Rerun v0.241.4 public acceptance for every MD4 and SHA-0 profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-classic`. |
| `0.242.0` | HAS-160 - Authority And API Freeze | Authenticate HAS-160 original standard and security analysis; freeze every HAS-160 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-has160` before implementation. |
| `0.242.1` | HAS-160 - Complete HAS-160 | After v0.242.0, implement complete HAS-160 in `brynja-legacy-hash-has160` with usable public operations, exact parameters and the frozen HAS-160 contract: Exact bit/byte length, IV, output order and streaming/fixed digest types behind explicit compatibility selection. |
| `0.242.2` | HAS-160 - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by HAS-160: Exact bit/byte length, IV, output order and streaming/fixed digest types behind explicit compatibility selection; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-has160`. |
| `0.242.3` | HAS-160 - Portable Public API Acceptance | Freeze an independently runnable package-external HAS-160 fixture covering every declared profile and operation in `brynja-legacy-hash-has160` before any optimized backend or final evidence sweep. |
| `0.242.4` | HAS-160 - Optional Backend Implementation And Dispatch | Only after v0.242.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for HAS-160, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.242.5` | HAS-160 - Final Evidence And Usability Closure | Rerun v0.242.3 public acceptance for every HAS-160 profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-has160`. |
| `0.243.0` | HAVAL - Authority And API Freeze | Authenticate HAVAL original specification and errata; freeze every HAVAL profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-haval` before implementation. |
| `0.243.1` | HAVAL - Complete three-pass output profiles | After v0.243.0, implement complete three-pass output profiles in `brynja-legacy-hash-haval` with usable public operations, exact parameters and the frozen HAVAL contract: All 3/4/5-pass and 128/160/192/224/256-bit combinations with distinct validated parameters, exact padding/tailoring and streaming digest. |
| `0.243.2` | HAVAL - Complete four-pass output profiles | After v0.243.0, implement complete four-pass output profiles in `brynja-legacy-hash-haval` with usable public operations, exact parameters and the frozen HAVAL contract: All 3/4/5-pass and 128/160/192/224/256-bit combinations with distinct validated parameters, exact padding/tailoring and streaming digest. |
| `0.243.3` | HAVAL - Complete five-pass output profiles | After v0.243.0, implement complete five-pass output profiles in `brynja-legacy-hash-haval` with usable public operations, exact parameters and the frozen HAVAL contract: All 3/4/5-pass and 128/160/192/224/256-bit combinations with distinct validated parameters, exact padding/tailoring and streaming digest. |
| `0.243.4` | HAVAL - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by HAVAL: All 3/4/5-pass and 128/160/192/224/256-bit combinations with distinct validated parameters, exact padding/tailoring and streaming digest; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-haval`. |
| `0.243.5` | HAVAL - Portable Public API Acceptance | Freeze an independently runnable package-external HAVAL fixture covering every declared profile and operation in `brynja-legacy-hash-haval` before any optimized backend or final evidence sweep. |
| `0.243.6` | HAVAL - Optional Backend Implementation And Dispatch | Only after v0.243.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for HAVAL, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.243.7` | HAVAL - Final Evidence And Usability Closure | Rerun v0.243.5 public acceptance for every HAVAL profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-haval`. |
| `0.244.0` | MD6 - Authority And API Freeze | Authenticate MD6 final submission and security analyses; freeze every MD6; keyed MD6 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-md6` before implementation. |
| `0.244.1` | MD6 - Complete sequential compression and output | After v0.244.0, implement complete sequential compression and output in `brynja-research-hash-md6` with usable public operations, exact parameters and the frozen MD6 contract: Exact d/r/L/tree parameters, keyed owner, sequential/tree output, bounded caller workspace and domain-bound jobs; do not imply every reduced-round parameter is secure. |
| `0.244.2` | MD6 - Complete bounded tree scheduler | After v0.244.0, implement complete bounded tree scheduler in `brynja-research-hash-md6` with usable public operations, exact parameters and the frozen MD6 contract: Exact d/r/L/tree parameters, keyed owner, sequential/tree output, bounded caller workspace and domain-bound jobs; do not imply every reduced-round parameter is secure. |
| `0.244.3` | MD6 - Complete keyed parameter domains | After v0.244.0, implement complete keyed parameter domains in `brynja-research-hash-md6` with usable public operations, exact parameters and the frozen MD6 contract: Exact d/r/L/tree parameters, keyed owner, sequential/tree output, bounded caller workspace and domain-bound jobs; do not imply every reduced-round parameter is secure. |
| `0.244.4` | MD6 - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MD6: Exact d/r/L/tree parameters, keyed owner, sequential/tree output, bounded caller workspace and domain-bound jobs; do not imply every reduced-round parameter is secure; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-md6`. |
| `0.244.5` | MD6 - Portable Public API Acceptance | Freeze an independently runnable package-external MD6 fixture covering every declared profile and operation in `brynja-research-hash-md6` before any optimized backend or final evidence sweep. |
| `0.244.6` | MD6 - Optional Backend Implementation And Dispatch | Only after v0.244.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MD6, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.244.7` | MD6 - Final Evidence And Usability Closure | Rerun v0.244.5 public acceptance for every MD6 profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-md6`. |
| `0.245.0` | RIPEMD extensions - Authority And API Freeze | Authenticate Original RIPEMD and RIPEMD extension specifications; freeze every RIPEMD; RIPEMD-128; RIPEMD-256; RIPEMD-320 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-ripemd` before implementation. |
| `0.245.1` | RIPEMD extensions - Complete original RIPEMD and RIPEMD-128 | After v0.245.0, implement complete original RIPEMD and RIPEMD-128 in `brynja-legacy-hash-ripemd` with usable public operations, exact parameters and the frozen RIPEMD extensions contract: Distinct original/128/256/320 IVs and compression, byte/bit profiles and streaming/fixed output; reuse existing RIPEMD-160 owner via explicit legacy facade. |
| `0.245.2` | RIPEMD extensions - Complete RIPEMD-256 and RIPEMD-320 | After v0.245.0, implement complete RIPEMD-256 and RIPEMD-320 in `brynja-legacy-hash-ripemd` with usable public operations, exact parameters and the frozen RIPEMD extensions contract: Distinct original/128/256/320 IVs and compression, byte/bit profiles and streaming/fixed output; reuse existing RIPEMD-160 owner via explicit legacy facade. |
| `0.245.3` | RIPEMD extensions - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by RIPEMD extensions: Distinct original/128/256/320 IVs and compression, byte/bit profiles and streaming/fixed output; reuse existing RIPEMD-160 owner via explicit legacy facade; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-ripemd`. |
| `0.245.4` | RIPEMD extensions - Portable Public API Acceptance | Freeze an independently runnable package-external RIPEMD extensions fixture covering every declared profile and operation in `brynja-legacy-hash-ripemd` before any optimized backend or final evidence sweep. |
| `0.245.5` | RIPEMD extensions - Optional Backend Implementation And Dispatch | Only after v0.245.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for RIPEMD extensions, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.245.6` | RIPEMD extensions - Final Evidence And Usability Closure | Rerun v0.245.4 public acceptance for every RIPEMD extensions profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-ripemd`. |
| `0.246.0` | Snefru - Authority And API Freeze | Authenticate Original Snefru specification and authenticated revisions; freeze every Snefru profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-snefru` before implementation. |
| `0.246.1` | Snefru - Complete Snefru | After v0.246.0, implement complete Snefru in `brynja-legacy-hash-snefru` with usable public operations, exact parameters and the frozen Snefru contract: Exact authenticated output/security-level profiles and byte/bit semantics, independent streaming/fixed output and explicit broken-parameter warnings. |
| `0.246.2` | Snefru - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Snefru: Exact authenticated output/security-level profiles and byte/bit semantics, independent streaming/fixed output and explicit broken-parameter warnings; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-snefru`. |
| `0.246.3` | Snefru - Portable Public API Acceptance | Freeze an independently runnable package-external Snefru fixture covering every declared profile and operation in `brynja-legacy-hash-snefru` before any optimized backend or final evidence sweep. |
| `0.246.4` | Snefru - Optional Backend Implementation And Dispatch | Only after v0.246.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Snefru, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.246.5` | Snefru - Final Evidence And Usability Closure | Rerun v0.246.3 public acceptance for every Snefru profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-snefru`. |
| `0.247.0` | Tiger - Authority And API Freeze | Authenticate Tiger/Tiger2 author specification and vectors; freeze every Tiger; Tiger2 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-tiger` before implementation. |
| `0.247.1` | Tiger - Complete Tiger; Tiger2 | After v0.247.0, implement complete Tiger; Tiger2 in `brynja-legacy-hash-tiger` with usable public operations, exact parameters and the frozen Tiger contract: Distinct Tiger/Tiger2 padding and specified 128/160/192 outputs, streaming byte API and fixed output; reject aliases that silently truncate or switch padding. |
| `0.247.2` | Tiger - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Tiger: Distinct Tiger/Tiger2 padding and specified 128/160/192 outputs, streaming byte API and fixed output; reject aliases that silently truncate or switch padding; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-tiger`. |
| `0.247.3` | Tiger - Portable Public API Acceptance | Freeze an independently runnable package-external Tiger fixture covering every declared profile and operation in `brynja-legacy-hash-tiger` before any optimized backend or final evidence sweep. |
| `0.247.4` | Tiger - Optional Backend Implementation And Dispatch | Only after v0.247.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Tiger, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.247.5` | Tiger - Final Evidence And Usability Closure | Rerun v0.247.3 public acceptance for every Tiger profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-tiger`. |
| `0.248.0` | PANAMA - Authority And API Freeze | Authenticate PANAMA original hash specification and attacks; freeze every PANAMA profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-panama` before implementation. |
| `0.248.1` | PANAMA - Complete PANAMA | After v0.248.0, implement complete PANAMA in `brynja-legacy-hash-panama` with usable public operations, exact parameters and the frozen PANAMA contract: Exact PANAMA hash mode and output/rate, consuming stream state; stream-cipher functionality requires separate named construction, not hidden API. |
| `0.248.2` | PANAMA - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by PANAMA: Exact PANAMA hash mode and output/rate, consuming stream state; stream-cipher functionality requires separate named construction, not hidden API; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-panama`. |
| `0.248.3` | PANAMA - Portable Public API Acceptance | Freeze an independently runnable package-external PANAMA fixture covering every declared profile and operation in `brynja-legacy-hash-panama` before any optimized backend or final evidence sweep. |
| `0.248.4` | PANAMA - Optional Backend Implementation And Dispatch | Only after v0.248.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for PANAMA, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.248.5` | PANAMA - Final Evidence And Usability Closure | Rerun v0.248.3 public acceptance for every PANAMA profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-panama`. |
| `0.249.0` | MDC - Authority And API Freeze | Authenticate Authenticated ISO/ANSI MDC definitions and rights review; freeze every MDC-2; MDC-4 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-mdc` before implementation. |
| `0.249.1` | MDC - Complete MDC-2 | After v0.249.0, implement complete MDC-2 in `brynja-legacy-hash-mdc` with usable public operations, exact parameters and the frozen MDC contract: Exact DES-based MDC-2/4 profiles and padding variants with typed algorithm identity, streaming/fixed output and reuse of isolated DES owner. |
| `0.249.2` | MDC - Complete MDC-4 | After v0.249.0, implement complete MDC-4 in `brynja-legacy-hash-mdc` with usable public operations, exact parameters and the frozen MDC contract: Exact DES-based MDC-2/4 profiles and padding variants with typed algorithm identity, streaming/fixed output and reuse of isolated DES owner. |
| `0.249.3` | MDC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MDC: Exact DES-based MDC-2/4 profiles and padding variants with typed algorithm identity, streaming/fixed output and reuse of isolated DES owner; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-mdc`. |
| `0.249.4` | MDC - Portable Public API Acceptance | Freeze an independently runnable package-external MDC fixture covering every declared profile and operation in `brynja-legacy-hash-mdc` before any optimized backend or final evidence sweep. |
| `0.249.5` | MDC - Optional Backend Implementation And Dispatch | Only after v0.249.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MDC, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.249.6` | MDC - Final Evidence And Usability Closure | Rerun v0.249.4 public acceptance for every MDC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-mdc`. |
| `0.250.0` | MASH - Authority And API Freeze | Authenticate Authenticated ISO MASH definitions and patent/rights review; freeze every MASH-1; MASH-2 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-mash` before implementation. |
| `0.250.1` | MASH - Complete MASH-1 | After v0.250.0, implement complete MASH-1 in `brynja-legacy-hash-mash` with usable public operations, exact parameters and the frozen MASH contract: Exact modulus and exponent parameter domains, bit input/output reduction, checked length and bounded arithmetic workspace; no guessed modulus defaults. |
| `0.250.2` | MASH - Complete MASH-2 | After v0.250.0, implement complete MASH-2 in `brynja-legacy-hash-mash` with usable public operations, exact parameters and the frozen MASH contract: Exact modulus and exponent parameter domains, bit input/output reduction, checked length and bounded arithmetic workspace; no guessed modulus defaults. |
| `0.250.3` | MASH - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MASH: Exact modulus and exponent parameter domains, bit input/output reduction, checked length and bounded arithmetic workspace; no guessed modulus defaults; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-mash`. |
| `0.250.4` | MASH - Portable Public API Acceptance | Freeze an independently runnable package-external MASH fixture covering every declared profile and operation in `brynja-legacy-hash-mash` before any optimized backend or final evidence sweep. |
| `0.250.5` | MASH - Optional Backend Implementation And Dispatch | Only after v0.250.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for MASH, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.250.6` | MASH - Final Evidence And Usability Closure | Rerun v0.250.4 public acceptance for every MASH profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-mash`. |
| `0.251.0` | N-Hash - Authority And API Freeze | Authenticate Original N-Hash specification and cryptanalysis; freeze every N-Hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-hash-nhash` before implementation. |
| `0.251.1` | N-Hash - Complete N-Hash | After v0.251.0, implement complete N-Hash in `brynja-legacy-hash-nhash` with usable public operations, exact parameters and the frozen N-Hash contract: Exact historical N-Hash modes and rounds, fixed/incremental input only as specified; distinct from Numeric nhash and explicit collision warnings. |
| `0.251.2` | N-Hash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by N-Hash: Exact historical N-Hash modes and rounds, fixed/incremental input only as specified; distinct from Numeric nhash and explicit collision warnings; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-hash-nhash`. |
| `0.251.3` | N-Hash - Portable Public API Acceptance | Freeze an independently runnable package-external N-Hash fixture covering every declared profile and operation in `brynja-legacy-hash-nhash` before any optimized backend or final evidence sweep. |
| `0.251.4` | N-Hash - Optional Backend Implementation And Dispatch | Only after v0.251.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for N-Hash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.251.5` | N-Hash - Final Evidence And Usability Closure | Rerun v0.251.3 public acceptance for every N-Hash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-hash-nhash`. |
| `0.252.0` | Universal one-way constructions - Authority And API Freeze | Authenticate Original universal one-way hash publications and selected concrete construction; freeze every Universal one-way hashing profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-uow` before implementation. |
| `0.252.1` | Universal one-way constructions - Complete Universal one-way hashing | After v0.252.0, implement complete Universal one-way hashing in `brynja-research-hash-uow` with usable public operations, exact parameters and the frozen Universal one-way constructions contract: Resolve the umbrella into named target-collision-resistant constructions, exact key sampling order, input/output domains and assumptions; each construction receives a separate patch before implementation. |
| `0.252.2` | Universal one-way constructions - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Universal one-way constructions: Resolve the umbrella into named target-collision-resistant constructions, exact key sampling order, input/output domains and assumptions; each construction receives a separate patch before implementation; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-uow`. |
| `0.252.3` | Universal one-way constructions - Portable Public API Acceptance | Freeze an independently runnable package-external Universal one-way constructions fixture covering every declared profile and operation in `brynja-research-hash-uow` before any optimized backend or final evidence sweep. |
| `0.252.4` | Universal one-way constructions - Optional Backend Implementation And Dispatch | Only after v0.252.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Universal one-way constructions, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.252.5` | Universal one-way constructions - Final Evidence And Usability Closure | Rerun v0.252.3 public acceptance for every Universal one-way constructions profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-uow`. |
| `0.253.0` | Experimental tree hashing - Authority And API Freeze | Authenticate Exact author publications and authenticated experimental profile identity; freeze every KangarooTwelve related experimental tree profiles profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-tree` before implementation. |
| `0.253.1` | Experimental tree hashing - Complete KangarooTwelve related experimental tree profiles | After v0.253.0, implement complete KangarooTwelve related experimental tree profiles in `brynja-research-hash-tree` with usable public operations, exact parameters and the frozen Experimental tree hashing contract: Freeze individually named published tree profiles distinct from RFC 9861, exact framing/rate/round/output and caller-workspace contracts; no arbitrary user permutation marketed secure. |
| `0.253.2` | Experimental tree hashing - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Experimental tree hashing: Freeze individually named published tree profiles distinct from RFC 9861, exact framing/rate/round/output and caller-workspace contracts; no arbitrary user permutation marketed secure; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-tree`. |
| `0.253.3` | Experimental tree hashing - Portable Public API Acceptance | Freeze an independently runnable package-external Experimental tree hashing fixture covering every declared profile and operation in `brynja-research-hash-tree` before any optimized backend or final evidence sweep. |
| `0.253.4` | Experimental tree hashing - Optional Backend Implementation And Dispatch | Only after v0.253.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Experimental tree hashing, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.253.5` | Experimental tree hashing - Final Evidence And Usability Closure | Rerun v0.253.3 public acceptance for every Experimental tree hashing profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-tree`. |
| `0.254.0` | SipHash - Authority And API Freeze | Authenticate SipHash/HalfSipHash authors specifications; freeze every SipHash; HalfSipHash; SipHash parameter variants profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-mac-siphash` before implementation. |
| `0.254.1` | SipHash - Complete SipHash-2-4 and exact 128-bit domain | After v0.254.0, implement complete SipHash-2-4 and exact 128-bit domain in `brynja-mac-siphash` with usable public operations, exact parameters and the frozen SipHash contract: Separate SipHash-2-4/1-3, 64/128 outputs and HalfSipHash profiles after exact admission; keyed one-shot/streaming, redacted owner and constant-time verify; PRF/hash-table use not collision digest. |
| `0.254.2` | SipHash - Complete SipHash-1-3 profiles | After v0.254.0, implement complete SipHash-1-3 profiles in `brynja-mac-siphash` with usable public operations, exact parameters and the frozen SipHash contract: Separate SipHash-2-4/1-3, 64/128 outputs and HalfSipHash profiles after exact admission; keyed one-shot/streaming, redacted owner and constant-time verify; PRF/hash-table use not collision digest. |
| `0.254.3` | SipHash - Complete HalfSipHash profiles | After v0.254.0, implement complete HalfSipHash profiles in `brynja-mac-siphash` with usable public operations, exact parameters and the frozen SipHash contract: Separate SipHash-2-4/1-3, 64/128 outputs and HalfSipHash profiles after exact admission; keyed one-shot/streaming, redacted owner and constant-time verify; PRF/hash-table use not collision digest. |
| `0.254.4` | SipHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by SipHash: Separate SipHash-2-4/1-3, 64/128 outputs and HalfSipHash profiles after exact admission; keyed one-shot/streaming, redacted owner and constant-time verify; PRF/hash-table use not collision digest; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-mac-siphash`. |
| `0.254.5` | SipHash - Portable Public API Acceptance | Freeze an independently runnable package-external SipHash fixture covering every declared profile and operation in `brynja-mac-siphash` before any optimized backend or final evidence sweep. |
| `0.254.6` | SipHash - Optional Backend Implementation And Dispatch | Only after v0.254.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for SipHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.254.7` | SipHash - Final Evidence And Usability Closure | Rerun v0.254.5 public acceptance for every SipHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-mac-siphash`. |
| `0.255.0` | HighwayHash - Authority And API Freeze | Authenticate HighwayHash authors specification and security scope; freeze every HighwayHash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-mac-highway` before implementation. |
| `0.255.1` | HighwayHash - Complete HighwayHash | After v0.255.0, implement complete HighwayHash in `brynja-mac-highway` with usable public operations, exact parameters and the frozen HighwayHash contract: Keyed 64/128/256 identities, portable update/finalize/verify, exact tree/streaming semantics if specified and sealed key owner; no collision-hash inference. |
| `0.255.2` | HighwayHash - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by HighwayHash: Keyed 64/128/256 identities, portable update/finalize/verify, exact tree/streaming semantics if specified and sealed key owner; no collision-hash inference; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-mac-highway`. |
| `0.255.3` | HighwayHash - Portable Public API Acceptance | Freeze an independently runnable package-external HighwayHash fixture covering every declared profile and operation in `brynja-mac-highway` before any optimized backend or final evidence sweep. |
| `0.255.4` | HighwayHash - Optional Backend Implementation And Dispatch | Only after v0.255.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for HighwayHash, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.255.5` | HighwayHash - Final Evidence And Usability Closure | Rerun v0.255.3 public acceptance for every HighwayHash profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-mac-highway`. |
| `0.256.0` | PMAC - Authority And API Freeze | Authenticate Original PMAC publication and revision-specific specification; freeze every PMAC profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-mac-pmac` before implementation. |
| `0.256.1` | PMAC - Complete PMAC | After v0.256.0, implement complete PMAC in `brynja-mac-pmac` with usable public operations, exact parameters and the frozen PMAC contract: Exact PMAC revision and AES key/tag profiles, streaming and bounded parallel jobs, distinct final-block offset, typed secret key and constant-time verify. |
| `0.256.2` | PMAC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by PMAC: Exact PMAC revision and AES key/tag profiles, streaming and bounded parallel jobs, distinct final-block offset, typed secret key and constant-time verify; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-mac-pmac`. |
| `0.256.3` | PMAC - Portable Public API Acceptance | Freeze an independently runnable package-external PMAC fixture covering every declared profile and operation in `brynja-mac-pmac` before any optimized backend or final evidence sweep. |
| `0.256.4` | PMAC - Optional Backend Implementation And Dispatch | Only after v0.256.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for PMAC, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.256.5` | PMAC - Final Evidence And Usability Closure | Rerun v0.256.3 public acceptance for every PMAC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-mac-pmac`. |
| `0.257.0` | UMAC - Authority And API Freeze | Authenticate RFC 4418 and errata; freeze every UMAC profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-mac-umac` before implementation. |
| `0.257.1` | UMAC - Complete NH/L1/L2/L3 universal core | After v0.257.0, implement complete NH/L1/L2/L3 universal core in `brynja-mac-umac` with usable public operations, exact parameters and the frozen UMAC contract: UMAC-32/64/96/128 with exact NH/L1/L2/L3, AES-derived keys, nonce domain and typed tag/verify policy; safe nonce ownership and checked message/work limits. |
| `0.257.2` | UMAC - Complete AES key derivation and nonce-pad lifecycle | After v0.257.0, implement complete AES key derivation and nonce-pad lifecycle in `brynja-mac-umac` with usable public operations, exact parameters and the frozen UMAC contract: UMAC-32/64/96/128 with exact NH/L1/L2/L3, AES-derived keys, nonce domain and typed tag/verify policy; safe nonce ownership and checked message/work limits. |
| `0.257.3` | UMAC - Complete UMAC-32/64/96/128 public authentication | After v0.257.0, implement complete UMAC-32/64/96/128 public authentication in `brynja-mac-umac` with usable public operations, exact parameters and the frozen UMAC contract: UMAC-32/64/96/128 with exact NH/L1/L2/L3, AES-derived keys, nonce domain and typed tag/verify policy; safe nonce ownership and checked message/work limits. |
| `0.257.4` | UMAC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by UMAC: UMAC-32/64/96/128 with exact NH/L1/L2/L3, AES-derived keys, nonce domain and typed tag/verify policy; safe nonce ownership and checked message/work limits; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-mac-umac`. |
| `0.257.5` | UMAC - Portable Public API Acceptance | Freeze an independently runnable package-external UMAC fixture covering every declared profile and operation in `brynja-mac-umac` before any optimized backend or final evidence sweep. |
| `0.257.6` | UMAC - Optional Backend Implementation And Dispatch | Only after v0.257.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for UMAC, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.257.7` | UMAC - Final Evidence And Usability Closure | Rerun v0.257.5 public acceptance for every UMAC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-mac-umac`. |
| `0.258.0` | VMAC - Authority And API Freeze | Authenticate VMAC original publication and versioned reference; freeze every VMAC profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-mac-vmac` before implementation. |
| `0.258.1` | VMAC - Complete universal core and AES pad | After v0.258.0, implement complete universal core and AES pad in `brynja-mac-vmac` with usable public operations, exact parameters and the frozen VMAC contract: Exact VMAC-64/128 profiles, universal-hash key derivation, AES pad and nonce rules, checked input limits and redacted streaming/tag/verify owners. |
| `0.258.2` | VMAC - Complete VMAC-64/128 public authentication | After v0.258.0, implement complete VMAC-64/128 public authentication in `brynja-mac-vmac` with usable public operations, exact parameters and the frozen VMAC contract: Exact VMAC-64/128 profiles, universal-hash key derivation, AES pad and nonce rules, checked input limits and redacted streaming/tag/verify owners. |
| `0.258.3` | VMAC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by VMAC: Exact VMAC-64/128 profiles, universal-hash key derivation, AES pad and nonce rules, checked input limits and redacted streaming/tag/verify owners; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-mac-vmac`. |
| `0.258.4` | VMAC - Portable Public API Acceptance | Freeze an independently runnable package-external VMAC fixture covering every declared profile and operation in `brynja-mac-vmac` before any optimized backend or final evidence sweep. |
| `0.258.5` | VMAC - Optional Backend Implementation And Dispatch | Only after v0.258.4, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for VMAC, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.258.6` | VMAC - Final Evidence And Usability Closure | Rerun v0.258.4 public acceptance for every VMAC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-mac-vmac`. |
| `0.259.0` | Poly1305-AES - Authority And API Freeze | Authenticate Original Poly1305-AES paper and specification; freeze every Poly1305-AES profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-mac-poly1305-aes` before implementation. |
| `0.259.1` | Poly1305-AES - Complete Poly1305-AES | After v0.259.0, implement complete Poly1305-AES in `brynja-legacy-mac-poly1305-aes` with usable public operations, exact parameters and the frozen Poly1305-AES contract: Explicit legacy AES nonce-pad construction over exact Poly1305 owner, r clamping, key/nonce lifecycle and constant-time verification; never plain Poly1305 alias. |
| `0.259.2` | Poly1305-AES - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Poly1305-AES: Explicit legacy AES nonce-pad construction over exact Poly1305 owner, r clamping, key/nonce lifecycle and constant-time verification; never plain Poly1305 alias; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-mac-poly1305-aes`. |
| `0.259.3` | Poly1305-AES - Portable Public API Acceptance | Freeze an independently runnable package-external Poly1305-AES fixture covering every declared profile and operation in `brynja-legacy-mac-poly1305-aes` before any optimized backend or final evidence sweep. |
| `0.259.4` | Poly1305-AES - Optional Backend Implementation And Dispatch | Only after v0.259.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Poly1305-AES, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.259.5` | Poly1305-AES - Final Evidence And Usability Closure | Rerun v0.259.3 public acceptance for every Poly1305-AES profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-mac-poly1305-aes`. |
| `0.260.0` | CMAC extension profiles - Authority And API Freeze | Authenticate SP 800-38B and each concrete legacy cipher profile authority; freeze every OMAC; CMAC additional block-cipher profiles profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-mac-cmac` before implementation. |
| `0.260.1` | CMAC extension profiles - Complete OMAC; CMAC additional block-cipher profiles | After v0.260.0, implement complete OMAC; CMAC additional block-cipher profiles in `brynja-legacy-mac-cmac` with usable public operations, exact parameters and the frozen CMAC extension profiles contract: Reuse AES-CMAC unchanged; individually admit other standardized 64/128-bit block-cipher CMAC profiles over existing cipher owners with distinct block limits/subkeys and tag policy. |
| `0.260.2` | CMAC extension profiles - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by CMAC extension profiles: Reuse AES-CMAC unchanged; individually admit other standardized 64/128-bit block-cipher CMAC profiles over existing cipher owners with distinct block limits/subkeys and tag policy; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-mac-cmac`. |
| `0.260.3` | CMAC extension profiles - Portable Public API Acceptance | Freeze an independently runnable package-external CMAC extension profiles fixture covering every declared profile and operation in `brynja-legacy-mac-cmac` before any optimized backend or final evidence sweep. |
| `0.260.4` | CMAC extension profiles - Optional Backend Implementation And Dispatch | Only after v0.260.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for CMAC extension profiles, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.260.5` | CMAC extension profiles - Final Evidence And Usability Closure | Rerun v0.260.3 public acceptance for every CMAC extension profiles profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-mac-cmac`. |
| `0.261.0` | HMAC catalogue adapters - Authority And API Freeze | Authenticate RFC 2104 plus each admitted hash profile; policy classification per adapter; freeze every HMAC catalogue hash adapters profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-legacy-mac-hmac` before implementation. |
| `0.261.1` | HMAC catalogue adapters - Complete HMAC catalogue hash adapters | After v0.261.0, implement complete HMAC catalogue hash adapters in `brynja-legacy-mac-hmac` with usable public operations, exact parameters and the frozen HMAC catalogue adapters contract: Bind newly completed eligible fixed-output hashes to exact modern or isolated legacy HMAC owners only through sealed hardened capabilities; never auto-admit every Digest or an XOF. |
| `0.261.2` | HMAC catalogue adapters - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by HMAC catalogue adapters: Bind newly completed eligible fixed-output hashes to exact modern or isolated legacy HMAC owners only through sealed hardened capabilities; never auto-admit every Digest or an XOF; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-legacy-mac-hmac`. |
| `0.261.3` | HMAC catalogue adapters - Portable Public API Acceptance | Freeze an independently runnable package-external HMAC catalogue adapters fixture covering every declared profile and operation in `brynja-legacy-mac-hmac` before any optimized backend or final evidence sweep. |
| `0.261.4` | HMAC catalogue adapters - Optional Backend Implementation And Dispatch | Only after v0.261.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for HMAC catalogue adapters, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.261.5` | HMAC catalogue adapters - Final Evidence And Usability Closure | Rerun v0.261.3 public acceptance for every HMAC catalogue adapters profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-legacy-mac-hmac`. |
| `0.262.0` | Salsa20 core for scrypt - Authority And API Freeze | Authenticate RFC 7914; freeze every Salsa20/8; BlockMix; ROMix profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-password-hash-scrypt` before implementation. |
| `0.262.1` | Salsa20 core for scrypt - Complete Salsa20/8 | After v0.262.0, implement complete Salsa20/8 in `brynja-password-hash-scrypt` with usable public operations, exact parameters and the frozen Salsa20 core for scrypt contract: Complete exact Salsa20/8 primitive and scrypt BlockMix/ROMix with caller-owned aligned workspace, checked r/N arithmetic and cleanup; no public stream cipher claim. |
| `0.262.2` | Salsa20 core for scrypt - Complete BlockMix | After v0.262.0, implement complete BlockMix in `brynja-password-hash-scrypt` with usable public operations, exact parameters and the frozen Salsa20 core for scrypt contract: Complete exact Salsa20/8 primitive and scrypt BlockMix/ROMix with caller-owned aligned workspace, checked r/N arithmetic and cleanup; no public stream cipher claim. |
| `0.262.3` | Salsa20 core for scrypt - Complete ROMix | After v0.262.0, implement complete ROMix in `brynja-password-hash-scrypt` with usable public operations, exact parameters and the frozen Salsa20 core for scrypt contract: Complete exact Salsa20/8 primitive and scrypt BlockMix/ROMix with caller-owned aligned workspace, checked r/N arithmetic and cleanup; no public stream cipher claim. |
| `0.262.4` | Salsa20 core for scrypt - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Salsa20 core for scrypt: Complete exact Salsa20/8 primitive and scrypt BlockMix/ROMix with caller-owned aligned workspace, checked r/N arithmetic and cleanup; no public stream cipher claim; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-password-hash-scrypt`. |
| `0.262.5` | Salsa20 core for scrypt - Portable Public API Acceptance | Freeze an independently runnable package-external Salsa20 core for scrypt fixture covering every declared profile and operation in `brynja-password-hash-scrypt` before any optimized backend or final evidence sweep. |
| `0.262.6` | Salsa20 core for scrypt - Optional Backend Implementation And Dispatch | Only after v0.262.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Salsa20 core for scrypt, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.262.7` | Salsa20 core for scrypt - Final Evidence And Usability Closure | Rerun v0.262.5 public acceptance for every Salsa20 core for scrypt profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-password-hash-scrypt`. |
| `0.263.0` | scrypt - Authority And API Freeze | Authenticate RFC 7914 and exact stored-format specification; freeze every scrypt profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-password-hash-scrypt` before implementation. |
| `0.263.1` | scrypt - Complete scrypt | After v0.263.0, implement complete scrypt in `brynja-password-hash-scrypt` with usable public operations, exact parameters and the frozen scrypt contract: Derive and constant-time verify with typed N/r/p/output limits, caller salt/entropy/workspace, preflight memory/work admission, cancellation and portable stored-parameter codec. |
| `0.263.2` | scrypt - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by scrypt: Derive and constant-time verify with typed N/r/p/output limits, caller salt/entropy/workspace, preflight memory/work admission, cancellation and portable stored-parameter codec; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-password-hash-scrypt`. |
| `0.263.3` | scrypt - Portable Public API Acceptance | Freeze an independently runnable package-external scrypt fixture covering every declared profile and operation in `brynja-password-hash-scrypt` before any optimized backend or final evidence sweep. |
| `0.263.4` | scrypt - Optional Backend Implementation And Dispatch | Only after v0.263.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for scrypt, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.263.5` | scrypt - Final Evidence And Usability Closure | Rerun v0.263.3 public acceptance for every scrypt profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-password-hash-scrypt`. |
| `0.264.0` | EksBlowfish and bcrypt - Authority And API Freeze | Authenticate Original bcrypt paper and authenticated OpenBSD format revisions; freeze every bcrypt; EksBlowfish profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-password-hash-bcrypt` before implementation. |
| `0.264.1` | EksBlowfish and bcrypt - Complete complete EksBlowfish schedule | After v0.264.0, implement complete complete EksBlowfish schedule in `brynja-password-hash-bcrypt` with usable public operations, exact parameters and the frozen EksBlowfish and bcrypt contract: Reuse complete legacy Blowfish round owner but implement exact EksBlowfish schedule; versioned 2a/2b/2y parsing/encoding, password-byte and 72-byte policy, cost preflight, derive/verify/needs-rehash. |
| `0.264.2` | EksBlowfish and bcrypt - Complete bcrypt 2a/2b/2y derive and verify | After v0.264.0, implement complete bcrypt 2a/2b/2y derive and verify in `brynja-password-hash-bcrypt` with usable public operations, exact parameters and the frozen EksBlowfish and bcrypt contract: Reuse complete legacy Blowfish round owner but implement exact EksBlowfish schedule; versioned 2a/2b/2y parsing/encoding, password-byte and 72-byte policy, cost preflight, derive/verify/needs-rehash. |
| `0.264.3` | EksBlowfish and bcrypt - Complete stored-format codecs and upgrade policy | After v0.264.0, implement complete stored-format codecs and upgrade policy in `brynja-password-hash-bcrypt` with usable public operations, exact parameters and the frozen EksBlowfish and bcrypt contract: Reuse complete legacy Blowfish round owner but implement exact EksBlowfish schedule; versioned 2a/2b/2y parsing/encoding, password-byte and 72-byte policy, cost preflight, derive/verify/needs-rehash. |
| `0.264.4` | EksBlowfish and bcrypt - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by EksBlowfish and bcrypt: Reuse complete legacy Blowfish round owner but implement exact EksBlowfish schedule; versioned 2a/2b/2y parsing/encoding, password-byte and 72-byte policy, cost preflight, derive/verify/needs-rehash; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-password-hash-bcrypt`. |
| `0.264.5` | EksBlowfish and bcrypt - Portable Public API Acceptance | Freeze an independently runnable package-external EksBlowfish and bcrypt fixture covering every declared profile and operation in `brynja-password-hash-bcrypt` before any optimized backend or final evidence sweep. |
| `0.264.6` | EksBlowfish and bcrypt - Optional Backend Implementation And Dispatch | Only after v0.264.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for EksBlowfish and bcrypt, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.264.7` | EksBlowfish and bcrypt - Final Evidence And Usability Closure | Rerun v0.264.5 public acceptance for every EksBlowfish and bcrypt profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-password-hash-bcrypt`. |
| `0.265.0` | Balloon - Authority And API Freeze | Authenticate Original Balloon hashing paper and exact reference profile; freeze every Balloon profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-password-hash-balloon` before implementation. |
| `0.265.1` | Balloon - Complete Balloon | After v0.265.0, implement complete Balloon in `brynja-password-hash-balloon` with usable public operations, exact parameters and the frozen Balloon contract: Freeze each named Balloon variant/hash dependency and time/space/delta parameters; explicit salt, bounded caller workspace, derive/verify and parameter encoding without calling it Argon2. |
| `0.265.2` | Balloon - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Balloon: Freeze each named Balloon variant/hash dependency and time/space/delta parameters; explicit salt, bounded caller workspace, derive/verify and parameter encoding without calling it Argon2; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-password-hash-balloon`. |
| `0.265.3` | Balloon - Portable Public API Acceptance | Freeze an independently runnable package-external Balloon fixture covering every declared profile and operation in `brynja-password-hash-balloon` before any optimized backend or final evidence sweep. |
| `0.265.4` | Balloon - Optional Backend Implementation And Dispatch | Only after v0.265.3, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Balloon, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.265.5` | Balloon - Final Evidence And Usability Closure | Rerun v0.265.3 public acceptance for every Balloon profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-password-hash-balloon`. |
| `0.266.0` | Poseidon - Authority And API Freeze | Authenticate Original Poseidon/Poseidon2 papers and versioned parameter generators; freeze every Poseidon; Poseidon2 profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-poseidon` before implementation. |
| `0.266.1` | Poseidon - Complete Poseidon parameterized native hash | After v0.266.0, implement complete Poseidon parameterized native hash in `brynja-research-hash-poseidon` with usable public operations, exact parameters and the frozen Poseidon contract: Distinct original/Poseidon2 parameter sets, prime-field and arity types, audited constants/generation transcript, compression/sponge domain and canonical field IO; witness-owned hardened API. |
| `0.266.2` | Poseidon - Complete Poseidon2 parameterized native hash | After v0.266.0, implement complete Poseidon2 parameterized native hash in `brynja-research-hash-poseidon` with usable public operations, exact parameters and the frozen Poseidon contract: Distinct original/Poseidon2 parameter sets, prime-field and arity types, audited constants/generation transcript, compression/sponge domain and canonical field IO; witness-owned hardened API. |
| `0.266.3` | Poseidon - Complete parameter generation and circuit equivalence fixtures | After v0.266.0, implement complete parameter generation and circuit equivalence fixtures in `brynja-research-hash-poseidon` with usable public operations, exact parameters and the frozen Poseidon contract: Distinct original/Poseidon2 parameter sets, prime-field and arity types, audited constants/generation transcript, compression/sponge domain and canonical field IO; witness-owned hardened API. |
| `0.266.4` | Poseidon - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Poseidon: Distinct original/Poseidon2 parameter sets, prime-field and arity types, audited constants/generation transcript, compression/sponge domain and canonical field IO; witness-owned hardened API; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-poseidon`. |
| `0.266.5` | Poseidon - Portable Public API Acceptance | Freeze an independently runnable package-external Poseidon fixture covering every declared profile and operation in `brynja-research-hash-poseidon` before any optimized backend or final evidence sweep. |
| `0.266.6` | Poseidon - Final Evidence And Usability Closure | Rerun v0.266.5 public acceptance for every Poseidon profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-poseidon`. |
| `0.267.0` | Rescue - Authority And API Freeze | Authenticate Original Rescue and Rescue-Prime specifications; freeze every Rescue; Rescue-Prime profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-rescue` before implementation. |
| `0.267.1` | Rescue - Complete Rescue | After v0.267.0, implement complete Rescue in `brynja-research-hash-rescue` with usable public operations, exact parameters and the frozen Rescue contract: Distinct field/arity/alpha/round matrices and generated constants; typed absorb/squeeze or compression profile and canonical field IO with witness cleanup. |
| `0.267.2` | Rescue - Complete Rescue-Prime | After v0.267.0, implement complete Rescue-Prime in `brynja-research-hash-rescue` with usable public operations, exact parameters and the frozen Rescue contract: Distinct field/arity/alpha/round matrices and generated constants; typed absorb/squeeze or compression profile and canonical field IO with witness cleanup. |
| `0.267.3` | Rescue - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Rescue: Distinct field/arity/alpha/round matrices and generated constants; typed absorb/squeeze or compression profile and canonical field IO with witness cleanup; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-rescue`. |
| `0.267.4` | Rescue - Portable Public API Acceptance | Freeze an independently runnable package-external Rescue fixture covering every declared profile and operation in `brynja-research-hash-rescue` before any optimized backend or final evidence sweep. |
| `0.267.5` | Rescue - Final Evidence And Usability Closure | Rerun v0.267.4 public acceptance for every Rescue profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-rescue`. |
| `0.268.0` | MiMC - Authority And API Freeze | Authenticate Original MiMC publication and frozen construction profiles; freeze every MiMC profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-mimc` before implementation. |
| `0.268.1` | MiMC - Complete MiMC | After v0.268.0, implement complete MiMC in `brynja-research-hash-mimc` with usable public operations, exact parameters and the frozen MiMC contract: Exact field/exponent/round/constant and permutation/hash construction profiles, not a generic exponentiation hash; domain-tagged IO and bounded witness owner. |
| `0.268.2` | MiMC - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by MiMC: Exact field/exponent/round/constant and permutation/hash construction profiles, not a generic exponentiation hash; domain-tagged IO and bounded witness owner; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-mimc`. |
| `0.268.3` | MiMC - Portable Public API Acceptance | Freeze an independently runnable package-external MiMC fixture covering every declared profile and operation in `brynja-research-hash-mimc` before any optimized backend or final evidence sweep. |
| `0.268.4` | MiMC - Final Evidence And Usability Closure | Rerun v0.268.3 public acceptance for every MiMC profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-mimc`. |
| `0.269.0` | Pedersen - Authority And API Freeze | Authenticate Original Pedersen construction and each exact protocol profile; freeze every Pedersen hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-pedersen` before implementation. |
| `0.269.1` | Pedersen - Complete Pedersen hash | After v0.269.0, implement complete Pedersen hash in `brynja-research-hash-pedersen` with usable public operations, exact parameters and the frozen Pedersen contract: Exact group/subgroup, independent generators, bit/field input domain and length binding; hash and blinded commitment separate typed APIs, complete commit/open verification where admitted. |
| `0.269.2` | Pedersen - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Pedersen: Exact group/subgroup, independent generators, bit/field input domain and length binding; hash and blinded commitment separate typed APIs, complete commit/open verification where admitted; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-pedersen`. |
| `0.269.3` | Pedersen - Portable Public API Acceptance | Freeze an independently runnable package-external Pedersen fixture covering every declared profile and operation in `brynja-research-hash-pedersen` before any optimized backend or final evidence sweep. |
| `0.269.4` | Pedersen - Final Evidence And Usability Closure | Rerun v0.269.3 public acceptance for every Pedersen profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-pedersen`. |
| `0.270.0` | Sinsemilla - Authority And API Freeze | Authenticate Sinsemilla original specification and protocol parameter authority; freeze every Sinsemilla profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-hash-sinsemilla` before implementation. |
| `0.270.1` | Sinsemilla - Complete Sinsemilla | After v0.270.0, implement complete Sinsemilla in `brynja-research-hash-sinsemilla` with usable public operations, exact parameters and the frozen Sinsemilla contract: Exact curve/generator derivation, chunk encoding, incomplete-addition failure and domain limits; typed hash and commit/verify profiles over reused curve owners. |
| `0.270.2` | Sinsemilla - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Sinsemilla: Exact curve/generator derivation, chunk encoding, incomplete-addition failure and domain limits; typed hash and commit/verify profiles over reused curve owners; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-research-hash-sinsemilla`. |
| `0.270.3` | Sinsemilla - Portable Public API Acceptance | Freeze an independently runnable package-external Sinsemilla fixture covering every declared profile and operation in `brynja-research-hash-sinsemilla` before any optimized backend or final evidence sweep. |
| `0.270.4` | Sinsemilla - Final Evidence And Usability Closure | Rerun v0.270.3 public acceptance for every Sinsemilla profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-hash-sinsemilla`. |
| `0.271.0` | Perceptual image hashes - Authority And API Freeze | Authenticate Original pHash/dhash and each named media-profile specification; freeze every pHash; dhash; perceptual aHash; wHash; color hash profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-perceptual-hash-image` before implementation. |
| `0.271.1` | Perceptual image hashes - Complete pHash DCT profile | After v0.271.0, implement complete pHash DCT profile in `brynja-perceptual-hash-image` with usable public operations, exact parameters and the frozen Perceptual image hashes contract: Separate exact DCT/difference/average/wavelet/color profiles; caller-normalized pixels, dimensions/stride/color/alpha rules and workspace, stable bit encoding and distance APIs; no image decoder. |
| `0.271.2` | Perceptual image hashes - Complete dhash and average hash profiles | After v0.271.0, implement complete dhash and average hash profiles in `brynja-perceptual-hash-image` with usable public operations, exact parameters and the frozen Perceptual image hashes contract: Separate exact DCT/difference/average/wavelet/color profiles; caller-normalized pixels, dimensions/stride/color/alpha rules and workspace, stable bit encoding and distance APIs; no image decoder. |
| `0.271.3` | Perceptual image hashes - Complete wavelet and color profiles | After v0.271.0, implement complete wavelet and color profiles in `brynja-perceptual-hash-image` with usable public operations, exact parameters and the frozen Perceptual image hashes contract: Separate exact DCT/difference/average/wavelet/color profiles; caller-normalized pixels, dimensions/stride/color/alpha rules and workspace, stable bit encoding and distance APIs; no image decoder. |
| `0.271.4` | Perceptual image hashes - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Perceptual image hashes: Separate exact DCT/difference/average/wavelet/color profiles; caller-normalized pixels, dimensions/stride/color/alpha rules and workspace, stable bit encoding and distance APIs; no image decoder; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-perceptual-hash-image`. |
| `0.271.5` | Perceptual image hashes - Portable Public API Acceptance | Freeze an independently runnable package-external Perceptual image hashes fixture covering every declared profile and operation in `brynja-perceptual-hash-image` before any optimized backend or final evidence sweep. |
| `0.271.6` | Perceptual image hashes - Final Evidence And Usability Closure | Rerun v0.271.5 public acceptance for every Perceptual image hashes profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-perceptual-hash-image`. |
| `0.272.0` | PDQ - Authority And API Freeze | Authenticate Meta ThreatExchange PDQ specification and licensed vectors; freeze every PDQ profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-perceptual-hash-pdq` before implementation. |
| `0.272.1` | PDQ - Complete PDQ | After v0.272.0, implement complete PDQ in `brynja-perceptual-hash-pdq` with usable public operations, exact parameters and the frozen PDQ contract: Exact PDQ normalized image profile, quality score and hash distance with caller workspace; explicit normalization owner and threshold metadata, no security digest traits. |
| `0.272.2` | PDQ - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by PDQ: Exact PDQ normalized image profile, quality score and hash distance with caller workspace; explicit normalization owner and threshold metadata, no security digest traits; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-perceptual-hash-pdq`. |
| `0.272.3` | PDQ - Portable Public API Acceptance | Freeze an independently runnable package-external PDQ fixture covering every declared profile and operation in `brynja-perceptual-hash-pdq` before any optimized backend or final evidence sweep. |
| `0.272.4` | PDQ - Final Evidence And Usability Closure | Rerun v0.272.3 public acceptance for every PDQ profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-perceptual-hash-pdq`. |
| `0.273.0` | Video perceptual hashes - Authority And API Freeze | Authenticate Exact author video-profile publications and lawful fixtures; freeze every video profiles profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-perceptual-hash-video` before implementation. |
| `0.273.1` | Video perceptual hashes - Complete exact frame-level profiles | After v0.273.0, implement complete exact frame-level profiles in `brynja-perceptual-hash-video` with usable public operations, exact parameters and the frozen Video perceptual hashes contract: Freeze individually named TMK/PDQF or other authenticated profiles, caller-decoded timestamped frames, bounded temporal workspace and compare APIs; no codec/network side effects. |
| `0.273.2` | Video perceptual hashes - Complete temporal composition and comparison | After v0.273.0, implement complete temporal composition and comparison in `brynja-perceptual-hash-video` with usable public operations, exact parameters and the frozen Video perceptual hashes contract: Freeze individually named TMK/PDQF or other authenticated profiles, caller-decoded timestamped frames, bounded temporal workspace and compare APIs; no codec/network side effects. |
| `0.273.3` | Video perceptual hashes - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Video perceptual hashes: Freeze individually named TMK/PDQF or other authenticated profiles, caller-decoded timestamped frames, bounded temporal workspace and compare APIs; no codec/network side effects; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-perceptual-hash-video`. |
| `0.273.4` | Video perceptual hashes - Portable Public API Acceptance | Freeze an independently runnable package-external Video perceptual hashes fixture covering every declared profile and operation in `brynja-perceptual-hash-video` before any optimized backend or final evidence sweep. |
| `0.273.5` | Video perceptual hashes - Final Evidence And Usability Closure | Rerun v0.273.4 public acceptance for every Video perceptual hashes profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-perceptual-hash-video`. |
| `0.274.0` | Placement - Authority And API Freeze | Authenticate Original consistent/rendezvous/jump hashing publications; freeze every consistent hashing; rendezvous hashing; jump hashing profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-placement` before implementation. |
| `0.274.1` | Placement - Complete consistent hashing | After v0.274.0, implement complete consistent hashing in `brynja-placement` with usable public operations, exact parameters and the frozen Placement contract: Distinct stable node-ID/weight profiles, assign/remove/rebalance and deterministic lookup, injected noncrypto hash policy with domain separation; no cryptographic or protocol routing authority. |
| `0.274.2` | Placement - Complete rendezvous and weighted profiles | After v0.274.0, implement complete rendezvous and weighted profiles in `brynja-placement` with usable public operations, exact parameters and the frozen Placement contract: Distinct stable node-ID/weight profiles, assign/remove/rebalance and deterministic lookup, injected noncrypto hash policy with domain separation; no cryptographic or protocol routing authority. |
| `0.274.3` | Placement - Complete jump hashing | After v0.274.0, implement complete jump hashing in `brynja-placement` with usable public operations, exact parameters and the frozen Placement contract: Distinct stable node-ID/weight profiles, assign/remove/rebalance and deterministic lookup, injected noncrypto hash policy with domain separation; no cryptographic or protocol routing authority. |
| `0.274.4` | Placement - Lifecycle And Composition Closure | Close every ordinary, secret-bearing, output and composition profile promised by Placement: Distinct stable node-ID/weight profiles, assign/remove/rebalance and deterministic lookup, injected noncrypto hash policy with domain separation; no cryptographic or protocol routing authority; make cleanup, misuse and unsupported-operation boundaries executable in `brynja-placement`. |
| `0.274.5` | Placement - Portable Public API Acceptance | Freeze an independently runnable package-external Placement fixture covering every declared profile and operation in `brynja-placement` before any optimized backend or final evidence sweep. |
| `0.274.6` | Placement - Optional Backend Implementation And Dispatch | Only after v0.274.5, assess and implement useful exact-operation x86_64 and AArch64/Apple Arm acceleration for Placement, or record a justified scalar-only disposition; never make native availability a portable API requirement. |
| `0.274.7` | Placement - Final Evidence And Usability Closure | Rerun v0.274.5 public acceptance for every Placement profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-placement`. |
| `0.275.0` | SSL1 provenance - Authority And API Freeze | Authenticate Authenticated SSL1 archival material; absence is a release scope blocker requiring explicit decision; freeze every SSL 1.0 research profile, security classification, lawful vectors, prerequisites and the complete public API contract for `brynja-research-ssl1` before implementation. |
| `0.275.1` | SSL1 provenance - Complete SSL 1.0 research | After v0.275.0, implement complete SSL 1.0 research in `brynja-research-ssl1` with usable public operations, exact parameters and the frozen SSL1 provenance contract: Authenticate source lineage and reconstruct only documented bounded parse/encode research fixtures; publish=false, no secure transport and no production credentials; cannot invent a full SSL1 specification. |
| `0.275.2` | SSL1 provenance - Portable Public API Acceptance | Freeze an independently runnable package-external SSL1 provenance fixture covering every declared profile and operation in `brynja-research-ssl1` before any optimized backend or final evidence sweep. |
| `0.275.3` | SSL1 provenance - Final Evidence And Usability Closure | Rerun v0.275.2 public acceptance for every SSL1 provenance profile through portable and each admitted route; freeze exact native or scalar-only evidence, limitations, specialist review and final package status for `brynja-research-ssl1`. |
| `0.276.0` | Complete Catalogue Reuse And Consumer Closure | Reconcile all new catalogue variants with the previously accepted SHA-1/MD2/MD5/RIPEMD-160/GOST/SM3, SHA-2/SHA-3/SP 800-185, BLAKE2b, HMAC/GMAC/Poly1305/CMAC, PBKDF2 and Argon2 owners; close exact transitive implementation and public API edges without duplicate code. |
| `0.276.1` | Catalogue Extended HMAC And Cipher Profile Acceptance | Close every newly promised HMAC, CMAC, password and tree-construction adapter against its exact hardened hash or cipher prerequisite; separately type modern and legacy selections, including all newly required standalone construction operations. |
| `0.277.0` | Catalogue Curated Facades And Independent Package Selection | Add only a small default-off reviewed modern brynja-hash facade and optional brynja/hash edge; independently selected utility, MAC, password, legacy and research packages never become an all-hashes feature or reverse dependency. |
| `0.277.1` | Catalogue Ecosystem Adapter And Documentation Acceptance | Extend the existing v0.151.1-v0.151.7 adapter boundaries for admitted new families: standard digest/MAC traits only in separately locked pure-Rust downstream adapters, explicit hosted streaming wrappers and complete public examples without core dependency expansion. |
| `0.278.0` | Complete Catalogue Cross-Platform Evidence Closure | Close per-profile portable and admitted-backend evidence across Linux, Windows, BSD, macOS, Android, iOS and no_std targets with exact MSRV/latest compiler claims; retain future Aesynx ports and the explicit post-1.0 RISC-V native qualification boundary. |
| `0.278.1` | Catalogue Standards Update And Source-Blocked Closure | Refresh every frozen authority, erratum, security classification, parameter set and rights decision at the integrated scope freeze; no ambiguous source, missing profile or promised operation can be silently marked complete or moved post-1.0. |
| `0.279.0` | Complete Catalogue Independent Review And Remediation | Obtain per-domain cryptographic, password, field/research, utility, parser and systems specialist reviews of the entire new catalogue and inherited primitive dependencies; fix findings, add permanent regressions and rebuild affected evidence before final integration. |
| `0.279.1` | Whole-Ecosystem Status And Deployment Claim Closure | Separate implementation, named independent review, research/legacy risk, native-backend admission and certificate-bound FIPS status for every package/profile; prove public tables and deployment guidance cannot promote a weak checksum, research construction or unadmitted backend into secure defaults. |
| `0.280.0` | Expanded Catalogue Cumulative Pentest Checkpoint | Pentest the complete delta after v0.275.0 through v0.280.0, including all intervening patch milestones, and independently review full catalogue scope closure; publish only changed selected package closures with the facade last after green GitHub and CodeQL. |
| `0.281.0` | Integrated Complete Brynja Ecosystem Package Freeze | Reconcile the complete modern TLS, DTLS, QUIC-TLS integration, PKIX, cryptography, FIPS, OpenPGP, all catalogue semantic domains, named legacy protocol and facade dependency graph, shared primitive identities, feature combinations, package versions, documentation and security claims; prove no protocol stack creates implicit cross-protocol keys, trust, configuration, fallback, state or release authority. |
| `0.281.1` | Whole-Project Public Operation-Direction Closure | Before the integrated release rehearsal, reconcile every promised primitive, construction, codec, container and protocol role with its implemented public package, complete safe operation directions and exact acceptance evidence; block 1.0 if any reusable capability is only accessible through private protocol code or a one-sided adapter. |
| `0.282.0` | Integrated Clean-Room Release Rehearsal | Repeat reproducible clean-room builds, package installation, artifact comparison, rollback, key-compromise, incident and disaster-recovery exercises for the complete protocol, hash, MAC, password, checksum, utility and separately selected legacy/research publication set on every promised Rust version and representative supported targets. |
| `0.283.0` | Final Whole-Project External Audit | Complete an exact-candidate independent audit spanning standards traceability, cryptography, PKI, TLS, DTLS, QUIC, PQ, FIPS boundaries and claims, OpenPGP, the entire catalogue including utility/field/perceptual/research domains, optional adapters, platform behavior, supply chain, release controls and all cross-component trust boundaries. |
| `0.284.0` | Final Whole-Project Remediation And Retest | Remediate every final-audit finding, add permanent regressions, rebuild every affected evidence artifact, repeat all applicable external reviews and obtain clean retests with no unresolved critical or high finding before release-candidate freeze. |
| `0.285.0` | Final Pre-RC Pentest And Publication Gate | Complete the cumulative v0.280.0-through-v0.285.0 pentest, commit its PASS report, freeze the selected crates.io closure and release notes, and require green GitHub, CodeQL, package, reproducibility and publication-dry-run evidence before the exact production candidate. |
| `1.0.0-rc.1` | Exact Production Candidate | Build final artifacts once and freeze source, compiler, flags, archives, SBOM, checksums, provenance, documentation, and the pentested candidate state with its committed report. |
| `1.0.0` | First Serious Production-Ready Complete Brynja Release | Promote only the byte-identical approved candidate covering modern TLS, DTLS, QUIC-TLS integration, PKIX, OpenPGP, the complete admitted hash/construction and utility catalogue, and the separately selected complete named legacy ecosystem with research-only packages distinctly non-production without rebuild, source change, metadata drift, or expanded capability claim. |

## Unified Pre-1.0 Catalogue And Legacy Boundary

Every named legacy protocol in the pre-1.0 workspace now closes through
v0.180.24 and blocks `1.0.0`; its crates may retain independent package SemVer,
but they no longer use a nonblocking subset sequence. SSL 1 research is now
assigned v0.275.0-v0.275.3, source-gated, unpublished and without a secure
transport claim. All 104 original hash inventory rows map to existing owners
or detailed new families in
[CATALOGUE_SCOPE_REGISTER.json](CATALOGUE_SCOPE_REGISTER.json).
v0.181.0-v0.280.0 closes the expanded catalogue, followed by integration,
rehearsal, audit, remediation and the v0.285.0 checkpoint. The
[unified contracts](RELEASE_PLAN.md#unified-catalogue-scope-and-api-contracts)
define complete APIs, cleanup, source admission and acceptance-before-evidence.
Unissued future standards are monitored, not claimed implemented; applicable
new requirements reopen this plan before final freeze.

## Post-1.0 RISC-V Native Qualification

RISC-V operations may use the registered native host before v1.0.0 only when
their exact required feature bundle is present on every eligible hart.
Unavailable instruction-specific paths remain explicitly QEMU/codegen-only,
unadmitted, and scalar-backed. After v1.0.0, the project will publish
reproducible capture instructions and seek qualifying real-hardware evidence
from Rust and RISC-V community members. Candidate reports remain distinct from
backend admission and cannot create performance, side-channel, independent-
review, or FIPS claims. The complete campaign and acceptance boundary are in
[`POST_1_0_RISCV_QUALIFICATION_PLAN.md`](POST_1_0_RISCV_QUALIFICATION_PLAN.md).
