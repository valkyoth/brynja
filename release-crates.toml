[release]
version = "0.17.0"
milestone = "0.17.0"
baseline = "0.15.0"
cumulative_milestones = ["0.16.0", "0.17.0"]
policy = "independent"
stage = "internal"
exceptional = true
exception_reason = "The v0.17.0 FIPS-aware architecture received an exceptional repository-owner assessment; two High operation-only approval and SSP-destruction-source findings are remediated and awaiting retest before tagging."

[crates."brynja"]
previous_version = "0.16.0"
version = "0.17.0"
change = "metadata"
publish = false
reason = "Advance to the v0.17.0 FIPS-aware architecture milestone with inert service, environment, self-test, permanent-failure, and SSP contracts and no crates.io publication."

[crates."brynja-core"]
previous_version = "0.8.0"
version = "0.8.0"
change = "code"
publish = false
reason = "Retain published 0.8.0 while adding the inert FIPS-aware provider architecture for the future v0.20.0 checkpoint."

[crates."brynja-crypto"]
previous_version = "0.1.1"
version = "0.1.1"
change = "metadata"
publish = false
reason = "Retain published 0.1.1 while reconciling checkpoint documentation; no cryptographic effect is implemented."

[crates."brynja-crypto-cpu"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 while reconciling checkpoint documentation; no executable kernel is admitted."

[crates."brynja-crypto-cpu-std"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 while reconciling checkpoint documentation; no runtime detector is implemented."

[crates."brynja-pki"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; PKI remains unimplemented."

[crates."brynja-tls"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; the TLS router remains unimplemented."

[crates."brynja-tls12"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; TLS 1.2 remains unimplemented."

[crates."brynja-tls13"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; TLS 1.3 remains unimplemented."

[crates."brynja-tls13-handshake"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; the TLS 1.3 handshake engine remains unimplemented."

[crates."brynja-quic-tls"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; QUIC-TLS remains unimplemented."

[crates."brynja-sanitization"]
previous_version = "0.1.0"
version = "0.1.0"
change = "metadata"
publish = false
reason = "Retain published 0.1.0 while reconciling checkpoint documentation; it remains outside all facades, engines, default features, and FIPS boundaries."

[crates."brynja-dtls"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while updating checkpoint documentation; DTLS remains unimplemented."

[crates."brynja-platform"]
previous_version = "0.1.7"
version = "0.1.7"
change = "metadata"
publish = false
reason = "Retain published 0.1.7 while documenting that pending provider effects remain downstream and unimplemented."

[crates."brynja-legacy"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls11"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-tls10"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl3"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-ssl2"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-wtls"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-pct"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-legacy-snp"]
previous_version = "unpublished"
version = "0.1.0"
change = "unpublished"
publish = false
reason = "Foundation boundary only; no protocol implementation or release approval exists."

[crates."brynja-research-ssl1"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only SSL 1 reconstruction research; publication and secure transport claims are permanently prohibited."

[crates."brynja-test-support"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Retain deterministic clock, random, and RFC 9850 support; v0.17 FIPS-aware tests remain internal to brynja-core and production-unreachable."

[crates."brynja-interop"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only interoperability runner; it is never a crates.io product."

[crates."brynja-xtask"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only maintenance tooling; it is never a crates.io product."

[crates."brynja-proofs"]
previous_version = "unpublished"
version = "0.1.0"
change = "repository"
publish = false
reason = "Repository-only proof harnesses; they are never a crates.io product."
